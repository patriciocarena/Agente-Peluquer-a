/**
 * Placeholder module — apps/dashboard is a declared stub (package.json +
 * dependencies only) per Plan 01-01. No app/page code exists yet; Phase 2
 * builds the actual Next.js App Router UI (app/(tenant)/, app/(superadmin)/).
 *
 * This file exists solely so the package has a valid TS input for
 * `tsc --noEmit` until real app code lands.
 */
export {};
