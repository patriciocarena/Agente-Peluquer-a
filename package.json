{
  "name": "turnosbot",
  "private": true,
  "version": "0.0.0",
  "description": "TurnosBot — SaaS multitenant de agentes de WhatsApp para peluquerías",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "tsc -b --pretty"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.110.0",
    "@turnosbot/availability-engine": "workspace:*",
    "@turnosbot/db-types": "workspace:*",
    "fastify": "^5.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3"
  }
}
