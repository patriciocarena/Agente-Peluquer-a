{
  "name": "@turnosbot/availability-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Pure TS engine: horario - bloqueos - turnos confirmados = slots reales (AVAIL-01..05)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prepare": "tsc -b",
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@date-fns/tz": "^1.5.0",
    "date-fns": "^4.4.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "4.1.9"
  }
}
