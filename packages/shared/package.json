{
  "name": "@turnosbot/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared constants and es-AR locale strings used by both apps/bot and apps/dashboard (stub)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
