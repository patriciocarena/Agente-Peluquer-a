---
phase: 03-motor-de-disponibilidad
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/src/db/negocioScoped.ts
  - apps/bot/src/db/negocioScoped.test.ts
autonomous: false
requirements: [AVAIL-04]
user_setup: []
must_haves:
  truths:
    - "La capa de acceso a datos del bot filtra por negocio_id (columna que existe post-migración 0003), no por tenant_id (columna que ya no existe)"
    - "Una consulta del bot a turno/bloqueo/horario_trabajo/servicio ya no lanza 'column ... tenant_id does not exist'"
    - "El accessor negocio() sigue filtrando por tenant_id porque negocio.tenant_id (FK al tenant padre) SÍ existe y es legítimo"
    - "El smoke test verifica aislamiento cross-negocio contra la DB live bdgufnitakelyialjoqg"
  artifacts:
    - path: "apps/bot/src/db/negocioScoped.ts"
      provides: "Capa de queries aisladas por negocio_id para el bot (service_role)"
      contains: "negocioScoped"
    - path: "apps/bot/src/db/negocioScoped.test.ts"
      provides: "Smoke test de aislamiento cross-negocio contra la DB live"
      contains: "negocio_id"
  key_links:
    - from: "apps/bot/src/db/negocioScoped.ts"
      to: "turno/bloqueo/horario_trabajo/servicio"
      via: ".eq('negocio_id', negocioId)"
      pattern: "\\.eq\\(\"negocio_id\", negocioId\\)"
---

<objective>
Reparar el defecto BLOQUEANTE preexistente (Pitfall 7): `apps/bot/src/db/tenantScoped.ts` filtra por `.eq("tenant_id", ...)` en tablas cuya columna fue renombrada a `negocio_id` por la migración 0003 (ya aplicada en vivo). Verificado en vivo: `.eq("tenant_id", ...)` contra `turno` lanza `column turno.tenant_id does not exist`. Renombrar la capa en el lugar a `negocioScoped(negocioId)` y actualizar su smoke test.

Purpose: Esta capa es el ÚNICO camino de acceso a datos sancionado del bot (service_role, sin RLS). Es la fuente de datos del motor de disponibilidad por el lado del bot (Fase 6) — sin este fix no se puede escribir ningún test de integración ni script de verificación live del engine. NO es scope nuevo de AVAIL-01..05, pero está en el camino crítico de la fase (soporta AVAIL-04: el path bot que alimenta el motor compartido). Es una fase temprana (Wave 1) porque desbloquea la verificación live posterior.

Output: `negocioScoped.ts` + `negocioScoped.test.ts`, con el defecto de columna cerrado y el aislamiento cross-negocio verificado en vivo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md
@.planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md

<interfaces>
<!-- Patrón (roto) actual en apps/bot/src/db/tenantScoped.ts — TODOS los accessors -->
```typescript
export function tenantScoped(tenantId: string) {
  return {
    negocio: () => supabaseAdmin.from("negocio").select("*").eq("tenant_id", tenantId), // ← ESTE se queda (negocio.tenant_id es FK legítimo al tenant padre)
    turnos: () => supabaseAdmin.from("turno").select("*").eq("tenant_id", tenantId),     // ← ROTO: turno.tenant_id no existe → negocio_id
    // ... y así para: profesionales, horariosTrabajo, servicios, profesionalServicios,
    //     clientes, turnoServicios, bloqueos, conversaciones, mensajes, recordatorios
  } as const;
}
```
<!-- Confirmado vía lectura directa de packages/db-types/src/database.types.ts:
     turno, profesional, horario_trabajo, servicio, profesional_servicio, cliente,
     turno_servicio, bloqueo, conversacion, mensaje, recordatorio → todas usan negocio_id.
     negocio → sigue teniendo tenant_id (FK al tenant padre) — NO renombrar ese accessor. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renombrar tenantScoped → negocioScoped y fijar el filtro a negocio_id</name>
  <read_first>
    - apps/bot/src/db/tenantScoped.ts (archivo a renombrar/reparar en el lugar — 12 accessors)
    - packages/db-types/src/database.types.ts (confirmar nombre de columna post-0003 para cada tabla, especialmente conversacion/mensaje/recordatorio)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (sección tenantScoped.ts FIX — instrucción exacta de qué renombrar y qué NO)
    - .planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md (Pitfall 7, Assumption A4)
  </read_first>
  <action>
    Renombrar el archivo `apps/bot/src/db/tenantScoped.ts` → `apps/bot/src/db/negocioScoped.ts` (usar `git mv` para preservar historia).
    Dentro del archivo:
    - Renombrar la función `tenantScoped(tenantId: string)` → `negocioScoped(negocioId: string)`.
    - Para TODOS los accessors EXCEPTO `negocio()`: reemplazar `.eq("tenant_id", tenantId)` por `.eq("negocio_id", negocioId)`. Tablas afectadas: `profesional`(profesionales), `horario_trabajo`(horariosTrabajo), `servicio`(servicios), `profesional_servicio`(profesionalServicios), `cliente`(clientes), `turno`(turnos), `turno_servicio`(turnoServicios), `bloqueo`(bloqueos), `conversacion`(conversaciones), `mensaje`(mensajes), `recordatorio`(recordatorios).
    - DEJAR `negocio()` EXACTAMENTE como está: `.eq("tenant_id", tenantId)` → pasa a `.eq("tenant_id", negocioId)`. IMPORTANTE: `negocio.tenant_id` es el FK legítimo al tenant padre y SÍ existe post-0003 (verificado en database.types.ts). Confirmar con una lectura targeted de `negocio.Row` antes de tocarlo — si `negocio` se scopea por su propio `id` en vez de `tenant_id`, ajustar; pero por PATTERNS.md el filtro correcto de `negocio()` es su `tenant_id`. Documentar la decisión en un comentario inline.
    - Actualizar el doc-comment del encabezado: reemplazar la prosa "tenant_id" por "negocio_id" donde corresponda, y actualizar la Scope note para mencionar que la Fase 03 (motor de disponibilidad, data-feed del bot) es el primer consumidor real de esta capa (supersede la nota "Phase 6").
    Verificar que no queden referencias colgantes: `grep -rn "tenantScoped" apps/bot/src` debe devolver 0 (salvo el propio test que se arregla en Task 2).
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && test -f apps/bot/src/db/negocioScoped.ts && ! test -f apps/bot/src/db/tenantScoped.ts && grep -q 'export function negocioScoped(negocioId: string)' apps/bot/src/db/negocioScoped.ts && [ "$(grep -c '\.eq(\"negocio_id\", negocioId)' apps/bot/src/db/negocioScoped.ts)" -ge 11 ] && grep -q 'from("negocio").select("\*").eq("tenant_id", negocioId)' apps/bot/src/db/negocioScoped.ts && pnpm --filter @turnosbot/bot exec tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/bot/src/db/negocioScoped.ts` existe; `apps/bot/src/db/tenantScoped.ts` NO existe.
    - `grep "export function negocioScoped(negocioId: string)" apps/bot/src/db/negocioScoped.ts` matchea.
    - `grep -c '\.eq("negocio_id", negocioId)' apps/bot/src/db/negocioScoped.ts` devuelve >= 11 (todos los accessors operacionales).
    - `grep 'from("negocio").select("\*").eq("tenant_id", negocioId)' apps/bot/src/db/negocioScoped.ts` matchea (el accessor negocio() conserva tenant_id — FK legítimo).
    - `grep -rn "tenantScoped" apps/bot/src/db/negocioScoped.ts` devuelve 0 (sin referencias colgantes al nombre viejo en el archivo).
    - `pnpm --filter @turnosbot/bot exec tsc --noEmit` pasa (los tipos generados de db-types aceptan negocio_id en esas tablas).
  </acceptance_criteria>
  <done>negocioScoped(negocioId) filtra por negocio_id en las 11 tablas operacionales y conserva tenant_id solo en negocio(); typecheck del bot verde.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar el smoke test negocioScoped.test.ts</name>
  <read_first>
    - apps/bot/src/db/tenantScoped.test.ts (smoke test actual — asserts sobre tenant_id, IDs de tenant)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (sección tenantScoped.test.ts FIX)
    - scripts/seed-fixtures.ts (o supabase/seed.sql) para obtener los negocio.id reales por tenant
    - CLAUDE.md (guard de aislamiento SUPABASE_URL.includes("bdgufnitakelyialjoqg"))
  </read_first>
  <action>
    Renombrar `apps/bot/src/db/tenantScoped.test.ts` → `apps/bot/src/db/negocioScoped.test.ts` (git mv).
    - Cambiar el import a `import { negocioScoped } from "./negocioScoped.js";`.
    - Renombrar las constantes `TENANT_A_ID`/`TENANT_B_ID` → `NEGOCIO_A_ID`/`NEGOCIO_B_ID`. Sus valores actuales son UUIDs de TENANT (nivel tenant, no negocio) — sourcear los `negocio.id` reales que existen bajo cada tenant en la DB live (leer `scripts/seed-fixtures.ts`/`supabase/seed.sql`; si no están explícitos, el ejecutor debe consultar la DB live con `@supabase/supabase-js` + keys del `.env` — NUNCA el MCP global — para listar `negocio.id` y elegir uno de cada tenant distinto).
    - Cambiar las llamadas `tenantScoped(...)` → `negocioScoped(...)`.
    - Cambiar las aserciones: `(t as { tenant_id: string }).tenant_id === TENANT_A_ID` → `(t as { negocio_id: string }).negocio_id === NEGOCIO_A_ID` (y equivalente para B / cross-leak).
    - Agregar (si no está) el guard de aislamiento al inicio de `main()`: si `!SUPABASE_URL.includes("bdgufnitakelyialjoqg")` → `console.error(...); process.exit(1)`.
    - Preservar el estilo `assert()`-based (NO vitest) y el framing "run via pnpm exec tsx" del encabezado — este archivo intencionalmente no usa vitest (el bot no tiene runner de tests aún).
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && test -f apps/bot/src/db/negocioScoped.test.ts && grep -q 'import { negocioScoped } from "./negocioScoped.js"' apps/bot/src/db/negocioScoped.test.ts && grep -q 'negocio_id' apps/bot/src/db/negocioScoped.test.ts && ! grep -q 'tenant_id' apps/bot/src/db/negocioScoped.test.ts && grep -q 'bdgufnitakelyialjoqg' apps/bot/src/db/negocioScoped.test.ts && pnpm --filter @turnosbot/bot exec tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/bot/src/db/negocioScoped.test.ts` existe; `tenantScoped.test.ts` NO existe.
    - `grep 'import { negocioScoped } from "./negocioScoped.js"'` matchea.
    - `grep 'negocio_id' ...test.ts` matchea y `grep 'tenant_id' ...test.ts` NO matchea (aserciones migradas).
    - `grep 'bdgufnitakelyialjoqg' ...test.ts` matchea (guard de aislamiento presente).
    - `pnpm --filter @turnosbot/bot exec tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>negocioScoped.test.ts importa el helper renombrado, afirma sobre negocio_id con IDs de negocio reales, tiene el guard de aislamiento; typecheck verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacion humana del smoke test cross-negocio contra la DB live</name>
  <action>Correr el smoke test negocioScoped.test.ts contra bdgufnitakelyialjoqg (requiere .env con SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY) y confirmar aislamiento sin errores de columna.</action>
  <what-built>Se renombró la capa de acceso a datos del bot a `negocioScoped(negocioId)` filtrando por `negocio_id`, y se actualizó su smoke test de aislamiento cross-negocio. El smoke test corre contra la DB live `bdgufnitakelyialjoqg` (requiere `.env` con `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`, que solo el usuario tiene).</what-built>
  <how-to-verify>
    1. Confirmá que el `.env` tiene `SUPABASE_URL` (apuntando a bdgufnitakelyialjoqg) y `SUPABASE_SERVICE_ROLE_KEY`.
    2. Corré el smoke test: `pnpm exec tsx apps/bot/src/db/negocioScoped.test.ts`
    3. Esperado: imprime "OK: negocioScoped(A).turnos() devuelve N fila(s), todas del negocio A." para A y B, y termina con "negocioScoped.test.ts: PASSED". NO debe aparecer ningún `column ... tenant_id does not exist`.
    4. Si algún negocio no tiene turnos sembrados, el assert de ">0 filas" puede fallar legítimamente — en ese caso reportá qué negocio_id usaste y cuántas filas devolvió, para ajustar los IDs de fixture.
  </how-to-verify>
  <resume-signal>Escribí "approved" si el smoke test pasó (PASSED, sin errores de columna), o pegá el output si falló.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot (service_role) → Postgres | El bot usa service_role que BYPASSEA RLS; la única barrera entre datos de un negocio y otro es que esta capa filtre por negocio_id en cada query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | negocioScoped.ts (todos los accessors) | mitigate | Cada accessor hornea `.eq("negocio_id", negocioId)` antes de devolver el query builder, haciendo estructuralmente imposible construir una query sin scope. El smoke test cross-negocio (Task 2) prueba en vivo que negocioScoped(A) nunca devuelve filas del negocio B. |
| T-03-04 | Information Disclosure | Estado roto previo (tenant_id inexistente) | mitigate | El defecto no era fuga sino un throw duro (`column does not exist`) que impedía todo consumo — al arreglarlo con el nombre de columna correcto se restaura la barrera de aislamiento que la capa promete. La aserción cross-leak del test (negocio A no ve B) es la verificación de que el fix no introdujo fuga. |
| T-03-05 | Tampering | Guard de proyecto en el smoke test | mitigate | El guard `SUPABASE_URL.includes("bdgufnitakelyialjoqg")` aborta si se apunta accidentalmente al proyecto del restaurante (regla dura de aislamiento de CLAUDE.md). |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/bot exec tsc --noEmit` verde (sin referencias a columnas inexistentes).
- Smoke test live `pnpm exec tsx apps/bot/src/db/negocioScoped.test.ts` → PASSED (verificado en el checkpoint humano).
- `grep -rn "tenantScoped\|tenant_id" apps/bot/src/db/negocioScoped.ts` solo matchea el accessor legítimo `negocio()`.
</verification>

<success_criteria>
- La capa de datos del bot filtra por negocio_id en las 11 tablas operacionales; negocio() conserva tenant_id (FK padre).
- Ninguna query del bot lanza `column ... tenant_id does not exist`.
- Smoke test cross-negocio verde en vivo contra bdgufnitakelyialjoqg.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motor-de-disponibilidad/03-02-SUMMARY.md`
</output>
