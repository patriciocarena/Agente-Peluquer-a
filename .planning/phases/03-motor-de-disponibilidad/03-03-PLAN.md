---
phase: 03-motor-de-disponibilidad
plan: 03
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/availability-engine/src/intervals.ts
  - packages/availability-engine/src/intervals.test.ts
  - packages/availability-engine/src/grid.ts
  - packages/availability-engine/src/grid.test.ts
  - packages/availability-engine/src/schedule.ts
  - packages/availability-engine/src/schedule.test.ts
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
must_haves:
  truths:
    - "subtractIntervals resta correctamente intervalos busy de free con semántica half-open [start, end) idéntica a tstzrange(inicio, fin, '[)') del DB"
    - "Dos turnos back-to-back (fin de uno = inicio del otro) NO se tratan como solapados (D-02, sin buffer)"
    - "snapToGrid emite solo slots alineados a granularidad_min donde start + totalDuration cabe antes del fin del intervalo libre"
    - "Un slot que no entra antes del fin del bloque de trabajo NO se emite (Pitfall 5, hueco muerto aceptado)"
    - "resolveWorkIntervalsForDate resuelve horario_trabajo por dia_semana en el timezone del negocio vía TZDate (nunca UTC-naive, nunca offset -3 hardcodeado)"
  artifacts:
    - path: "packages/availability-engine/src/intervals.ts"
      provides: "subtractIntervals(free, busy) — resta de intervalos half-open"
      contains: "subtractIntervals"
    - path: "packages/availability-engine/src/grid.ts"
      provides: "snapToGrid(free, granularidadMin, totalDurationMin, anchor)"
      contains: "snapToGrid"
    - path: "packages/availability-engine/src/schedule.ts"
      provides: "resolveWorkIntervalsForDate — horario_trabajo + dia_semana → intervalos con TZDate"
      contains: "TZDate"
  key_links:
    - from: "packages/availability-engine/src/schedule.ts"
      to: "@date-fns/tz"
      via: "TZDate con zona IANA"
      pattern: "import \\{ TZDate \\} from \"@date-fns/tz\""
---

<objective>
Implementar (estilo TDD: test rojo → verde) los tres primitivos puros del motor de intervalos: `subtractIntervals` (resta half-open), `snapToGrid` (alineación a grilla dimensionada a la duración multi-servicio), y `resolveWorkIntervalsForDate` (horario_trabajo recurrente → intervalos de fecha concretos en el timezone del negocio). Cada uno con su suite de vitest exhaustiva de casos de borde.

Purpose: Estos son los primitivos de AVAIL-01 (resta horario − busy) y AVAIL-02 (dimensionado a la suma de duraciones). El riesgo de todo el dominio NO es que el algoritmo sea difícil — es que las condiciones de borde (semántica `[)`, back-to-back sin buffer, hueco muerto, day-boundary en la zona IANA) se manejen inconsistentemente. Concentrar toda la matemática de intervalos en estos módulos, testeados exhaustivamente, es la mitigación central del riesgo de la fase.

Output: intervals.ts, grid.ts, schedule.ts con sus tests verdes. computeSlots (Wave 3) los orquesta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-motor-de-disponibilidad/03-CONTEXT.md
@.planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md
@.planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md

<interfaces>
<!-- Tipos ya definidos en Plan 03-01 (types.ts). Importar desde ahí. -->
```typescript
// Interval { start: number; end: number }  // epoch ms, half-open [start, end)
// HorarioTrabajoRow: { dia_semana: number (0=dom..6=sáb); hora_inicio: string ("HH:mm:ss"); hora_fin: string; ... }
// NegocioRow: { timezone: string; granularidad_min: number; ... }
```
<!-- Algoritmos de referencia (03-RESEARCH.md Patterns 1 y 2, Code Examples) -->
<!-- subtractIntervals: test "no overlap" = (b.end <= f.start || b.start >= f.end) → semántica [) -->
<!-- snapToGrid: emitir solo si candidate + durMs <= free.end -->
<!-- resolveWorkIntervalsForDate: new TZDate(y, m-1, d, h, min, 0, timezone), .getTime() → epoch ms -->
</interfaces>
</context>

<feature>
  <name>subtractIntervals — resta de intervalos half-open [start, end)</name>
  <files>packages/availability-engine/src/intervals.ts, packages/availability-engine/src/intervals.test.ts</files>
  <read_first>
    - packages/availability-engine/src/types.ts (el tipo Interval — Plan 03-01)
    - apps/dashboard/lib/reorder.ts + reorder.test.ts (forma de archivo pura + estilo de test)
    - .planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md (Pattern 1 código completo; Pitfall 1)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (secciones intervals.ts / intervals.test.ts)
  </read_first>
  <behavior>
    RED primero (intervals.test.ts, import explícito `from "vitest"`), luego GREEN:
    - Test: restar un busy del medio de un free lo parte en dos ([9,12) − [10,11) = [9,10)+[11,12)).
    - Test: back-to-back — busy [10,11) contra free [9,10): NO se solapan (b.start >= f.end), free queda intacto (Pitfall 1 / D-02 sin buffer).
    - Test: busy tocando el inicio exacto (b.end === f.start) NO crea un hueco falso ni se trata como overlap.
    - Test: busy que cubre todo el free → resultado vacío.
    - Test: múltiples busy contra un free → resta acumulada correcta.
    - Test: intervalos de longitud cero resultantes se filtran (end > start).
    - Test: fast-path — busy vacío devuelve free tal cual.
    - Invariante reutilizable: helper `assertSinSolapamientos(result)` o `assertBordesHalfOpen(result)`.
  </behavior>
  <action>
    Crear `intervals.ts` con doc-comment de rationale (estilo reorder.ts: nombra el caller — computeSlots —, la semántica `[)` y por qué debe espejar `tstzrange(inicio,fin,'[)')` del DB). Exportar `subtractIntervals(free: Interval[], busy: Interval[]): Interval[]` usando el cuerpo del Pattern 1 de RESEARCH.md: test de "no overlap" = `b.end <= f.start || b.start >= f.end`, guard `if (busy.length === 0) return free;`, filtrado final `i.end > i.start`. Opcionalmente exportar `mergeIntervals(intervals)` si computeSlots lo necesita para fusionar busy antes de restar. Importar `Interval` desde `./types.js`.
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && pnpm --filter @turnosbot/availability-engine exec vitest run src/intervals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "b.end <= f.start || b.start >= f.end" packages/availability-engine/src/intervals.ts` matchea (semántica half-open exacta).
    - `intervals.ts` importa `Interval` desde `./types.js` (no lo redeclara).
    - `intervals.test.ts` importa `{ describe, expect, it } from "vitest"` explícitamente.
    - Existe un test cuyo nombre menciona back-to-back / adyacente / Pitfall 1.
    - `pnpm --filter @turnosbot/availability-engine exec vitest run src/intervals.test.ts` verde con >= 6 tests.
  </acceptance_criteria>
  <implementation>Body directo del Pattern 1 de RESEARCH.md; el análogo reorder.ts gobierna forma/estilo, no el algoritmo.</implementation>
</feature>

<feature>
  <name>snapToGrid — alineación a grilla dimensionada a duración multi-servicio (D-01, AVAIL-02, Pitfall 5)</name>
  <files>packages/availability-engine/src/grid.ts, packages/availability-engine/src/grid.test.ts</files>
  <read_first>
    - packages/availability-engine/src/intervals.ts (tipo Interval y estilo, recién creado)
    - .planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md (Pattern 2 código completo; Pitfall 5; Open Question 1 sobre anchor)
    - .planning/phases/03-motor-de-disponibilidad/03-CONTEXT.md (D-01 slots en múltiplos de granularidad)
  </read_first>
  <behavior>
    RED primero, luego GREEN:
    - Test: free [09:00,12:00), gran=30, dur=30 → slots 09:00, 09:30, 10:00, 10:30, 11:00, 11:30 (6 slots, todos alineados).
    - Test: dur=60 en el mismo free → 09:00, 09:30, 10:00, 10:30, 11:00 (último que entra 11:00-12:00; 11:30-12:30 NO se emite).
    - Test Pitfall 5 (hueco muerto): free que tras el último step completo deja un resto < dur NO emite slot parcial (ej. bloque termina 18:00, gran=30, dur=45 → 17:30-18:15 NO se emite; hueco muerto aceptado por D-01).
    - Test: multi-servicio — dur = suma de duraciones (corte 30 + barba 15 = 45) reserva un único bloque contiguo (AVAIL-02).
    - Test: alineación a grilla — todos los start emitidos son múltiplos de granularidad desde el anchor.
  </behavior>
  <action>
    Crear `grid.ts` con doc-comment (cita D-01, AVAIL-02, Pitfall 5). Exportar `snapToGrid(freeIntervals: Interval[], granularidadMin: number, totalDurationMin: number, anchor: number): Interval[]` con el cuerpo del Pattern 2 de RESEARCH.md. DECISIÓN DE ANCHOR (Open Question 1 / A1): anclar la grilla a **medianoche en el timezone del negocio** (marcas de reloj absolutas 9:00, 9:30…), NO al inicio del turno — documentar esta decisión en el doc-comment. El caller (computeSlots, Wave 3) pasará el epoch ms de medianoche-en-zona como `anchor`. Gate de emisión OBLIGATORIO: `candidate + durMs <= free.end` (nunca solo `candidate < free.end`) — Pitfall 5. `durMs = totalDurationMin * 60_000`, `granMs = granularidadMin * 60_000`, primer candidato = `anchor + Math.ceil((free.start - anchor)/granMs)*granMs`.
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && pnpm --filter @turnosbot/availability-engine exec vitest run src/grid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "candidate + durMs <= free.end\|+ durMs <= free.end" packages/availability-engine/src/grid.ts` matchea (gate Pitfall 5).
    - `grid.ts` doc-comment menciona el anchor de medianoche-en-zona y cita D-01.
    - Existe un test cuyo nombre menciona Pitfall 5 / hueco muerto / no entra antes del fin.
    - Existe un test multi-servicio que suma duraciones (AVAIL-02).
    - `pnpm --filter @turnosbot/availability-engine exec vitest run src/grid.test.ts` verde con >= 5 tests.
  </acceptance_criteria>
  <implementation>Body del Pattern 2 de RESEARCH.md con anchor = medianoche-en-zona (decidido, documentado).</implementation>
</feature>

<feature>
  <name>resolveWorkIntervalsForDate — horario_trabajo recurrente → intervalos de fecha en zona IANA (Pitfall 2)</name>
  <files>packages/availability-engine/src/schedule.ts, packages/availability-engine/src/schedule.test.ts</files>
  <read_first>
    - packages/availability-engine/src/types.ts (HorarioTrabajoRow, Interval)
    - apps/dashboard/lib/schemas/horario.ts (idiom minutosDesdeMedianoche / split HH:mm)
    - .planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md (Code Examples: dayBoundsInZone, resolveWorkIntervalsForDate; Pitfall 2; convención dia_semana 0=dom..6=sáb)
    - supabase/migrations/0001_schema_core.sql (línea 125 — CHECK dia_semana BETWEEN 0 AND 6, 0=domingo)
  </read_first>
  <behavior>
    RED primero, luego GREEN:
    - Test: fecha "2026-07-06" (lunes) con horario dia_semana=1 (lunes) 09:00-13:00 → un intervalo [epoch 09:00 AR, epoch 13:00 AR).
    - Test: horario de otro dia_semana NO matchea la fecha → intervalos vacíos.
    - Test multi-bloque: dos filas del mismo dia_semana (mañana + tarde) → dos intervalos.
    - Test Pitfall 2 (day boundary): construir con `TZDate(...timezone)` produce epoch distinto a `new Date(dateString)` UTC-naive; un horario 00:00-02:00 debe resolver a medianoche AR, no UTC (verificar que el epoch resultante = medianoche en America/Argentina/Buenos_Aires, no medianoche UTC).
    - Test: `hora_inicio` viene como "HH:mm:ss" de Postgrest (time column) — el parser tolera el `:ss` extra (Pitfall: horario.ts solo valida HH:mm de input tipeado; schedule.ts debe tolerar el segundo).
  </behavior>
  <action>
    Crear `schedule.ts` con doc-comment (cita Pitfall 2, la convención dia_semana 0=domingo, y la regla dura: NUNCA offset -3 hardcodeado — siempre zona IANA vía TZDate). `import { TZDate } from "@date-fns/tz";`. Exportar `resolveWorkIntervalsForDate(horarios: HorarioTrabajoRow[], dateStr: string, timezone: string): Interval[]` siguiendo el Code Example de RESEARCH.md: parsear `[year, month, day]` de dateStr, construir `anchor = new TZDate(year, month-1, day, 0,0,0, timezone)`, `dow = anchor.getDay()` (0=domingo, coincide con el CHECK del DB), filtrar `horarios` por `dia_semana === dow` (y `activo !== false`), y por cada uno construir start/end vía `new TZDate(year, month-1, day, h, min, 0, timezone).getTime()`. El parser de "HH:mm:ss" debe hacer `.split(":").map(Number)` y usar solo horas/minutos (tolerar el `:ss`). Exportar además un helper `dayStartEpochInZone(dateStr, timezone): number` (medianoche-en-zona) que computeSlots (Wave 3) usará como `anchor` de snapToGrid — mantiene una única fuente de la lógica de medianoche-en-zona.
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && pnpm --filter @turnosbot/availability-engine exec vitest run src/schedule.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'import { TZDate } from "@date-fns/tz"' packages/availability-engine/src/schedule.ts` matchea.
    - `grep -c "new Date(" packages/availability-engine/src/schedule.ts` NO cuenta ningún `new Date(dateStr)` UTC-naive sobre el string de fecha (usar solo TZDate para límites de día).
    - `grep "\-3\|-3\b" packages/availability-engine/src/schedule.ts` NO matchea ningún offset hardcodeado.
    - `schedule.ts` exporta `resolveWorkIntervalsForDate` y `dayStartEpochInZone`.
    - Existe un test que verifica el day-boundary en zona (Pitfall 2) y otro que verifica el filtrado por dia_semana.
    - `pnpm --filter @turnosbot/availability-engine exec vitest run src/schedule.test.ts` verde con >= 5 tests.
  </acceptance_criteria>
  <implementation>Code Example de RESEARCH.md (resolveWorkIntervalsForDate + dayBoundsInZone); idiom de parsing de horario.ts adaptado para tolerar :ss.</implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input del caller → primitivos puros | Los primitivos operan sobre datos ya scopeados por el caller; su corrección de bordes es lo que evita ofrecer slots que el DB luego rechaza. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering (integridad) | subtractIntervals / semántica de bordes | mitigate | La semántica half-open `[)` debe espejar exactamente `tstzrange(inicio,fin,'[)') WHERE estado != 'cancelado'` del constraint `turno_no_overlap`. Si diverge, el motor ofrece slots que el DB rechaza (23P01 sin concurrencia). Los tests de borde (back-to-back, tocando el inicio) son la mitigación: prueban que la definición de "busy" del motor es el inverso lógico de "no excluido" del DB. |
| T-03-07 | Tampering | schedule.ts / day-boundary en zona | mitigate | Todo cálculo de límite de día vía TZDate con zona IANA (nunca `new Date(dateStr)` UTC-naive ni offset -3). Un test explícito de day-boundary cerca de medianoche cachea el bug de shift de 3 horas. |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/availability-engine exec vitest run` verde para intervals/grid/schedule.
- `pnpm --filter @turnosbot/availability-engine exec tsc --noEmit` verde.
- Ningún `new Date(dateStr)` UTC-naive ni offset -3 en schedule.ts.
</verification>

<success_criteria>
- subtractIntervals con semántica `[)` exacta al DB, back-to-back sin falso overlap.
- snapToGrid alineado a granularidad, anchor medianoche-en-zona, gate Pitfall 5, multi-servicio contiguo (AVAIL-02).
- resolveWorkIntervalsForDate resuelve por dia_semana en zona IANA vía TZDate (AVAIL-01, Pitfall 2).
- Las tres suites verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motor-de-disponibilidad/03-03-SUMMARY.md`
</output>
