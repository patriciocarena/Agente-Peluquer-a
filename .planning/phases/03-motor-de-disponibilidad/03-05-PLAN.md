---
phase: 03-motor-de-disponibilidad
plan: 05
type: tdd
wave: 4
depends_on: ["03-01", "03-04"]
files_modified:
  - packages/availability-engine/src/booking.ts
  - packages/availability-engine/src/booking.test.ts
  - packages/availability-engine/src/index.ts
  - scripts/verify-availability-engine.ts
autonomous: false
requirements: [AVAIL-03, AVAIL-04]
user_setup: []
must_haves:
  truths:
    - "bookAppointment congela nombre/precio/duracion de cada servicio como snapshot al agendar (AVAIL-03); nunca hace join vivo a servicio.precio"
    - "precio_total del turno = suma de los precio_snapshot, escrito en la misma transaccion (Pitfall 3)"
    - "un cambio posterior a servicio.precio NO altera el precio_total de un turno ya creado"
    - "bookAppointment detecta el conflicto de concurrencia (SQLSTATE 23P01) y lo devuelve como 'slot tomado', no como error 500"
    - "bookAppointment vive en el mismo paquete que computeSlots (unico camino de escritura compartido, AVAIL-04)"
  artifacts:
    - path: "packages/availability-engine/src/booking.ts"
      provides: "bookAppointment: insert atomico de turno + turno_servicio snapshots + precio_total, manejo de 23P01"
      contains: "bookAppointment"
    - path: "scripts/verify-availability-engine.ts"
      provides: "Smoke test live opcional que siembra horario/bloqueo y verifica el round-trip contra bdgufnitakelyialjoqg"
      contains: "bdgufnitakelyialjoqg"
  key_links:
    - from: "packages/availability-engine/src/booking.ts"
      to: "turno_servicio.precio_snapshot suma en turno.precio_total"
      via: "suma de snapshots en la misma transaccion"
      pattern: "precio_snapshot"
    - from: "packages/availability-engine/src/index.ts"
      to: "bookAppointment"
      via: "re-export barrel"
      pattern: "export \\{ bookAppointment \\}"
---

<objective>
Implementar el modulo de agendado `bookAppointment` (mismo paquete, camino de escritura unico que garantiza AVAIL-04): inserta el `turno` + las filas `turno_servicio` con snapshots congelados de nombre/precio/duracion, calcula `precio_total` sumando los `precio_snapshot` en la misma transaccion, y trata el `23P01` (exclusion_violation) como senal de "slot tomado concurrentemente" en vez de un 500. Agregar `bookAppointment` al barrel y un smoke test live opcional que siembre `horario_trabajo`/`bloqueo` (hoy vacios en la DB live) y verifique el round-trip.

Purpose: Cierra AVAIL-03 (snapshots congelados) — la ultima pieza de la fase. El congelado protege el historico: cambiar el precio de un servicio la semana que viene NO debe reescribir el total de un turno ya agendado. El manejo de 23P01 apoya la ultima linea de defensa anti-doble-reserva del DB (GiST EXCLUDE, CORE-05) con una UX controlada.

Output: booking.ts + su test; index.ts completo (computeSlots + bookAppointment); script de verificacion live opcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-motor-de-disponibilidad/03-CONTEXT.md
@.planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md
@.planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md

<interfaces>
<!-- De Waves previas: -->
```typescript
// ./types.js:  BookAppointmentInput { negocioId; profesionalId; clienteId; serviceIds: string[]; inicio; fin }
//              TurnoRow, TurnoServicioRow, ServicioRow (aliases de db-types)
// ./computeSlots.js: computeSlots(input, data, now?) — para la re-validacion pre-insert
```
<!-- Patron de insert + 23P01: 03-RESEARCH.md Code Examples (isSlotTakenConcurrently) + scripts/verify-double-booking.ts (lineas 67-101) -->
<!-- CRITICO: turno.Insert usa negocio_id (NO tenant_id); verify-double-booking.ts predata la migracion 0003 -->
</interfaces>
</context>

<feature>
  <name>bookAppointment — insert atomico con snapshots congelados + manejo de 23P01 (AVAIL-03, Pitfall 3, CORE-05)</name>
  <files>packages/availability-engine/src/booking.ts, packages/availability-engine/src/booking.test.ts</files>
  <read_first>
    - packages/availability-engine/src/types.ts (BookAppointmentInput, TurnoRow, TurnoServicioRow, ServicioRow)
    - packages/availability-engine/src/computeSlots.ts (para la re-validacion de freshness pre-insert)
    - scripts/verify-double-booking.ts (patron de insert en turno + branch sobre 23P01; NOTA: usa tenant_id, hay que usar negocio_id)
    - packages/db-types/src/database.types.ts (turno.Insert y turno_servicio.Insert — confirmar columnas snapshot)
    - .planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md (Code Examples isSlotTakenConcurrently; Pitfall 3; Anti-Pattern "never cache computed availability"; Open Question 2 sobre inyeccion del cliente)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (secciones booking.ts / booking.test.ts)
  </read_first>
  <behavior>
    RED primero (unit sobre la logica PURA pre-insert, con fixtures — NO probar el INSERT live aqui), luego GREEN:
    - Test (suma de snapshots): dados servicios [corte precio 6000 dur 30, barba precio 3000 dur 15], la funcion que arma los snapshots produce filas turno_servicio con nombre/precio/duracion_snapshot correctos y precio_total = 9000 (suma, Pitfall 3).
    - Test (congelado historico): calcular precio_total a partir de los snapshots PASADOS, no de un servicio.precio "actual" — un fixture donde servicio.precio cambio a 8000 despues NO altera el precio_total ya calculado (9000). Prueba que la suma usa precio_snapshot, no un re-derive de servicio.
    - Test (isSlotTakenConcurrently): code 23P01 devuelve true; code "other" devuelve false; null devuelve false. Funcion pura, fixture trivial.
    - Test (input validation zod): un BookAppointmentInput con serviceIds vacio o UUID invalido es rechazado con error claro (V5 Input Validation).
    NO unit-testear el INSERT/EXCLUDE live aqui — eso lo cubre scripts/verify-double-booking.ts a nivel DB y el smoke script de Feature 3.
  </behavior>
  <action>
    Crear `booking.ts` con doc-comment (cita AVAIL-03, Pitfall 3, y el anti-pattern de nunca cachear disponibilidad). Estructura (Open Question 2 de RESEARCH.md — limite puro/impuro explicito):
    - Funcion pura `buildTurnoServicioSnapshots(serviceIds, servicios)`: por cada serviceId, produce nombre_snapshot=servicio.nombre, precio_snapshot=servicio.precio, duracion_snapshot=servicio.duracion_min — congela desde la fila de servicio pasada, NUNCA un join vivo (Pitfall 3).
    - Funcion pura `sumPrecioTotal(snapshots)`: suma de los precio_snapshot.
    - `isSlotTakenConcurrently(error)`: devuelve error?.code === "23P01" (constante EXCLUSION_VIOLATION = "23P01").
    - `bookAppointment(input, deps)` donde deps = { supabase, freshData }: valida input con zod; re-valida el slot elegido contra computeSlots(..., freshData) inmediatamente antes de insertar (freshness — anti-pattern de cache); inserta `turno` (con negocio_id, NO tenant_id; estado inicial; inicio/fin; precio_total=sumPrecioTotal) y las filas `turno_servicio` con los snapshots, idealmente en una RPC/transaccion unica (si no hay RPC disponible, insertar turno y luego turno_servicio, y documentar el gap); si el error del insert de turno es 23P01, devolver ok:false reason:"slot_taken" sin lanzar; en exito devolver ok:true turnoId. El cliente Supabase se INYECTA como dependencia (deps.supabase) — el paquete NO importa @supabase/supabase-js como dependency runtime (solo el tipo PostgrestError/SupabaseClient via type-only import si hace falta), preservando la pureza por defecto del paquete y su testabilidad (AVAIL-04).
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && pnpm --filter @turnosbot/availability-engine exec vitest run src/booking.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "precio_snapshot" packages/availability-engine/src/booking.ts matchea (snapshots congelados, no join vivo).
    - grep "23P01" packages/availability-engine/src/booking.ts matchea (manejo de exclusion_violation).
    - grep "negocio_id" packages/availability-engine/src/booking.ts matchea y grep -w "tenant_id" packages/availability-engine/src/booking.ts NO matchea (columna correcta post-0003).
    - booking.ts NO tiene import de valor runtime de @supabase/supabase-js (solo type-only si aplica) — el cliente se inyecta.
    - Existe un test que prueba que un cambio posterior en servicio.precio no altera el precio_total ya calculado (congelado historico).
    - Existe un test de isSlotTakenConcurrently con los 3 casos (23P01 true, otro false, null false).
    - pnpm --filter @turnosbot/availability-engine exec vitest run src/booking.test.ts verde con >= 4 tests.
  </acceptance_criteria>
  <implementation>Code Example isSlotTakenConcurrently de RESEARCH.md + patron de insert de verify-double-booking.ts (con negocio_id); cliente inyectado (Open Question 2).</implementation>
</feature>

<feature>
  <name>index.ts — agregar bookAppointment al barrel (AVAIL-04)</name>
  <files>packages/availability-engine/src/index.ts</files>
  <read_first>
    - packages/availability-engine/src/index.ts (barrel de Wave 3)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (seccion index.ts barrel)
  </read_first>
  <behavior>
    - El barrel exporta computeSlots Y bookAppointment desde el mismo path publico.
    - typecheck y toda la suite verdes.
  </behavior>
  <action>
    Agregar al barrel: export { bookAppointment } from "./booking.js"; (y isSlotTakenConcurrently si algun consumidor lo necesita para decidir UX). Mantener la superficie publica minima. Preservar el path publico @turnosbot/availability-engine inalterado.
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && grep -q "export { bookAppointment }" packages/availability-engine/src/index.ts && pnpm --filter @turnosbot/availability-engine exec tsc --noEmit && pnpm --filter @turnosbot/availability-engine exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep "export { bookAppointment }" packages/availability-engine/src/index.ts matchea.
    - pnpm --filter @turnosbot/availability-engine exec tsc --noEmit verde.
    - pnpm --filter @turnosbot/availability-engine exec vitest run verde (toda la suite de la fase: intervals, grid, schedule, autoAssign, computeSlots, booking, constants).
  </acceptance_criteria>
  <implementation>Barrel simple; path publico inalterado.</implementation>
</feature>

<feature>
  <name>scripts/verify-availability-engine.ts — smoke test live opcional (Pitfall 8, AVAIL-01/03)</name>
  <files>scripts/verify-availability-engine.ts</files>
  <read_first>
    - scripts/verify-double-booking.ts + scripts/verify-timezone.ts (guard de aislamiento + patron de script tsx)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (seccion Live-schema verification before trusting any column name)
    - .planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md (Pitfall 8: horario_trabajo/bloqueo VACIOS en la DB live)
    - CLAUDE.md (regla dura de aislamiento)
  </read_first>
  <behavior>
    - El script aborta si SUPABASE_URL no incluye "bdgufnitakelyialjoqg".
    - Siembra al menos un horario_trabajo y un bloqueo para un profesional de prueba (las tablas estan vacias en la DB live).
    - Llama computeSlots con las filas fetcheadas y compara contra la agenda esperada; imprime PASSED/FAIL.
    - Verifica el round-trip de bookAppointment: agenda, luego confirma que precio_total = suma de snapshots, y que un cambio de servicio.precio posterior NO lo altera (AVAIL-03 live).
  </behavior>
  <action>
    Crear `scripts/verify-availability-engine.ts` como script tsx (estilo verify-double-booking.ts), con el guard de aislamiento `SUPABASE_URL.includes("bdgufnitakelyialjoqg")` verbatim al inicio. Usa @supabase/supabase-js con las keys del .env (NUNCA el MCP global). Siembra filas de horario_trabajo/bloqueo/servicio para un negocio/profesional de prueba (las live estan vacias, Pitfall 8), fetchea via esas filas, llama computeSlots y afirma la lista de slots esperada; luego llama bookAppointment y afirma precio_total. Limpiar (o usar IDs claramente de prueba) para no ensuciar datos. Este script es SECUNDARIO — la estrategia primaria de validacion son los unit tests con fixtures; este solo da un smoke live end-to-end.
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && test -f scripts/verify-availability-engine.ts && grep -q "bdgufnitakelyialjoqg" scripts/verify-availability-engine.ts && grep -q "computeSlots" scripts/verify-availability-engine.ts && pnpm exec tsc --noEmit scripts/verify-availability-engine.ts 2>/dev/null; echo "script created"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-availability-engine.ts existe.
    - grep "bdgufnitakelyialjoqg" scripts/verify-availability-engine.ts matchea (guard de aislamiento).
    - grep "computeSlots" y grep "bookAppointment" en el script matchean (usa el motor real).
    - El script NO referencia el proyecto del restaurante (hzgunbftloevclkohcdf) en ningun lado.
  </acceptance_criteria>
  <implementation>Estilo verify-double-booking.ts con guard verbatim; siembra horario/bloqueo (Pitfall 8) y valida el round-trip real.</implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (bot/dashboard) → bookAppointment → Postgres | El insert cruza al DB; la GiST EXCLUDE es la ultima linea anti-doble-reserva. El cliente se inyecta; el caller es responsable de scopear al negocio correcto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering (integridad) | precio_total via snapshots (Pitfall 3, AVAIL-03) | mitigate | precio_total se calcula sumando los precio_snapshot escritos en la misma transaccion, NUNCA re-derivado de servicio.precio en read-time. Test de congelado historico prueba que un cambio posterior de precio no reescribe el total. |
| T-03-12 | Tampering (integridad) | doble-reserva concurrente (CORE-05) | mitigate | La GiST EXCLUDE del DB es la mitigacion autoritativa; bookAppointment captura 23P01 y devuelve "slot_taken" sin reintentar ciegamente (reintentar una exclusion sin re-chequear disponibilidad solo reproduce la falla). |
| T-03-13 | Tampering | freshness pre-insert (never-cache) | mitigate | bookAppointment re-valida el slot contra computeSlots(freshData) inmediatamente antes del insert — no confia en un slot calculado en un turno de conversacion previo (anti-pattern de cache documentado en conversacion.context). |
| T-03-14 | Information Disclosure / aislamiento | scripts/verify-availability-engine.ts | mitigate | Guard `SUPABASE_URL.includes("bdgufnitakelyialjoqg")` verbatim aborta si se apunta al proyecto del restaurante (regla dura de CLAUDE.md); usa @supabase/supabase-js con keys del .env, nunca el MCP global. |
| T-03-15 | Input Validation | BookAppointmentInput (zod) | mitigate | zod valida shape (serviceIds no vacio, UUIDs, fechas) en el limite del paquete, cortando input malformado con error claro (V5) — relevante ante input LLM-driven manipulable (BOT-11, Fase 6). |
</threat_model>

<verification>
- pnpm --filter @turnosbot/availability-engine exec vitest run verde para TODA la suite de la fase (intervals, grid, schedule, autoAssign, computeSlots, booking, constants).
- pnpm --filter @turnosbot/availability-engine exec tsc --noEmit verde.
- grep -rn "@supabase/supabase-js" packages/availability-engine/src/ solo aparece como type-only import (si acaso) en booking.ts; nunca en la ruta de computo puro.
- AVAIL-04 (import hygiene): grep -rn "computeSlots\|bookAppointment" apps/bot/src apps/dashboard --include="*.ts" -l — confirmar (revision manual) que cualquier consumidor futuro importa solo desde @turnosbot/availability-engine, cero reimplementaciones locales.
</verification>

<success_criteria>
- bookAppointment congela snapshots de nombre/precio/duracion y suma precio_total en la misma transaccion (AVAIL-03).
- Un cambio posterior de servicio.precio no altera un precio_total ya calculado.
- 23P01 se detecta y se devuelve como "slot tomado", no como 500 (apoya CORE-05).
- bookAppointment exportado del mismo paquete que computeSlots (AVAIL-04).
- Script de verificacion live opcional con guard de aislamiento, siembra horario/bloqueo (Pitfall 8) y valida el round-trip.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motor-de-disponibilidad/03-05-SUMMARY.md`
</output>
