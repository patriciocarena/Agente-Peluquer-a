---
phase: 03-motor-de-disponibilidad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/availability-engine/package.json
  - packages/availability-engine/vitest.config.ts
  - packages/availability-engine/src/constants.ts
  - packages/availability-engine/src/constants.test.ts
  - packages/availability-engine/src/types.ts
  - packages/availability-engine/src/__fixtures__/rows.ts
autonomous: true
requirements: [AVAIL-04]
must_haves:
  truths:
    - "El paquete availability-engine tiene date-fns, @date-fns/tz, zod y vitest instalados y un runner configurado"
    - "Los tipos del motor (ComputeSlotsInput, AvailableSlot, BookAppointmentInput, row aliases) están definidos contra @turnosbot/db-types, sin declarar shapes paralelos"
    - "Las constantes de ventana de reserva (60 min / 30 días) viven en un único archivo, promovibles a config luego"
    - "Existen fixtures deterministas de horario_trabajo/bloqueo/turno/servicio/negocio para los unit tests (la DB live tiene esas tablas vacías)"
  artifacts:
    - path: "packages/availability-engine/package.json"
      provides: "Deps date-fns/@date-fns/tz/zod + devDep vitest + script test"
      contains: "@date-fns/tz"
    - path: "packages/availability-engine/vitest.config.ts"
      provides: "Vitest runner apuntando a src/**/*.test.ts"
      contains: "src/**/*.test.ts"
    - path: "packages/availability-engine/src/constants.ts"
      provides: "BOOKING_MIN_LEAD_MINUTES=60, BOOKING_MAX_ADVANCE_DAYS=30"
      contains: "BOOKING_MIN_LEAD_MINUTES"
    - path: "packages/availability-engine/src/types.ts"
      provides: "Contratos públicos + row aliases desde db-types"
      contains: "@turnosbot/db-types"
    - path: "packages/availability-engine/src/__fixtures__/rows.ts"
      provides: "Filas de prueba deterministas para los unit tests"
      contains: "horario_trabajo"
  key_links:
    - from: "packages/availability-engine/src/types.ts"
      to: "@turnosbot/db-types"
      via: "type-only import"
      pattern: "import type \\{ Database \\} from \"@turnosbot/db-types\""
---

<objective>
Preparar la fundación del paquete `@turnosbot/availability-engine` para que sea implementable y testeable en aislamiento: instalar dependencias, configurar el runner de vitest, definir los contratos de tipos (contra `@turnosbot/db-types`, sin duplicar shapes), las constantes hardcodeadas de la ventana de reserva (D-04/D-05), y fixtures deterministas.

Purpose: El paquete hoy tiene CERO dependencias y solo un stub. Nada del algoritmo puede escribirse ni probarse hasta que exista date-fns/@date-fns/tz/zod, un runner, los tipos y los fixtures. Esta es la Wave 0 que desbloquea todas las demás. Materializa la garantía AVAIL-04 por construcción: un único paquete puro con contratos únicos que ambos consumidores (bot Fase 6 / dashboard Fase 4) importarán sin drift.

Output: Paquete con toolchain de tests listo, `constants.ts`, `types.ts` y un módulo de fixtures. No hay lógica de cálculo todavía (eso es Wave 2+).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-motor-de-disponibilidad/03-CONTEXT.md
@.planning/phases/03-motor-de-disponibilidad/03-RESEARCH.md
@.planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md

<interfaces>
<!-- Contrato existente del stub que este plan evoluciona. Extraído de packages/availability-engine/src/index.ts -->
```typescript
// STUB actual — este plan lo mueve a types.ts y lo evoluciona (NO redisena el contrato):
export interface ComputeSlotsInput {
  tenantId: string;        // → renombrar a negocioId (migración 0003: tenant_id → negocio_id)
  serviceId: string;       // → serviceIds: string[] (AVAIL-02 multi-servicio)
  professionalId?: string; // se queda tal cual — su ausencia dispara auto-asignación (D-03)
  date: string;            // ISO YYYY-MM-DD, interpretado en timezone del negocio
}
export interface AvailableSlot {
  start: string;           // HH:mm local
  end: string;             // HH:mm local
  professionalId: string;
}
```
<!-- Row shapes confirmados en packages/db-types/src/database.types.ts (post-migración 0003) -->
```typescript
// turno.Row usa negocio_id (NO tenant_id). Campos: id, negocio_id, profesional_id,
//   cliente_id, inicio, fin, estado, precio_total.
// turno_servicio.Row: id, negocio_id, turno_id, servicio_id,
//   nombre_snapshot, precio_snapshot, duracion_snapshot.
// horario_trabajo.Row: id, negocio_id, profesional_id, dia_semana (0=dom..6=sáb),
//   hora_inicio ("HH:mm:ss"), hora_fin ("HH:mm:ss"), activo.
// bloqueo.Row: id, negocio_id, profesional_id, inicio, fin.
// servicio.Row: id, negocio_id, nombre, precio, duracion_min, ...
// negocio.Row: id, tenant_id (FK al tenant padre — legítimo), timezone, granularidad_min, ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar deps + configurar vitest en availability-engine</name>
  <read_first>
    - packages/availability-engine/package.json (stub actual — 0 deps)
    - apps/dashboard/package.json (pin exacto de vitest a copiar)
    - apps/dashboard/vitest.config.ts (plantilla de config a adaptar)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (secciones package.json y vitest.config.ts)
  </read_first>
  <action>
    Editar `packages/availability-engine/package.json`:
    - Agregar bloque `dependencies`: `"date-fns": "^4.4.0"`, `"@date-fns/tz": "^1.5.0"`, `"zod": "^4.4.3"` (mismo pin de zod que `apps/dashboard/package.json` — NO introducir una segunda versión de zod).
    - Agregar a `devDependencies`: `"vitest": "4.1.9"` (pin EXACTO usado en `apps/dashboard/package.json`).
    - Agregar a `scripts`: `"test": "vitest run"`.
    - NO agregar `@supabase/supabase-js` ni `@turnosbot/db-types` como dependency runtime (solo type-only import en types.ts).
    - Actualizar `description` para que NO diga "stub, sin lógica de negocio aún".
    Instalar con: `pnpm --filter @turnosbot/availability-engine add date-fns @date-fns/tz zod` y `pnpm --filter @turnosbot/availability-engine add -D vitest@4.1.9`.
    Crear `packages/availability-engine/vitest.config.ts` copiando la estructura de `apps/dashboard/vitest.config.ts` con estas adaptaciones: `environment: "node"`, `include: ["src/**/*.test.ts"]`, `globals: true`, y OMITIR el bloque `resolve.alias` (es específico de Next.js del dashboard; este paquete usa imports relativos).
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && grep -q "@date-fns/tz" packages/availability-engine/package.json && grep -q '"vitest"' packages/availability-engine/package.json && grep -q "src/\*\*/\*.test.ts" packages/availability-engine/vitest.config.ts && test -d packages/availability-engine/node_modules/@date-fns/tz && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@date-fns/tz" packages/availability-engine/package.json` matchea (dependency presente).
    - `grep '"date-fns"' packages/availability-engine/package.json` matchea.
    - `grep '"zod": "\^4.4.3"' packages/availability-engine/package.json` matchea (pin alineado con dashboard).
    - `grep '"test": "vitest run"' packages/availability-engine/package.json` matchea.
    - `packages/availability-engine/vitest.config.ts` existe, contiene `include: ["src/**/*.test.ts"]` y NO contiene `resolve` con `alias`.
    - `packages/availability-engine/package.json` ya NO contiene la cadena "stub".
    - `packages/availability-engine/node_modules/@date-fns/tz` existe (deps instaladas).
  </acceptance_criteria>
  <done>date-fns, @date-fns/tz, zod, vitest instalados; vitest.config.ts existe y apunta a src/**/*.test.ts; script test agregado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Definir constants.ts (ventana de reserva D-04/D-05) + su test</name>
  <read_first>
    - apps/dashboard/lib/schemas/horario.ts (líneas 20-44 — estilo de constantes exportadas con doc-comment de rationale + helper minutosDesdeMedianoche)
    - .planning/phases/03-motor-de-disponibilidad/03-CONTEXT.md (D-04/D-05)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (sección constants.ts)
  </read_first>
  <behavior>
    - Test 1: `BOOKING_MIN_LEAD_MINUTES` es exactamente `60`.
    - Test 2: `BOOKING_MAX_ADVANCE_DAYS` es exactamente `30`.
  </behavior>
  <action>
    Crear `packages/availability-engine/src/constants.ts` con doc-comment de rationale (estilo `horario.ts`) explicando el POR QUÉ (D-04/D-05: límites hardcodeados en v1, promovibles a config por-negocio en Fase 4; se definen en un único archivo para que esa promoción sea un cambio de un solo archivo):
    ```typescript
    export const BOOKING_MIN_LEAD_MINUTES = 60;
    export const BOOKING_MAX_ADVANCE_DAYS = 30;
    ```
    Crear `packages/availability-engine/src/constants.test.ts` importando `{ describe, expect, it } from "vitest"` (estilo import-explícito del repo aunque globals:true), afirmando los dos valores exactos. Esto valida el contrato de la ventana de reserva antes de que computeSlots lo consuma (D-04/D-05).
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && pnpm --filter @turnosbot/availability-engine exec vitest run src/constants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BOOKING_MIN_LEAD_MINUTES = 60" packages/availability-engine/src/constants.ts` matchea.
    - `grep "BOOKING_MAX_ADVANCE_DAYS = 30" packages/availability-engine/src/constants.ts` matchea.
    - `constants.ts` contiene un doc-comment que menciona `D-04` y `D-05`.
    - `constants.test.ts` importa `from "vitest"` explícitamente.
    - `pnpm --filter @turnosbot/availability-engine exec vitest run src/constants.test.ts` termina con 2 tests passing.
  </acceptance_criteria>
  <done>constants.ts define los dos límites como constantes exportadas con rationale; constants.test.ts verde afirmando 60 y 30.</done>
</task>

<task type="auto">
  <name>Task 3: Definir types.ts (contratos + row aliases) y fixtures deterministas</name>
  <read_first>
    - packages/availability-engine/src/index.ts (contrato stub actual — la fuente del contrato a evolucionar)
    - packages/db-types/src/database.types.ts (row shapes reales post-migración 0003: turno, turno_servicio, horario_trabajo, bloqueo, servicio, negocio)
    - apps/bot/src/db/client.ts (línea 19 — patrón `import type { Database } from "@turnosbot/db-types"`)
    - .planning/phases/03-motor-de-disponibilidad/03-PATTERNS.md (secciones types.ts y Row-type sourcing)
  </read_first>
  <action>
    Crear `packages/availability-engine/src/types.ts`:
    - `import type { Database } from "@turnosbot/db-types";` (type-only, cero runtime coupling — NO importar @supabase/supabase-js).
    - Alias de filas: `TurnoRow`, `TurnoServicioRow`, `HorarioTrabajoRow`, `BloqueoRow`, `ServicioRow`, `NegocioRow` vía `Database["public"]["Tables"]["<tabla>"]["Row"]`. NUNCA declarar shapes paralelos a mano.
    - Evolucionar el contrato público (preservando la convención de doc-comments inline del stub):
      * `ComputeSlotsInput`: renombrar `tenantId` → `negocioId` (migración 0003); `serviceId: string` → `serviceIds: string[]` (AVAIL-02 multi-servicio); `professionalId?: string` se queda igual (su ausencia dispara auto-asignación, D-03); `date: string` (ISO YYYY-MM-DD en tz del negocio) se queda.
      * `AvailableSlot`: se queda igual (`start`/`end` HH:mm local, `professionalId`).
      * Agregar `AvailabilityData` (los arrays de filas ya-fetcheadas que computeSlots recibe como 2º parámetro): `{ horarios: HorarioTrabajoRow[]; bloqueos: BloqueoRow[]; turnos: TurnoRow[]; servicios: ServicioRow[]; negocio: NegocioRow }`.
      * Agregar `BookAppointmentInput` (para el módulo booking de la Wave 4): al menos `negocioId`, `profesionalId`, `clienteId`, `serviceIds: string[]`, `inicio`, `fin` — con doc-comments; los servicios se congelan como snapshots (AVAIL-03).
    - Definir un tipo interno compartido `Interval { start: number; end: number }` (epoch ms, half-open `[start, end)`) que intervals.ts/grid.ts reutilizarán, con un doc-comment que cite la semántica `[)` del DB (`tstzrange(inicio, fin, '[)')`).
    Crear `packages/availability-engine/src/__fixtures__/rows.ts` con fixtures deterministas tipados con los aliases anteriores: al menos un `NegocioRow` (con `timezone: "America/Argentina/Buenos_Aires"`, `granularidad_min: 30`), varias `HorarioTrabajoRow` (ej. lunes 09:00-13:00 y 14:00-18:00), 1-2 `BloqueoRow`, `TurnoRow` en estados `pendiente`/`confirmado`/`cancelado`, y `ServicioRow` con `duracion_min` conocidas (ej. corte=30, barba=15). Estos fixtures son la estrategia PRIMARIA de validación (la DB live tiene horario_trabajo/bloqueo VACÍOS — Pitfall 8). Exportar helpers de construcción (ej. `makeTurno(partial)`) para que cada test override solo la dimensión que prueba.
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente Peluqueria" && grep -q 'import type { Database } from "@turnosbot/db-types"' packages/availability-engine/src/types.ts && grep -q "negocioId" packages/availability-engine/src/types.ts && grep -q "serviceIds" packages/availability-engine/src/types.ts && grep -q "horario_trabajo\|HorarioTrabajoRow" packages/availability-engine/src/__fixtures__/rows.ts && pnpm --filter @turnosbot/availability-engine exec tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'import type { Database } from "@turnosbot/db-types"' packages/availability-engine/src/types.ts` matchea (type-only import).
    - `grep -c "@supabase/supabase-js" packages/availability-engine/src/types.ts` devuelve 0 (motor puro, sin cliente DB).
    - `types.ts` contiene `negocioId` (renombrado desde tenantId) y `serviceIds` (array, AVAIL-02).
    - `types.ts` define `Interval` con comentario que menciona `[start, end)` o `[)`.
    - `types.ts` exporta `AvailabilityData` y `BookAppointmentInput`.
    - `__fixtures__/rows.ts` existe, tipado con los aliases, con al menos un turno `cancelado` y uno `confirmado` (para probar Pitfall 4 luego).
    - `pnpm --filter @turnosbot/availability-engine exec tsc --noEmit` pasa sin errores de tipos.
  </acceptance_criteria>
  <done>types.ts define contratos evolucionados + row aliases desde db-types (sin shapes paralelos); fixtures deterministas existen y tipan contra los aliases; typecheck verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumidor (bot/dashboard) → motor puro | El motor recibe filas ya-fetcheadas; confía en que el caller las scopeó al negocio correcto. El contrato debe documentar que el motor NO hace su propio scoping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `ComputeSlotsInput`/`AvailabilityData` (types.ts) | mitigate | El input requiere un campo `negocioId` explícito; documentar en el doc-comment de `AvailabilityData` que el caller debe pasar SOLO filas del `negocio_id` correcto (el motor es puro y no puede enforcar scoping por sí mismo — V4 Access Control es responsabilidad del caller). El uso de aliases de `@turnosbot/db-types` (nunca shapes a mano) garantiza que `negocio_id` esté tipado en cada fila, habilitando una aserción de scoping en Wave 2+. |
| T-03-02 | Tampering | Constantes de ventana de reserva (constants.ts) | mitigate | Los límites 60min/30d viven en el motor (constants.ts), no en la UI — futura línea de defensa contra input manipulado por prompt-injection (BOT-11, Fase 6). En este plan solo se definen; su enforcement es Wave 3. |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/availability-engine exec vitest run` corre (aunque solo constants.test.ts existe en este plan) y pasa.
- `pnpm --filter @turnosbot/availability-engine exec tsc --noEmit` verde (tipos consistentes con db-types).
- Ninguna importación de `@supabase/supabase-js` en el paquete (grep vacío).
</verification>

<success_criteria>
- date-fns@^4.4.0, @date-fns/tz@^1.5.0, zod@^4.4.3, vitest@4.1.9 instalados en el paquete.
- vitest.config.ts apunta a `src/**/*.test.ts`, sin alias del dashboard.
- constants.ts define BOOKING_MIN_LEAD_MINUTES=60 y BOOKING_MAX_ADVANCE_DAYS=30 con rationale D-04/D-05, verde en su test.
- types.ts evoluciona el contrato (negocioId, serviceIds[], AvailabilityData, BookAppointmentInput, Interval) usando aliases de @turnosbot/db-types, sin shapes paralelos, sin cliente DB.
- Fixtures deterministas existen (horario/bloqueo/turno/servicio/negocio) tipados con los aliases.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motor-de-disponibilidad/03-01-SUMMARY.md`
</output>
