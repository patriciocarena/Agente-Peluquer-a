---
phase: 04-grilla-y-turnos-del-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/components/ui/popover.tsx
  - apps/dashboard/components/owner-sidebar.tsx
  - apps/dashboard/lib/schemas/turno.ts
  - apps/dashboard/lib/schemas/turno.test.ts
  - apps/dashboard/lib/schemas/bloqueo.ts
  - apps/dashboard/lib/schemas/bloqueo.test.ts
  - apps/dashboard/lib/schemas/cliente.ts
  - apps/dashboard/lib/schemas/cliente.test.ts
autonomous: true
requirements: [APPT-01, APPT-02, APPT-06]

must_haves:
  truths:
    - "El componente shadcn Popover está instalado y disponible en components/ui/popover.tsx"
    - "El sidebar del owner muestra 'Turnos' como primer ítem, enlazando a /turnos"
    - "Existen schemas zod para turno, bloqueo y cliente que validan el input de las Server Actions de la fase"
  artifacts:
    - apps/dashboard/components/ui/popover.tsx
    - apps/dashboard/lib/schemas/turno.ts
    - apps/dashboard/lib/schemas/bloqueo.ts
    - apps/dashboard/lib/schemas/cliente.ts
  key_links:
    - "owner-sidebar.tsx enlaza /turnos (la ruta que crea Plan 07)"
    - "los schemas los importan las Server Actions de Plan 03 y Plan 04"
---

<objective>
Fundación de la fase 4 en el dashboard, sin dependencias de los otros planes (Wave 1, paralelo al Plan 01):

1. Instalar el ÚNICO componente shadcn faltante — `Popover` (necesario para D-03 slot-popover y D-05 bloqueo-popover). Ya está instalado el resto (confirmado por scan: alert-dialog, avatar, badge, button, card, checkbox, dialog, dropdown-menu, form, input, label, select, separator, sheet, sidebar, skeleton, sonner, switch, table, tabs, textarea, tooltip).
2. Agregar el ítem "Turnos" al `owner-sidebar.tsx` como PRIMER ítem de la nav (es la pantalla operativa diaria — 04-UI-SPEC.md Layout & Navigation).
3. Crear los tres schemas zod (`turno`, `bloqueo`, `cliente`) que validan el input de las Server Actions de esta fase, siguiendo el molde de `lib/schemas/servicio.ts`.

Purpose: Desbloquear los planes de acciones (03/04) y de UI (05/06/07) con la infraestructura de validación y el primitivo Popover, sin acoplar a la lógica del motor.
Output: `components/ui/popover.tsx` generado, `owner-sidebar.tsx` con nav de Turnos, tres schemas zod con tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-grilla-y-turnos-del-dashboard/04-UI-SPEC.md
@.planning/phases/04-grilla-y-turnos-del-dashboard/04-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `apps/dashboard/components/ui/popover.tsx` — componente shadcn generado (Popover, PopoverTrigger, PopoverContent)
- ítem `{ href: "/turnos", label: "Turnos", icon: CalendarDays }` en `NAV_ITEMS` de owner-sidebar.tsx
- `turnoSchema` + `TurnoInput` (lib/schemas/turno.ts)
- `bloqueoSchema` + `BloqueoInput` (lib/schemas/bloqueo.ts)
- `clienteInlineSchema` + `ClienteInlineInput`, `clienteBusquedaSchema` (lib/schemas/cliente.ts)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Popover + agregar nav "Turnos" al sidebar</name>
  <files>apps/dashboard/components/ui/popover.tsx, apps/dashboard/components/owner-sidebar.tsx</files>
  <read_first>
    - apps/dashboard/components/owner-sidebar.tsx (array NAV_ITEMS líneas ~30-34, import de lucide-react línea ~16, loop de render ~50-63)
    - apps/dashboard/components/ui/dropdown-menu.tsx (referencia del estilo de un primitivo shadcn ya generado en este repo, para confirmar que popover.tsx debe verse igual)
    - .planning/phases/04-grilla-y-turnos-del-dashboard/04-UI-SPEC.md (sección Component Inventory + Layout & Navigation: "Turnos" primer ítem, ícono CalendarDays)
  </read_first>
  <action>
    Desde `apps/dashboard/`, correr `pnpm dlx shadcn@latest add popover`. Esto genera `components/ui/popover.tsx` sourcing del paquete umbrella `radix-ui@^1.6.1` ya instalado (NO agrega una dependencia npm nueva; verificado en 04-RESEARCH.md contra el registry: `@radix-ui/react-popover@1.1.18`). Confirmar que el archivo se generó y exporta `Popover`, `PopoverTrigger`, `PopoverContent`.
    En `owner-sidebar.tsx`: agregar `CalendarDays` al import de `lucide-react`; insertar como PRIMER elemento del array `NAV_ITEMS` el objeto `{ href: "/turnos", label: "Turnos", icon: CalendarDays }` (antes de Profesionales/Servicios/Negocio, per 04-UI-SPEC.md Layout & Navigation). NO tocar el loop de render — ya maneja cualquier largo/orden del array.
  </action>
  <verify>
    <automated>test -f apps/dashboard/components/ui/popover.tsx && grep -q "PopoverContent" apps/dashboard/components/ui/popover.tsx && grep -q "/turnos" apps/dashboard/components/owner-sidebar.tsx && grep -q "CalendarDays" apps/dashboard/components/owner-sidebar.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dashboard/components/ui/popover.tsx` existe y exporta `Popover`, `PopoverTrigger`, `PopoverContent`.
    - `NAV_ITEMS` en owner-sidebar.tsx tiene `{ href: "/turnos", label: "Turnos", icon: CalendarDays }` como PRIMER elemento.
    - No se agregó ninguna entrada nueva a `apps/dashboard/package.json` dependencies (popover viene del umbrella ya instalado).
    - `pnpm --filter @turnosbot/dashboard typecheck` sin errores nuevos.
  </acceptance_criteria>
  <done>Popover instalado; sidebar enlaza /turnos como pantalla operativa principal.</done>
</task>

<task type="auto">
  <name>Task 2: Schemas zod turno / bloqueo / cliente</name>
  <files>apps/dashboard/lib/schemas/turno.ts, apps/dashboard/lib/schemas/turno.test.ts, apps/dashboard/lib/schemas/bloqueo.ts, apps/dashboard/lib/schemas/bloqueo.test.ts, apps/dashboard/lib/schemas/cliente.ts, apps/dashboard/lib/schemas/cliente.test.ts</files>
  <read_first>
    - apps/dashboard/lib/schemas/servicio.ts (molde estructural exacto: import zod, export const schema, export type Input, JSDoc que explica cada regla)
    - apps/dashboard/lib/schemas/servicio.test.ts (patrón de tests de schema)
    - packages/availability-engine/src/booking.ts (patrón `uuidLike` líneas 71-76 — usar el mismo regex de forma UUID, NO `z.uuid()` estricto, para no rechazar ids reales que la propia DB guardó; consistencia con el motor)
    - .planning/phases/04-grilla-y-turnos-del-dashboard/04-UI-SPEC.md (Copywriting: teléfono se normaliza trim/sin espacios; D-12: bloqueo sin campo motivo obligatorio, motivo es opcional)
  </read_first>
  <action>
    Crear `lib/schemas/turno.ts` con `turnoSchema` = z.object: `profesionalId` (uuidLike), `clienteId` (uuidLike), `serviceIds` (`z.array(uuidLike).min(1, "Elegí al menos un servicio.")`), `inicio` (`z.iso.datetime()`), `fin` (`z.iso.datetime()`). Export `type TurnoInput = z.infer<typeof turnoSchema>`. Definir el mismo helper `uuidLike` (regex de forma 8-4-4-4-12) que usa el motor — replicar el patrón, con JSDoc explicando por qué no `z.uuid()` estricto.
    Crear `lib/schemas/bloqueo.ts` con `bloqueoSchema` = z.object: `profesionalId` (uuidLike), `inicio` (`z.iso.datetime()`), `fin` (`z.iso.datetime()`), `motivo` (`z.string().max(280).optional()` — D-12/D-05: motivo es OPCIONAL, nunca requerido; el schema `bloqueo` de la DB tiene `motivo text` nullable). Export `type BloqueoInput`.
    Crear `lib/schemas/cliente.ts` con `clienteInlineSchema` = z.object: `telefono` (`z.string().trim().min(6, "Teléfono inválido.").max(30)`), `nombre` (`z.string().trim().max(120).optional()`). Export `type ClienteInlineInput`. Agregar también `clienteBusquedaSchema` = z.object con `telefono` (string trim min 3) para la búsqueda. JSDoc: el teléfono se normaliza (trim) antes de guardar/buscar (04-UI-SPEC.md Formatting).
    Escribir tests por cada schema (`*.test.ts`) siguiendo `servicio.test.ts`: al menos un caso válido y uno inválido por schema (ej. serviceIds vacío falla; teléfono muy corto falla; motivo ausente en bloqueo pasa).
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard test -- schemas</automated>
  </verify>
  <acceptance_criteria>
    - `turnoSchema`, `bloqueoSchema`, `clienteInlineSchema` existen y exportan sus `*Input` types.
    - `bloqueoSchema` acepta un input SIN `motivo` (opcional, D-12) — test lo prueba.
    - `turnoSchema` rechaza `serviceIds: []` con el mensaje "Elegí al menos un servicio." — test lo prueba.
    - Los schemas usan el regex `uuidLike` de forma UUID (no `z.uuid()` estricto), consistente con `booking.ts`.
    - `pnpm --filter @turnosbot/dashboard test` verde para los archivos de schema.
  </acceptance_criteria>
  <done>Tres schemas zod listos y testeados; las Server Actions de Plan 03/04 pueden importarlos sin drift respecto al motor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (form del dashboard) → Server Action | Los schemas zod son la primera capa de validación de input server-side (V5); la validación client-side es solo UX |
| shadcn CLI → repo | El componente popover se genera desde el registro oficial de shadcn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | Input malformado hacia las Server Actions de la fase | medium | mitigate | Schemas zod (`turnoSchema`/`bloqueoSchema`/`clienteInlineSchema`) con `uuidLike` + caps de longitud (`.max()`) en campos free-text (motivo, nombre) — cortan input basura en el borde |
| T-04-06 | Tampering | Cadena de suministro: instalar popover | low | accept | Popover viene del registro OFICIAL de shadcn y del umbrella `radix-ui@^1.6.1` YA instalado (verificado Fases 2/3 sin incidentes); no es un package install de una familia nueva ni de un registry de terceros — no requiere gate de legitimidad (04-UI-SPEC.md Registry Safety) |
| T-04-07 | Information Disclosure | Free-text `motivo`/`nombre` re-renderizado luego (BOT-11 Fase 6) | low | accept | Caps de longitud en el schema; el escapado por defecto de React cubre el render; sanitización profunda es alcance de Fase 6 |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/dashboard typecheck` sin errores.
- `pnpm --filter @turnosbot/dashboard test` verde para los schemas.
- `components/ui/popover.tsx` existe; `owner-sidebar.tsx` enlaza `/turnos`.
</verification>

<success_criteria>
- Popover instalado desde el registro oficial, sin dependencia npm nueva.
- Sidebar con "Turnos" como primer ítem.
- Tres schemas zod validados con tests, consistentes con el `uuidLike` del motor.
</success_criteria>

<output>
Create `.planning/phases/04-grilla-y-turnos-del-dashboard/04-02-SUMMARY.md` when done.
</output>
