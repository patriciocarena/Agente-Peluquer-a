---
phase: 04-grilla-y-turnos-del-dashboard
plan: 06
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - apps/dashboard/components/bloqueo-form-dialog.tsx
  - apps/dashboard/components/bloqueo-popover.tsx
autonomous: true
requirements: [APPT-02]

must_haves:
  truths:
    - "El dueño puede crear un bloqueo manual desde un modal con profesional + hora pre-cargados (D-03)"
    - "El dueño puede ver el motivo de un bloqueo existente y eliminarlo desde un popover (D-05)"
  artifacts:
    - apps/dashboard/components/bloqueo-form-dialog.tsx
    - apps/dashboard/components/bloqueo-popover.tsx
  key_links:
    - "bloqueo-form-dialog llama crearBloqueo (Plan 03); bloqueo-popover llama eliminarBloqueo (Plan 03)"
    - "ambos los monta la grilla (Plan 07) desde el slot-popover (crear) y las celdas de bloqueo (eliminar)"
---

<objective>
UI de bloqueos manuales (APPT-02), consumiendo las Server Actions de Plan 03. Dos componentes cliente:

1. `bloqueo-form-dialog.tsx` (D-03 rama "Bloquear"): `Dialog` con `react-hook-form` + `zodResolver(bloqueoSchema)` + `useTransition` + `sonner`, con `profesionalId` y hora de inicio PRE-CARGADOS vía props (no re-tipeo). Submit → `crearBloqueo`. Campo `motivo` opcional (Textarea).
2. `bloqueo-popover.tsx` (D-05): `Popover` con el motivo del bloqueo (o "Sin motivo especificado" si es null) + botón destructivo "Eliminar bloqueo" con confirmación. Submit → `eliminarBloqueo`.

Purpose: Cerrar la superficie visual de APPT-02 en piezas reutilizables que la grilla (Plan 07) monta. Corre en paralelo al Plan 04 (sin overlap de archivos).
Output: dos componentes cliente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-grilla-y-turnos-del-dashboard/04-UI-SPEC.md
@.planning/phases/04-grilla-y-turnos-del-dashboard/04-PATTERNS.md
@apps/dashboard/components/servicio-dialog.tsx
@apps/dashboard/components/profesionales-table.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)

- `BloqueoFormDialog` (props: `profesionalId`, `horaInicio`, `fecha`, `open`, `onOpenChange`) — apps/dashboard/components/bloqueo-form-dialog.tsx
- `BloqueoPopover` (props: `bloqueo` con motivo, `open`, `onOpenChange`) — apps/dashboard/components/bloqueo-popover.tsx
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: bloqueo-form-dialog.tsx (crear bloqueo, D-03 rama Bloquear)</name>
  <files>apps/dashboard/components/bloqueo-form-dialog.tsx</files>
  <read_first>
    - apps/dashboard/components/servicio-dialog.tsx (COMPLETO — shell Dialog + useForm + zodResolver + useTransition + setServerError + toast.success + setOpen(false); campo descripcion Textarea con value ?? "" como molde del campo motivo)
    - apps/dashboard/app/actions/bloqueos.ts (crearBloqueo signature + BloqueoActionResult — Plan 03)
    - apps/dashboard/lib/schemas/bloqueo.ts (bloqueoSchema, BloqueoInput — Plan 02)
    - .planning/phases/04-grilla-y-turnos-del-dashboard/04-UI-SPEC.md (Copywriting: título "Bloquear horario", submit "Bloquear horario", toast "Horario bloqueado."; loading inline "Bloqueando…"; typography Heading 18px para título del Dialog)
  </read_first>
  <action>
    Crear `bloqueo-form-dialog.tsx` (`"use client"`) siguiendo el shell de `servicio-dialog.tsx`. Props: `{ profesionalId: string; horaInicio: string; fecha: string; open: boolean; onOpenChange: (o: boolean) => void }` — controlado desde afuera (lo abre el slot-popover de Plan 07). `useForm<BloqueoInput>({ resolver: zodResolver(bloqueoSchema), defaultValues })` con `profesionalId` e `inicio` derivados de props (pre-cargados, D-03: "no vía re-tipeo"). El `fin` se deriva del inicio + la granularidad/duración del bloqueo — incluir un input simple de duración (ej. select de minutos) o un input de hora fin; usar la granularidad del negocio como default. Campo `motivo` como `Textarea` opcional (molde del campo `descripcion` de servicio-dialog, con `value ?? ""`). `onSubmit` en `startTransition`: `crearBloqueo(values)`; si `"error" in result` → `setServerError(result.error)`; si no → `toast.success("Horario bloqueado.")` + `onOpenChange(false)`. Botón submit: label "Bloquear horario", `disabled={isPending}`, texto "Bloqueando…" cuando `isPending` (04-UI-SPEC.md loading states). Título del Dialog "Bloquear horario" (Heading 18px semibold). Acento (azul) solo en el botón submit.
    NO usar service_role ni negocio_id en el cliente — la action deriva el negocio server-side.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bloqueo-form-dialog.tsx` es `"use client"`, usa `useForm` + `zodResolver(bloqueoSchema)` + `useTransition`.
    - `profesionalId` e `inicio` salen de props (pre-cargados), no de inputs re-tipeados.
    - `onSubmit` llama `crearBloqueo` y en éxito hace `toast.success("Horario bloqueado.")` + cierra.
    - El botón submit muestra "Bloquear horario" / "Bloqueando…" según `isPending`.
    - typecheck sin errores.
  </acceptance_criteria>
  <done>D-03 (rama Bloquear) / APPT-02: modal de creación de bloqueo con datos pre-cargados.</done>
</task>

<task type="auto">
  <name>Task 2: bloqueo-popover.tsx (motivo + eliminar, D-05)</name>
  <files>apps/dashboard/components/bloqueo-popover.tsx</files>
  <read_first>
    - apps/dashboard/components/profesionales-table.tsx (ProfesionalActivoSwitch líneas ~54-114 — patrón useState + useTransition + toast + confirmación destructiva; AlertDialog líneas ~88-111)
    - apps/dashboard/components/ui/popover.tsx (Popover/PopoverTrigger/PopoverContent — instalado en Plan 02)
    - apps/dashboard/app/actions/bloqueos.ts (eliminarBloqueo — Plan 03)
    - .planning/phases/04-grilla-y-turnos-del-dashboard/04-UI-SPEC.md (D-05: Popover con motivo o "Sin motivo especificado" en text-muted-foreground + Button destructive size sm "Eliminar bloqueo"; Copywriting confirmación "¿Eliminar este bloqueo? El horario vuelve a estar disponible."; toast "Bloqueo eliminado."; aria-label "Eliminar bloqueo")
  </read_first>
  <action>
    Crear `bloqueo-popover.tsx` (`"use client"`). Props: `{ bloqueo: { id: string; motivo: string | null }; open: boolean; onOpenChange: (o:boolean)=>void }`. Render con `Popover`/`PopoverContent`: si `bloqueo.motivo` existe, mostrarlo (`Body` 14px); si es null, "Sin motivo especificado" en `text-muted-foreground`. Debajo, un `Button` `variant="destructive"` `size="sm"` con label "Eliminar bloqueo" y `aria-label="Eliminar bloqueo"`. Al hacer click, mostrar confirmación (AlertDialog, molde de profesionales-table.tsx): título "¿Eliminar este bloqueo? El horario vuelve a estar disponible.", botones "Eliminar" (destructive) / "Cancelar". `useTransition`: `eliminarBloqueo(bloqueo.id)`; si `"error" in result` → `toast.error(result.error)`; si no → `toast.success("Bloqueo eliminado.")` + cerrar popover. Botón deshabilitado con texto "Eliminando…" mientras `isPending`.
    Destructive (rojo) para el botón de eliminar; nunca el acento azul.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bloqueo-popover.tsx` es `"use client"`, usa `Popover` de `@/components/ui/popover`.
    - Muestra el motivo o "Sin motivo especificado" cuando es null.
    - El botón de eliminar es `variant="destructive"`, tiene `aria-label="Eliminar bloqueo"`, y hay confirmación antes de llamar `eliminarBloqueo`.
    - En éxito hace `toast.success("Bloqueo eliminado.")`.
    - typecheck sin errores.
  </acceptance_criteria>
  <done>D-05 / APPT-02: popover de detalle+eliminación de bloqueo, liberando el slot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| componente cliente → Server Action | El cliente pasa ids/valores; la action re-valida y scopea server-side (nunca confía el negocio_id) |
| render de free-text (`motivo`) | El motivo se muestra en el popover; el escapado de React previene inyección de markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Tampering | `bloqueoId` manipulado en eliminar desde el cliente | high | mitigate | `eliminarBloqueo` (Plan 03) scopea con `.eq("negocio_id", negocio.id)` + RLS; el componente no puede saltear el scoping server-side |
| T-04-18 | Information Disclosure | Render de `motivo` free-text almacenado | low | accept | React escapa el texto por defecto; el schema (Plan 02) ya capa la longitud; sin `dangerouslySetInnerHTML` |
| T-04-19 | Denial of Service (UX) | Doble submit de eliminar/crear | low | mitigate | `disabled={isPending}` en los botones durante `useTransition` |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/dashboard typecheck` sin errores.
- Ambos componentes son `"use client"` y delegan la escritura a las actions de Plan 03.
- Botones destructivos con `aria-label` y confirmación.
- **QA manual obligatoria (no automatizable con typecheck):** los comportamientos runtime de `bloqueo-form-dialog`/`bloqueo-popover` — creación de bloqueo con profesional+hora pre-cargados, campo `motivo` (o "Sin motivo especificado"), confirmación destructiva, toasts "Horario bloqueado."/"Bloqueo eliminado." (APPT-02) — se verifican según `04-VALIDATION.md` → tabla "Manual-Only Verifications", paso **MQ-4** (crear/eliminar bloqueo APPT-02). El typecheck NO ejercita toasts, la pre-carga por props, la confirmación ni el repintado del slot liberado; correr MQ-4 contra el dev server tras esta wave es un gate requerido, no opcional.
</verification>

<success_criteria>
- Crear bloqueo con profesional+hora pre-cargados; eliminar bloqueo con confirmación; toasts y estados de loading correctos.
- Cero negocio_id/service_role en el cliente.
</success_criteria>

<output>
Create `.planning/phases/04-grilla-y-turnos-del-dashboard/04-06-SUMMARY.md` when done.
</output>
