---
phase: 04-grilla-y-turnos-del-dashboard
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - apps/dashboard/lib/availability-data.ts
  - apps/dashboard/app/actions/bloqueos.ts
  - apps/dashboard/app/actions/clientes.ts
autonomous: true
requirements: [APPT-02, APPT-06]

must_haves:
  truths:
    - "buildAvailabilityData(negocioId) devuelve el shape exacto AvailabilityData (6 tablas scopeadas al negocio) que consume computeSlots"
    - "El dueño puede crear un bloqueo manual y eliminarlo; ambos scopeados al negocio activo vía RLS + .eq explícito"
    - "El dueño puede buscar un cliente por teléfono o crearlo inline, siempre dentro del negocio activo"
  artifacts:
    - apps/dashboard/lib/availability-data.ts
    - apps/dashboard/app/actions/bloqueos.ts
    - apps/dashboard/app/actions/clientes.ts
  key_links:
    - "buildAvailabilityData es el único helper de fetch — lo reusan page.tsx (Plan 07) y las Server Actions de turnos (Plan 04) para evitar drift"
    - "crearBloqueo/eliminarBloqueo derivan negocio_id de getNegocioActivo(), nunca del cliente (T-02-13)"
---

<objective>
Capa de datos y de escritura NO acoplada al motor, reutilizando el patrón de Server Actions de Fase 2:

1. `lib/availability-data.ts` — `buildAvailabilityData(negocioId)`: el ÚNICO helper de fetch que arma el shape `AvailabilityData` (horarios, bloqueos, turnos, servicios, negocio) scopeado por `negocio_id` vía cliente RLS. Lo reusan tanto `page.tsx` (render de la grilla, Plan 07) como las Server Actions de turnos (revalidación, Plan 04) — un solo fetch, sin drift (RESEARCH.md Pattern 1). Además expone un helper separado para traer `turno_servicio` del detalle (Pitfall 5 — dato de UI, no input del motor).
2. `app/actions/bloqueos.ts` — `crearBloqueo` / `eliminarBloqueo`: escritura directa a la tabla `bloqueo` (APPT-02), sin pasar por el motor (bloqueo no es un turno). Con `revalidatePath("/turnos")` (Pitfall 4).
3. `app/actions/clientes.ts` — `buscarClientePorTelefono` / `crearClienteInline` (D-09, backing de APPT-06): búsqueda + alta inline de `cliente`, scopeadas al negocio activo.

Purpose: Aislar la plomería de datos/escritura simple del dashboard antes de las acciones que sí usan el motor (Plan 04) y de la UI (Planes 05-07).
Output: helper de fetch + dos módulos de Server Actions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-grilla-y-turnos-del-dashboard/04-RESEARCH.md
@.planning/phases/04-grilla-y-turnos-del-dashboard/04-PATTERNS.md
@apps/dashboard/app/actions/servicios.ts
@apps/dashboard/lib/negocio-context.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `buildAvailabilityData(negocioId: string): Promise<AvailabilityData>` (lib/availability-data.ts)
- `fetchTurnoServicios(negocioId, turnoId): Promise<TurnoServicioRow[]>` — helper de detalle (lib/availability-data.ts)
- `crearBloqueo(input: BloqueoInput): Promise<BloqueoActionResult>` (app/actions/bloqueos.ts)
- `eliminarBloqueo(bloqueoId: string): Promise<BloqueoActionResult>` (app/actions/bloqueos.ts)
- `buscarClientePorTelefono(telefono: string)` (app/actions/clientes.ts)
- `crearClienteInline(input: ClienteInlineInput)` (app/actions/clientes.ts)
- Tipos `BloqueoActionResult`, `ClienteActionResult`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: buildAvailabilityData + helper de turno_servicio</name>
  <files>apps/dashboard/lib/availability-data.ts</files>
  <read_first>
    - apps/dashboard/app/actions/servicios.ts (patrón de fetch RLS: getNegocioActivo + createClient + .eq("negocio_id", negocio.id))
    - apps/dashboard/lib/negocio-context.ts (getNegocioActivo signature: devuelve { negocio, negocios })
    - packages/availability-engine/src/types.ts (shape EXACTO de AvailabilityData líneas 82-95: horarios, bloqueos, turnos, servicios, negocio; y los row aliases líneas 22-27)
    - apps/dashboard/lib/supabase/server.ts (createClient — cliente RLS del owner, NUNCA admin/service_role)
  </read_first>
  <action>
    Crear `lib/availability-data.ts` que exporte `async function buildAvailabilityData(negocioId: string): Promise<AvailabilityData>`. Importar `AvailabilityData` (y los row types que haga falta) desde `@turnosbot/availability-engine`, y `createClient` desde `@/lib/supabase/server`. Hacer un `Promise.all` de cinco queries scopeadas por `.eq("negocio_id", negocioId)` (excepto `negocio` que es `.eq("id", negocioId).single()`): `horario_trabajo` (`select("*")`), `bloqueo` (`select("*")`), `turno` (`select("*")`), `servicio` (`select("*")`), y `negocio` (`.eq("id", negocioId).single()`). Devolver el objeto `{ horarios, bloqueos, turnos, servicios, negocio }` mapeando `data ?? []` por tabla; si el fetch de `negocio` falla o es null, `throw new Error("Hubo un problema al cargar la agenda. Recargá la página o intentá más tarde.")` (copy de 04-UI-SPEC.md Error state). El cliente DEBE ser el RLS del owner (nunca `lib/supabase/admin.ts`). NO pre-filtrar por fecha (computeSlots filtra internamente; RESEARCH.md A2 acepta el fetch completo scopeado por negocio para v1).
    Exportar además `async function fetchTurnoServicios(negocioId: string, turnoId: string): Promise<TurnoServicioRow[]>` que hace `.from("turno_servicio").select("*").eq("negocio_id", negocioId).eq("turno_id", turnoId)` — dato SOLO para el panel de detalle (Pitfall 5), separado de `buildAvailabilityData` para no contaminar el contrato del motor.
    Recibir `negocioId` como parámetro (el caller lo deriva de getNegocioActivo) para que el helper sea reusable desde page.tsx y desde las actions sin re-resolver el negocio dos veces.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `buildAvailabilityData` devuelve un objeto asignable a `AvailabilityData` (typecheck lo prueba — las 5 claves con los tipos correctos).
    - Todas las queries están scopeadas: `grep -c 'negocio_id' apps/dashboard/lib/availability-data.ts` ≥ 5 (excluyendo comentarios: usar `grep -v '^\s*//' | grep -c`).
    - El archivo importa `createClient` de `@/lib/supabase/server` y NO de `@/lib/supabase/admin`.
    - `fetchTurnoServicios` existe y filtra por `turno_id` + `negocio_id`.
  </acceptance_criteria>
  <done>Helper de fetch único listo; el shape AvailabilityData sale de la fuente de tipos del motor, sin declarar shapes paralelos.</done>
</task>

<task type="auto">
  <name>Task 2: Server Actions de bloqueo (crear/eliminar)</name>
  <files>apps/dashboard/app/actions/bloqueos.ts</files>
  <read_first>
    - apps/dashboard/app/actions/servicios.ts (COMPLETO — patrón "use server", requireRole("owner"), getNegocioActivo, SAVE_ERROR_COPY/GENERIC_ERROR_COPY, ServicioActionResult, revalidatePath; createServicio insert y toggleServicioActivo update como moldes)
    - apps/dashboard/lib/schemas/bloqueo.ts (bloqueoSchema, BloqueoInput — creado en Plan 02)
    - packages/db-types/src/database.types.ts (columnas de `bloqueo`: negocio_id, profesional_id, inicio, fin, motivo nullable)
  </read_first>
  <action>
    Crear `app/actions/bloqueos.ts` con `"use server"`. Reusar el patrón de `servicios.ts`: constantes `SAVE_ERROR_COPY`/`GENERIC_ERROR_COPY`, `export type BloqueoActionResult = { error: string } | { success: true }`.
    `crearBloqueo(input: BloqueoInput): Promise<BloqueoActionResult>`: `await requireRole("owner")`; `bloqueoSchema.safeParse(input)` → si falla, `{ error: SAVE_ERROR_COPY }`; `const { negocio } = await getNegocioActivo()` (negocio_id NUNCA del cliente, T-02-13); `createClient()`; `.from("bloqueo").insert({ negocio_id: negocio.id, profesional_id, inicio, fin, motivo: input.motivo?.trim() || null })`; si error, `{ error: GENERIC_ERROR_COPY }`; `revalidatePath("/turnos")`; `{ success: true }`.
    `eliminarBloqueo(bloqueoId: string): Promise<BloqueoActionResult>`: `requireRole("owner")`; `getNegocioActivo()`; `.from("bloqueo").delete().eq("id", bloqueoId).eq("negocio_id", negocio.id)` (defensa en profundidad además de RLS); manejo de error; `revalidatePath("/turnos")`; `{ success: true }`.
    Ambas terminan SIEMPRE en `revalidatePath("/turnos")` en el path de éxito (Pitfall 4 — el bloqueo debe reflejarse de inmediato, Success Criteria #2).
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `crearBloqueo` y `eliminarBloqueo` existen, ambas `await requireRole("owner")` como primera línea.
    - Ambas derivan `negocio.id` de `getNegocioActivo()`; ninguna lee un `negocioId` de su parámetro.
    - `eliminarBloqueo` incluye `.eq("negocio_id", negocio.id)` además del `.eq("id", ...)`.
    - Ambas contienen `revalidatePath("/turnos")` en el camino de éxito.
    - typecheck sin errores.
  </acceptance_criteria>
  <done>APPT-02 (backend): crear/eliminar bloqueo scopeado y con revalidación inmediata.</done>
</task>

<task type="auto">
  <name>Task 3: Server Actions de cliente (buscar/crear inline, D-09)</name>
  <files>apps/dashboard/app/actions/clientes.ts</files>
  <read_first>
    - apps/dashboard/app/actions/servicios.ts (patrón auth/scoping/result type)
    - apps/dashboard/lib/schemas/cliente.ts (clienteInlineSchema, clienteBusquedaSchema — Plan 02)
    - packages/db-types/src/database.types.ts (columnas de `cliente`: negocio_id, nombre nullable, telefono)
    - .planning/phases/04-grilla-y-turnos-del-dashboard/04-RESEARCH.md (Pattern 4 — skeleton de buscarClientePorTelefono/crearClienteInline)
  </read_first>
  <action>
    Crear `app/actions/clientes.ts` con `"use server"`. `export type ClienteActionResult`... usar shapes de retorno que las devuelvan datos: `buscarClientePorTelefono` devuelve `{ clientes: {id, nombre, telefono}[] }` o `{ error }`; `crearClienteInline` devuelve `{ clienteId: string }` o `{ error }`.
    `buscarClientePorTelefono(telefono: string)`: `requireRole("owner")`; validar con `clienteBusquedaSchema` (si falla, `{ clientes: [] }`); `getNegocioActivo()`; `createClient()`; `.from("cliente").select("id, nombre, telefono").eq("negocio_id", negocio.id).ilike("telefono", "%" + telefono.trim() + "%")` — usar `.ilike` con match parcial (resuelve A3 de RESEARCH.md: el dueño puede tipear dígitos parciales); devolver `{ clientes: data ?? [] }`.
    `crearClienteInline(input: ClienteInlineInput)`: `requireRole("owner")`; `clienteInlineSchema.safeParse(input)` → si falla `{ error: SAVE_ERROR_COPY }`; `getNegocioActivo()`; `.from("cliente").insert({ negocio_id: negocio.id, telefono: input.telefono.trim(), nombre: input.nombre?.trim() || null }).select("id").single()`; si error `{ error: GENERIC_ERROR_COPY }`; devolver `{ clienteId: data.id }`. NO revalidar `/turnos` (crear un cliente no cambia la grilla; el flujo del modal usa el clienteId devuelto directamente).
    negocio_id SIEMPRE de getNegocioActivo(), nunca del parámetro (T-02-13).
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `buscarClientePorTelefono` usa `.ilike` (match parcial) scopeado por `.eq("negocio_id", negocio.id)`.
    - `crearClienteInline` inserta con `negocio_id` de getNegocioActivo() y devuelve `{ clienteId }`.
    - Ninguna de las dos lee `negocioId` de su input.
    - typecheck sin errores.
  </acceptance_criteria>
  <done>D-09 (backend): búsqueda parcial por teléfono + alta inline de cliente, scopeadas al negocio activo. Backing de APPT-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (form/param) → Server Action | El `negocio_id` y los ids nunca se confían del cliente; se re-derivan/scopean server-side |
| Server Action → Postgres (RLS del owner) | RLS (`bloqueo_aislamiento`, `cliente_aislamiento`) + `.eq` explícito son doble capa de aislamiento |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege / Information Disclosure | `negocioId` manipulado hacia crearBloqueo/clientes | high | mitigate | `negocio_id` SIEMPRE derivado de `getNegocioActivo()` server-side, nunca del parámetro (T-02-13); ninguna action acepta negocioId del cliente |
| T-04-09 | Tampering | `bloqueoId` manipulado apuntando a otro negocio en eliminarBloqueo | high | mitigate | RLS `bloqueo_aislamiento` bloquea a nivel DB + `.eq("negocio_id", negocio.id)` explícito como defensa en profundidad |
| T-04-10 | Information Disclosure | Fuga cross-negocio en búsqueda de cliente | high | mitigate | `buscarClientePorTelefono` scopea con `.eq("negocio_id", negocio.id)` + RLS `cliente_aislamiento`; solo devuelve clientes del negocio activo |
| T-04-11 | Elevation of Privilege | Uso de service_role en el dashboard | high | mitigate | Todas las actions usan `createClient` (RLS del owner), nunca `lib/supabase/admin.ts` — regla dura confirmada |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/dashboard typecheck` sin errores.
- Revisión de scoping: cada query en los tres archivos incluye `.eq("negocio_id", ...)` o `.eq("id", ...).single()` para negocio.
- `buildAvailabilityData` produce un valor asignable a `AvailabilityData`.
</verification>

<success_criteria>
- Un solo helper de fetch (`buildAvailabilityData`) que otros planes reusan.
- Bloqueo crear/eliminar y cliente buscar/crear operativos, scopeados, con revalidación donde corresponde.
- Cero uso de service_role; cero `negocioId` desde el cliente.
</success_criteria>

<output>
Create `.planning/phases/04-grilla-y-turnos-del-dashboard/04-03-SUMMARY.md` when done.
</output>
