---
phase: 05-integraci-n-whatsapp-cloud-api
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/bot/src/conversation/findOrCreateCliente.ts
  - apps/bot/src/conversation/findOrCreateConversacion.ts
  - apps/bot/src/conversation/responder.ts
  - apps/bot/src/conversation/responder.test.ts
autonomous: true
requirements: [WA-02, WA-05]
must_haves:
  truths:
    - "findOrCreateCliente matches an existing cliente by EXACT telefono == wa_id (digits-only, no +), and creates one scoped to the negocio if absent (D-08)"
    - "findOrCreateConversacion returns the (negocio_id, cliente_id) conversacion, creating it with context {} if absent, and refreshes ventana_expira_at to now()+24h on every inbound (D-09, D-10)"
    - "responder(conversacion, mensajeEntrante) returns a deterministic placeholder reply and is the single swap point Phase 6 replaces (D-02)"
  artifacts:
    - apps/bot/src/conversation/findOrCreateCliente.ts
    - apps/bot/src/conversation/findOrCreateConversacion.ts
    - apps/bot/src/conversation/responder.ts
  key_links:
    - "All three are called by the worker (plan 05-05) and go through negocioScoped write accessors (D-11)"
    - "responder is the ONLY function Phase 6 swaps to attach the Vercel AI SDK agent"
---

<objective>
Build the conversation/persistence helpers of WA-02 (client identity) and WA-05 (conversation/message persistence): resolve-or-create the `cliente` from the inbound `wa_id`, find-or-create the `conversacion` and refresh its 24h window, and the deterministic `responder()` stub that Phase 6 replaces. These are the negocio-scoped write helpers the worker orchestrates.

Purpose: persistence is the source of truth for audit/debugging (WA-05) and the seam to Phase 6 (`conversacion.context`, `responder`); getting exact-match phone identity right here prevents duplicate `cliente` rows (Pitfall 7).
Output: `findOrCreateCliente.ts`, `findOrCreateConversacion.ts`, `responder.ts` (+ test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integraci-n-whatsapp-cloud-api/05-CONTEXT.md
@.planning/phases/05-integraci-n-whatsapp-cloud-api/05-RESEARCH.md
@.planning/phases/05-integraci-n-whatsapp-cloud-api/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: findOrCreateCliente (exact wa_id match, D-08)</name>
  <files>apps/bot/src/conversation/findOrCreateCliente.ts</files>
  <read_first>
    - .planning/phases/05-integraci-n-whatsapp-cloud-api/05-PATTERNS.md (findOrCreateCliente.ts section + Pitfall 7 exact-match warning)
    - apps/bot/src/db/negocioScoped.ts (read accessors + the write accessors added in 05-01: clientes(), insertCliente())
    - apps/dashboard/app/actions/clientes.ts (crearClienteInline insert shape — the analog; do NOT reuse its .ilike partial search)
    - packages/db-types/src/database.types.ts (cliente Insert shape)
  </read_first>
  <action>
    Create `apps/bot/src/conversation/findOrCreateCliente.ts` exporting `async findOrCreateCliente(negocioId: string, waId: string): Promise<string>` returning the cliente id. Find first with an EXACT match — `negocioScoped(negocioId).clientes().eq("telefono", waId).maybeSingle()` — and return the id if found. Do NOT use `.ilike` (that is the dashboard's live-search UX feature and would over-match, Pitfall 7). If absent, insert via the write accessor `negocioScoped(negocioId).insertCliente({ telefono: waId, nombre: null })` and return the new id. Document inline the phone-normalization contract (05-RESEARCH.md Open Question 3, resolved): store `telefono` as digits-only exactly as WhatsApp's `wa_id` arrives — no `+` prefix, no separators — which is a strict superset-compatible format for the dashboard's partial search. Lead with a doc-comment citing D-08, WA-02, Pitfall 7. All DB access goes through `negocioScoped` (D-11).
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Lookup uses `.eq("telefono", waId)` exact match (grep confirms `.eq("telefono"`); the file does NOT contain `.ilike`.
    - Insert path uses the `negocioScoped(...).insertCliente(...)` write accessor (negocio_id baked in, D-11).
    - The digits-only-no-`+` normalization contract is documented in a comment.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: findOrCreateConversacion + 24h window refresh (D-09, D-10)</name>
  <files>apps/bot/src/conversation/findOrCreateConversacion.ts</files>
  <read_first>
    - .planning/phases/05-integraci-n-whatsapp-cloud-api/05-RESEARCH.md (Code Examples §5 — complete find-or-create shape)
    - .planning/phases/05-integraci-n-whatsapp-cloud-api/05-PATTERNS.md (findOrCreateConversacion.ts section + the negocioScoped writable-builder note)
    - apps/bot/src/db/negocioScoped.ts (conversaciones() read + insertConversacion()/updateConversacion() write accessors from 05-01)
    - packages/db-types/src/database.types.ts lines 100-144 (conversacion Row/Insert: context Json, ventana_expira_at)
  </read_first>
  <action>
    Create `apps/bot/src/conversation/findOrCreateConversacion.ts` exporting `async findOrCreateConversacion(negocioId: string, clienteId: string): Promise<Tables<"conversacion">>`. Compute `ventanaExpiraIso = new Date(Date.now() + 24*60*60*1000).toISOString()` (D-09). Look up via `negocioScoped(negocioId).conversaciones().eq("cliente_id", clienteId).maybeSingle()`. If absent, create with the write accessor `negocioScoped(negocioId).insertConversacion({ cliente_id: clienteId, context: {}, ventana_expira_at: ventanaExpiraIso })` and return the created row — document that the minimal `context` shape written now is `{}` and that Phase 6 owns extending it (Pitfall 8: state the contract explicitly, do not leave it implicit). If present, refresh the window via `negocioScoped(negocioId).updateConversacion(existing.id, { ventana_expira_at: ventanaExpiraIso })` and return the row with the refreshed value. Lead with a doc-comment citing D-09, D-10, WA-05, Pitfall 8. Import `Tables` from `@turnosbot/db-types`.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Create path sets `context` to `{}` and `ventana_expira_at` to now()+24h; a comment states Phase 6 owns extending `context` (Pitfall 8).
    - Existing path refreshes `ventana_expira_at` to now()+24h via `updateConversacion`.
    - Both paths go through `negocioScoped` accessors (no direct `supabaseAdmin.from("conversacion")` in this file).
    - `tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: responder stub — the Phase 6 swap point (D-02)</name>
  <files>apps/bot/src/conversation/responder.ts, apps/bot/src/conversation/responder.test.ts</files>
  <read_first>
    - .planning/phases/05-integraci-n-whatsapp-cloud-api/05-RESEARCH.md (Code Examples "responder() swap point" — the exact intended implementation)
    - .planning/phases/05-integraci-n-whatsapp-cloud-api/05-PATTERNS.md (responder.ts + responder.test.ts sections)
    - packages/availability-engine/src/constants.test.ts (trivial pure-function test style)
  </read_first>
  <behavior>
    - responder returns a non-empty deterministic Spanish placeholder reply for any input.
    - The reply is obviously temporary (contains "Recibimos tu mensaje").
  </behavior>
  <action>
    Create `apps/bot/src/conversation/responder.ts` exporting `async responder(conversacion: Tables<"conversacion">, mensajeEntrante: string): Promise<string>` returning a deterministic placeholder such as `"¡Hola! Recibimos tu mensaje 🙌 En breve te ayudamos a reservar tu turno."`. Lead with the doc-comment from 05-PATTERNS.md stating this is the Phase 5 stub and that Phase 6 replaces the BODY of this function (Vercel AI SDK + Gemini) without changing this signature or any call site (D-02) — this single-point-of-replacement is the whole reason the worker calls `responder(...)` rather than inlining a reply. Create `apps/bot/src/conversation/responder.test.ts` asserting the returned string contains "Recibimos tu mensaje" for an arbitrary input, mirroring constants.test.ts.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec vitest run src/conversation/responder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `responder({} as any, "cualquier mensaje")` resolves to a string containing "Recibimos tu mensaje".
    - The file's header comment names it as the Phase 6 swap point (cites D-02).
    - `pnpm --filter @turnosbot/bot exec vitest run src/conversation/responder.test.ts` is green.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- `apps/bot/src/conversation/findOrCreateCliente.ts` (NEW) — `findOrCreateCliente(negocioId, waId)` exact-match identity.
- `apps/bot/src/conversation/findOrCreateConversacion.ts` (NEW) — `findOrCreateConversacion(negocioId, clienteId)` + 24h window refresh; writes minimal `context: {}`.
- `apps/bot/src/conversation/responder.ts` (NEW) — `responder(conversacion, mensajeEntrante)` deterministic stub (Phase 6 swap point).
- `apps/bot/src/conversation/responder.test.ts` (NEW).
- Documented contract: `conversacion.context` minimal shape is `{}` in Phase 5; Phase 6 owns extension.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → cliente/conversacion (service_role DB) | RLS is bypassed; negocio isolation is application-enforced via negocioScoped |
| inbound wa_id → cliente identity | wa_id is attacker-influenceable data; it must only ever key rows within the already-resolved negocio |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Elevation of Privilege / Info Disclosure | cliente/conversacion writes | high | mitigate | Every read/write goes through `negocioScoped(negocioId)` write accessors (D-11); `negocioId` is always the DB-resolved tenant (plan 05-05), never a payload field, so a wa_id can only ever attach to its own negocio |
| T-05-11 | Tampering (data integrity) | duplicate cliente rows | low | mitigate | Exact `.eq("telefono", waId)` match with documented digits-only-no-`+` normalization (Pitfall 7); never the dashboard's `.ilike` partial search |
| T-05-12 | Information Disclosure | conversacion.context leaking bot internals to Phase 6 | low | accept | Phase 5 writes only `{}`; the shape contract is documented so Phase 6 extends it deliberately (Pitfall 8) |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/bot test` green (responder suite).
- `tsc --noEmit` passes for the conversation modules.
- No `.ilike` and no direct `supabaseAdmin.from(...)` for scoped tables in these files.
</verification>

<success_criteria>
- Client identity resolves by exact wa_id within the negocio; conversation persists with a refreshed 24h window and a minimal documented context; the reply is produced by the single swappable responder stub.
</success_criteria>

<output>
Create `.planning/phases/05-integraci-n-whatsapp-cloud-api/05-04-SUMMARY.md` when done.
</output>
