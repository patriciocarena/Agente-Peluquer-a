---
phase: 05-integraci-n-whatsapp-cloud-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/package.json
  - apps/bot/vitest.config.ts
  - apps/bot/src/config/env.ts
  - apps/bot/src/config/env.test.ts
  - apps/bot/src/db/negocioScoped.ts
  - supabase/migrations/0004_mensaje_wa_message_id_unique.sql
autonomous: true
requirements: [WA-03]
must_haves:
  truths:
    - "apps/bot has a working vitest runner (pnpm --filter @turnosbot/bot test executes)"
    - "loadEnv() exposes WHATSAPP_APP_SECRET, WHATSAPP_VERIFY_TOKEN, WHATSAPP_LIVE (boolean), WHATSAPP_GRAPH_API_VERSION, WHATSAPP_DEV_TOKEN and SUPABASE_DB_URL"
    - "A durable UNIQUE constraint on mensaje.wa_message_id is confirmed to exist (dedup backstop for WA-03)"
    - "negocioScoped exposes write accessors that bake negocio_id into inserts/update filters"
  artifacts:
    - apps/bot/vitest.config.ts
    - apps/bot/src/config/env.test.ts
    - supabase/migrations/0004_mensaje_wa_message_id_unique.sql
  key_links:
    - "loadEnv() is the single env-access point consumed by every Phase 5 module"
    - "negocioScoped write accessors are the only sanctioned write path for the worker (D-11)"
---

<objective>
Lay the Wave 0 foundation for Phase 5: install the messaging dependencies, stand up the vitest runner in `apps/bot` (which has no test infra today), extend the env accessor with the WhatsApp/pg-boss variables every downstream module needs, add write capability to the `negocioScoped` layer (Phase 5 is its first writer), and confirm the durable dedup backstop constraint on `mensaje.wa_message_id` (WA-03).

Purpose: nothing else in the phase can be built or tested until the runner, env vars, write path, and dedup guarantee exist.
Output: extended `package.json`/`env.ts`/`negocioScoped.ts`, new `vitest.config.ts`, `env.test.ts`, and an idempotent self-documenting migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integraci-n-whatsapp-cloud-api/05-CONTEXT.md
@.planning/phases/05-integraci-n-whatsapp-cloud-api/05-RESEARCH.md
@.planning/phases/05-integraci-n-whatsapp-cloud-api/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 5 deps + stand up vitest in apps/bot</name>
  <files>apps/bot/package.json, apps/bot/vitest.config.ts</files>
  <read_first>
    - apps/bot/package.json (current scripts/deps)
    - packages/availability-engine/package.json (test script + pinned vitest 4.1.9)
    - packages/availability-engine/vitest.config.ts (exact config to mirror)
  </read_first>
  <action>
    Add production deps to `apps/bot/package.json` at the versions VERIFIED in 05-RESEARCH.md Standard Stack: `pg-boss@^12.25.1`, `@fastify/rate-limit@^11.1.0`, `@fastify/helmet@^13.0.2`, `zod@^4.4.3` (zod is not yet a dep of apps/bot). Add `vitest` pinned to exactly `4.1.9` to devDependencies (match the monorepo — do NOT let it float). Add a `"test": "vitest run"` script alongside the existing dev/build/start/typecheck scripts. Create `apps/bot/vitest.config.ts` mirroring `packages/availability-engine/vitest.config.ts` verbatim except the doc-comment: `environment: "node"`, `include: ["src/**/*.test.ts"]`, `globals: true`, no path-alias block (apps/bot uses relative NodeNext imports). Run `pnpm install` at the repo root to resolve the lockfile. All four production packages are `[VERIFIED: npm registry]` in the 05-RESEARCH.md Package Legitimacy Audit (clean slopcheck, no [ASSUMED]/[SUS]/[SLOP], no postinstall scripts) — no blocking legitimacy checkpoint is required.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @turnosbot/bot exec vitest run --passWithNoTests` exits 0 (runner is wired).
    - `apps/bot/package.json` dependencies include `pg-boss`, `@fastify/rate-limit`, `@fastify/helmet`, `zod`; devDependencies include `vitest` at exactly `4.1.9`.
    - `apps/bot/package.json` has a `test` script equal to `vitest run`.
    - `pnpm-lock.yaml` resolves with the four new packages present.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend env accessor with WhatsApp/pg-boss vars + test</name>
  <files>apps/bot/src/config/env.ts, apps/bot/src/config/env.test.ts</files>
  <read_first>
    - apps/bot/src/config/env.ts (current BotEnv interface + loadEnv)
    - apps/bot/src/db/client.ts (fail-fast-on-missing-env throw convention to mirror)
    - .env.example (existing block style — Spanish comments; extend it, do not rewrite)
    - packages/availability-engine/src/constants.test.ts (vitest pure-function test style)
  </read_first>
  <behavior>
    - loadEnv() parses WHATSAPP_LIVE === "true" into boolean true; any other value (including undefined) into false.
    - loadEnv() defaults WHATSAPP_GRAPH_API_VERSION to "v23.0" when the env var is unset.
    - loadEnv() surfaces WHATSAPP_APP_SECRET, WHATSAPP_VERIFY_TOKEN, WHATSAPP_DEV_TOKEN, SUPABASE_DB_URL as strings when set.
  </behavior>
  <action>
    Extend the `BotEnv` interface and `loadEnv()` in `apps/bot/src/config/env.ts` with: `WHATSAPP_APP_SECRET?: string` (HMAC secret, D-06), `WHATSAPP_VERIFY_TOKEN?: string` (GET handshake, D-05), `WHATSAPP_LIVE: boolean` (parse `process.env.WHATSAPP_LIVE === "true"`, default false, D-01), `WHATSAPP_GRAPH_API_VERSION: string` (default `"v23.0"` per 05-RESEARCH.md Open Question 1 — one overridable env var, never hardcoded at call sites; the executor should do one fresh check against Meta's changelog before finalizing the default), `WHATSAPP_DEV_TOKEN?: string` (dev-only token override consumed only by getWhatsappToken, D-04). `SUPABASE_DB_URL` already exists as optional — keep it optional here (the boss.ts module in plan 05-05 throws on absence at its own boot, mirroring client.ts). Add a one-line doc-comment on WHATSAPP_GRAPH_API_VERSION noting the value must be the port-5432 rule does not apply to it. Add the corresponding block to `.env.example` in the existing Spanish block style: `WHATSAPP_APP_SECRET`, `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_LIVE=false`, `WHATSAPP_GRAPH_API_VERSION=v23.0`, `WHATSAPP_DEV_TOKEN` (with a comment referencing getWhatsappToken.ts / D-04) — placeholders only, no real values. Create `apps/bot/src/config/env.test.ts` covering the three behaviors above using the constants.test.ts describe/it/expect style, mutating and restoring `process.env` around each case. Use `.js` extension on the relative import of `./env.js`.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @turnosbot/bot test` is green with `env.test.ts` present.
    - A test asserts `WHATSAPP_LIVE` is boolean `false` when the env var is unset and `true` only for the literal string "true".
    - A test asserts `WHATSAPP_GRAPH_API_VERSION` defaults to `"v23.0"` when unset.
    - `.env.example` contains the five WHATSAPP_* keys with placeholder values and no real secrets.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add write accessors to negocioScoped + confirm dedup constraint</name>
  <files>apps/bot/src/db/negocioScoped.ts, supabase/migrations/0004_mensaje_wa_message_id_unique.sql</files>
  <read_first>
    - apps/bot/src/db/negocioScoped.ts (full file — read accessors + the negocio() tenant_id caveat)
    - supabase/migrations/0001_schema_core.sql (the mensaje CREATE TABLE — confirm `wa_message_id text UNIQUE` and `direccion ... CHECK (direccion IN ('entrante','saliente'))`)
    - supabase/migrations/0003_tenant_negocio_split.sql (house-style header/isolation banner to mirror)
    - packages/db-types/src/database.types.ts lines 100-234 (conversacion + mensaje Insert shapes)
  </read_first>
  <action>
    The existing `negocioScoped(negocioId)` accessors return `.from(table).select("*").eq("negocio_id", negocioId)` builders — these are read-terminated and cannot be used for inserts. Phase 5 is the first writer through this layer (HANDOFF §5.4). Add write accessors to the returned object that preserve the "impossible to forget negocio_id" guarantee: `insertMensaje(row)` → `supabaseAdmin.from("mensaje").insert({ ...row, negocio_id: negocioId })`; `insertConversacion(row)` → `.from("conversacion").insert({ ...row, negocio_id: negocioId }).select("*").single()`; `updateConversacion(id, patch)` → `.from("conversacion").update(patch).eq("negocio_id", negocioId).eq("id", id)`; `insertCliente(row)` → `.from("cliente").insert({ ...row, negocio_id: negocioId }).select("id").single()`. Type each `row` param against the matching `Database["public"]["Tables"][t]["Insert"]` from `@turnosbot/db-types` with `negocio_id` omitted (Omit) so the accessor supplies it. Keep the existing read accessors untouched. Preserve the file's doc-comment-first convention: add a paragraph explaining that these are the first write accessors, cite D-11 and the negocio_id-baked-in guarantee. Do NOT touch the `negocio()` accessor's `.eq("tenant_id", ...)` — the file's existing comment explains why. Then create `supabase/migrations/0004_mensaje_wa_message_id_unique.sql`: an idempotent, self-documenting `CREATE UNIQUE INDEX IF NOT EXISTS mensaje_wa_message_id_key ON public.mensaje (wa_message_id)` guarded so it is a safe no-op given the plain global UNIQUE already created in 0001 (confirmed by direct file inspection in 05-PATTERNS.md). Head the file with the 0003 isolation banner and a comment: the durable dedup backstop for WA-03 already exists from 0001; this file documents intent and is safe to run against bdgufnitakelyialjoqg. Note in the file comment that live application/verification uses the existing Management API technique from scripts/verify-migration-0003.ts (querying pg_constraint) — that live run is gated on a real .env and is a merge-time check, not part of the automated suite.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec tsc -p tsconfig.json --noEmit && grep -c "wa_message_id text UNIQUE" supabase/migrations/0001_schema_core.sql</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes for apps/bot with the new write accessors present and typed.
    - `grep -c "wa_message_id text UNIQUE" supabase/migrations/0001_schema_core.sql` returns >= 1 (durable dedup backstop confirmed in applied schema).
    - `negocioScoped` returns `insertMensaje`, `insertConversacion`, `updateConversacion`, `insertCliente` accessors, each baking `negocio_id` from the scope argument.
    - `supabase/migrations/0004_mensaje_wa_message_id_unique.sql` exists, uses `CREATE UNIQUE INDEX IF NOT EXISTS`, and carries the isolation banner.
    - The `negocio()` accessor still filters by `tenant_id` (unchanged).
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- `apps/bot/vitest.config.ts` (NEW) — vitest runner config for apps/bot.
- `apps/bot/src/config/env.test.ts` (NEW) — tests for WhatsApp env parsing.
- `supabase/migrations/0004_mensaje_wa_message_id_unique.sql` (NEW) — idempotent, self-documenting dedup-constraint migration.
- `apps/bot/package.json` (MODIFIED) — new deps: `pg-boss`, `@fastify/rate-limit`, `@fastify/helmet`, `zod`; devDep `vitest@4.1.9`; `test` script.
- `apps/bot/src/config/env.ts` (MODIFIED) — new fields: `WHATSAPP_APP_SECRET`, `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_LIVE` (boolean), `WHATSAPP_GRAPH_API_VERSION` (default `v23.0`), `WHATSAPP_DEV_TOKEN`.
- `apps/bot/src/db/negocioScoped.ts` (MODIFIED) — new write accessors: `insertMensaje`, `insertConversacion`, `updateConversacion`, `insertCliente`.
- `.env.example` (MODIFIED) — five `WHATSAPP_*` placeholder keys.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party packages enter the bot's dependency tree at install time |
| worker code → Postgres (service_role) | service_role bypasses RLS; negocio isolation is application-enforced only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | pg-boss / @fastify/rate-limit / @fastify/helmet / zod installs | high | mitigate | All four are `[VERIFIED: npm registry]` in 05-RESEARCH.md Package Legitimacy Audit (clean slopcheck, no postinstall scripts, no [ASSUMED]/[SUS]/[SLOP]); no unpinned/unknown package introduced — no blocking human checkpoint required |
| T-05-03 | Tampering | mensaje dedup constraint | high | mitigate | Confirm the durable `UNIQUE(wa_message_id)` from 0001 exists (grep gate) and ship an idempotent `CREATE UNIQUE INDEX IF NOT EXISTS` as the documented backstop for WA-03 |
| T-05-07 | Elevation of Privilege | negocioScoped write accessors | high | mitigate | Write accessors bake `negocio_id` from the scope argument into every insert/update filter, so a caller cannot construct a negocio-unscoped write (D-11) |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/bot test` green.
- `pnpm --filter @turnosbot/bot exec tsc -p tsconfig.json --noEmit` passes.
- Migration file present with isolation banner; 0001 UNIQUE confirmed via grep.
</verification>

<success_criteria>
- vitest runs in apps/bot; env vars available; write path exists; dedup backstop confirmed. All downstream Phase 5 plans are unblocked.
</success_criteria>

<output>
Create `.planning/phases/05-integraci-n-whatsapp-cloud-api/05-01-SUMMARY.md` when done.
</output>
