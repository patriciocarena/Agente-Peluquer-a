---
phase: 07-hardening-y-listo-para-produccion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/src/db/negocioScoped.test.ts
autonomous: false
requirements: [SEC-03]
user_setup:
  - service: supabase
    why: "El test gated corre contra bdgufnitakelyialjoqg con los 2 negocios seed (service_role bypassa RLS, solo un test live prueba el aislamiento)"
    env_vars:
      - name: SUPABASE_URL
        source: ".env (debe contener bdgufnitakelyialjoqg)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env"

must_haves:
  truths:
    - "negocioScoped(A).<cada uno de los 12 accessors de lectura>() nunca devuelve filas del negocio B"
    - "Una tool de lectura del bot (consultarNegocioTool) con contexto del negocio A nunca surfacea ids/datos del negocio B"
  artifacts:
    - apps/bot/src/db/negocioScoped.test.ts
  key_links:
    - "El accessor negocio() se asserta por row.id === negocioId (su PK propia), no por negocio_id (no tiene esa columna)"
    - "Guard de aislamiento inline ya presente: no duplicarlo, mantenerlo al extender"
    - "Este es el camino service_role (RLS bypassed) — NO extender verify-isolation.ts (camino RLS/anon+JWT, distinto codepath — Pitfall 6)"
---

<objective>
Probar en vivo SEC-03 Success Criterion #3 (D-04): que las queries `service_role` del bot (que
bypassan RLS) con el contexto del negocio A nunca devuelven filas del negocio B. Extender
`apps/bot/src/db/negocioScoped.test.ts` (hoy solo cubre `turnos()`) para: (1) cubrir los 12
accessors de lectura de `negocioScoped`; (2) agregar un chequeo a nivel tool del bot
(`consultarNegocioTool`) con contexto del negocio equivocado.

Purpose: el service_role bypassa RLS, así que el aislamiento vive 100% en app-code
(`negocioScoped`) — solo un test live contra los 2 tenants seed lo prueba (un mock testearía la
capa misma bajo prueba). Este es explícitamente el camino service_role, NO el de RLS/dashboard de
verify-isolation.ts (Pitfall 6 de 07-RESEARCH.md).
Output: negocioScoped.test.ts extendido, gated, que sale 0 sin fugas cross-negocio.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-RESEARCH.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md
@apps/bot/src/db/negocioScoped.ts
@apps/bot/src/db/negocioScoped.test.ts
@apps/bot/src/conversation/tools/consultarNegocio.ts
@scripts/seed-fixtures.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender negocioScoped.test.ts a los 12 accessors de lectura</name>
  <files>apps/bot/src/db/negocioScoped.test.ts</files>
  <read_first>
    - apps/bot/src/db/negocioScoped.ts (los 12 accessors de lectura: negocio, profesionales, horariosTrabajo, servicios, profesionalServicios, clientes, turnos, turnoServicios, bloqueos, conversaciones, mensajes, recordatorios; nota clave: negocio() filtra por .eq("id", negocioId) — su PK — mientras el resto filtra por negocio_id)
    - apps/bot/src/db/negocioScoped.test.ts (el bloque existente de turnos() líneas ~57-83 a generalizar; el guard de aislamiento inline líneas ~48-55 a MANTENER sin duplicar; helper assert; NEGOCIO_A_ID/NEGOCIO_B_ID como constantes literales)
    - .planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md (sección negocioScoped.test.ts con la lista ACCESSORS y el loop redactado; la nota del caso especial negocio())
  </read_first>
  <action>
    Extender negocioScoped.test.ts (sin tocar el guard de aislamiento ni las constantes NEGOCIO_A_ID/
    NEGOCIO_B_ID, ya correctos). Reemplazar el bloque hard-coded de `turnos()` por un loop sobre un
    array `ACCESSORS` con los 12 accessors de lectura de negocioScoped. Para cada accessor (excepto
    negocio): correr `negocioScoped(NEGOCIO_A_ID)[accessor]().select("*")`, asertar sin error, y
    asertar que toda fila tiene `negocio_id === NEGOCIO_A_ID` y ninguna tiene `negocio_id ===
    NEGOCIO_B_ID` (con el mensaje: "negocioScoped(A).<accessor>() devolvió una fila que NO pertenece
    al negocio A." / "... devolvió filas del negocio B -- FUGA CROSS-NEGOCIO.").

    Caso especial `negocio()`: no tiene columna negocio_id (filtra por su PK `id`); asertar que toda
    fila devuelta tiene `row.id === NEGOCIO_A_ID` (nunca NEGOCIO_B_ID). Manejarlo con una rama aparte
    del loop o un predicado por-accessor.

    Repetir la pasada para NEGOCIO_B_ID (aislamiento simétrico), al menos para un subconjunto que
    tenga seed data, como ya hace el bloque B existente. Mantener el mensaje-convención existente y
    los console.log de OK por accessor. No convertir a vitest — sigue siendo un smoke corrido por
    `pnpm exec tsx` (D-05), con exit 0/1.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('apps/bot/src/db/negocioScoped.test.ts','utf8'); const need=['profesionales','servicios','clientes','bloqueos','conversaciones','mensajes','recordatorios','turnoServicios','profesionalServicios','horariosTrabajo']; const ok=need.every(a=>t.includes(a)) && t.includes('bdgufnitakelyialjoqg'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - El test enumera y ejercita los 12 accessors de lectura de negocioScoped (no solo turnos()).
    - El accessor `negocio()` se asserta por `row.id === NEGOCIO_A_ID`, no por `negocio_id`.
    - Cada accessor asserta cero filas del negocio B con el mensaje-convención de FUGA CROSS-NEGOCIO.
    - El guard de aislamiento inline preexistente queda intacto (no duplicado).
    - Sigue siendo un script tsx con exit 0/1 (no migrado a vitest).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Agregar chequeo a nivel tool del bot (consultarNegocioTool con contexto del negocio equivocado)</name>
  <files>apps/bot/src/db/negocioScoped.test.ts</files>
  <read_first>
    - apps/bot/src/conversation/tools/consultarNegocio.ts (firma consultarNegocioTool(negocioId, clienteId, deps?); tipo:"precios" devuelve { servicios: PrecioServicioView[] } con `id` real por servicio; la tool cierra sobre negocioId/clienteId y lee solo vía negocioScoped(negocioId); la forma de execute(args, { toolCallId, messages }))
    - scripts/seed-fixtures.ts (TENANT_A.clienteId; los servicio ids seed del negocio B para construir el set KNOWN_NEGOCIO_B_SERVICIO_IDS — derivarlos de la seed o leerlos con una query previa negocioScoped(B).servicios())
    - .planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md (bloque tool-level check redactado: consultarNegocioTool(NEGOCIO_A_ID, CLIENTE_A_ID).execute({tipo:'precios'}) y assert de cero overlap con los servicio ids del negocio B)
  </read_first>
  <action>
    Agregar al final de main() en negocioScoped.test.ts un bloque de chequeo a nivel tool: construir
    `consultarNegocioTool(NEGOCIO_A_ID, TENANT_A.clienteId)` (los ids nunca son controlados por el
    modelo — ver header de consultarNegocio.ts) y llamar
    `execute({ tipo: "precios" }, { toolCallId: "sec03", messages: [] })`. Construir un set
    `KNOWN_NEGOCIO_B_SERVICIO_IDS` con los servicio ids del negocio B (leerlos en vivo con
    `negocioScoped(NEGOCIO_B_ID).servicios().select("id")` al inicio del bloque, para no hardcodear).
    Asertar que `preciosA.servicios.map(s => s.id)` no tiene NINGÚN overlap con ese set (cero filtro
    a datos del negocio B), con mensaje "consultarNegocio(A) devolvió un servicio del negocio B --
    FUGA CROSS-NEGOCIO." Log OK y mantener el PASSED final del script.

    Usar el clienteId del negocio A (TENANT_A.clienteId literal, ya que apps/bot no importa de
    scripts/ — duplicar el literal como ya se hace con NEGOCIO_A_ID/NEGOCIO_B_ID).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('apps/bot/src/db/negocioScoped.test.ts','utf8'); const ok=t.includes('consultarNegocioTool') && t.includes('precios') && /NEGOCIO_B/.test(t); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - El test construye `consultarNegocioTool` con contexto del negocio A y ejecuta `tipo: "precios"`.
    - Deriva los servicio ids del negocio B en vivo (no hardcodeados) y asserta cero overlap con los servicios devueltos para A.
    - El mensaje de fallo nombra explícitamente FUGA CROSS-NEGOCIO.
    - El clienteId usado es el del negocio A (literal duplicado, convención del archivo).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Correr negocioScoped.test.ts extendido en vivo contra bdgufnitakelyialjoqg</name>
  <files>apps/bot/src/db/negocioScoped.test.ts</files>
  <action>Correr `pnpm exec tsx apps/bot/src/db/negocioScoped.test.ts` con el .env real (tras confirmar el ref bdgufnitakelyialjoqg) y confirmar exit 0 (PASSED, cero fugas cross-negocio). Ver diagnóstico en how-to-verify.</action>
  <what-built>
    negocioScoped.test.ts extendido (12 accessors + chequeo tool). Correrlo con el .env real y
    confirmar cero fugas cross-negocio.
  </what-built>
  <read_first>
    - CLAUDE.md (confirmar ref bdgufnitakelyialjoqg antes de correr)
    - apps/bot/src/db/negocioScoped.test.ts
  </read_first>
  <how-to-verify>
    1. Confirmar SUPABASE_URL → bdgufnitakelyialjoqg + SUPABASE_SERVICE_ROLE_KEY en .env.
    2. Correr `pnpm exec tsx apps/bot/src/db/negocioScoped.test.ts`.
    3. Esperado: imprime OK por cada uno de los 12 accessors (todas las filas del negocio A, cero del
       B), OK del chequeo tool consultarNegocio (cero servicios del negocio B), y termina con
       "negocioScoped.test.ts: PASSED" y exit 0.
    4. Si algún accessor sin seed data devuelve 0 filas, el assert de "toda fila pertenece a A" pasa
       vacuamente (correcto — 0 fugas). Si algún assert de FUGA CROSS-NEGOCIO dispara, es un hallazgo
       real de aislamiento: escalar, no "arreglar el test".
  </how-to-verify>
  <verify>
    <automated>pnpm exec tsx apps/bot/src/db/negocioScoped.test.ts</automated>
  </verify>
  <resume-signal>Escribí "verde" cuando salga 0 (PASSED, cero fugas), o pegá el mensaje de FUGA/FAIL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot (service_role, RLS bypassed) → tablas negocio-scoped | El único guard es negocioScoped() en app-code; RLS no protege este camino |
| tool del bot con contexto del negocio A → datos del negocio B | La tool nunca debe surfacear ids/datos de otro negocio |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information Disclosure | fuga cross-negocio vía service_role (negocioScoped) | high | mitigate | Test live que ejercita los 12 accessors + una tool del bot con los 2 tenants seed, asertando cero filas del negocio B (BOT-11 / T-06-05/08/14/17) |
| T-07-04b | Information Disclosure | extender el script equivocado (verify-isolation.ts) | medium | mitigate | Se extiende negocioScoped.test.ts (camino service_role), NO verify-isolation.ts (camino RLS/anon+JWT) — Pitfall 6 |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Sin paquetes nuevos |
</threat_model>

<verification>
- `pnpm exec tsx apps/bot/src/db/negocioScoped.test.ts` sale 0 contra bdgufnitakelyialjoqg, con OK por cada accessor + chequeo tool.
- Independiente de la migración 0005 (no toca columnas de token) — puede correrse en cualquier wave.
</verification>

<success_criteria>
SEC-03 Success Criterion #3 probado en vivo: las queries service_role del bot con contexto del
negocio A (los 12 accessors + una tool de lectura) nunca devuelven filas/ids del negocio B.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-05)
- **Modified:** `apps/bot/src/db/negocioScoped.test.ts` — extendido de 1 a 12 accessors + chequeo a nivel tool consultarNegocioTool (SEC-03 Success Criterion #3)
</artifacts_produced>

<output>
Create `.planning/phases/07-hardening-y-listo-para-produccion/07-05-SUMMARY.md` when done
</output>
