---
phase: 07-hardening-y-listo-para-produccion
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/bot/src/whatsapp/getWhatsappToken.ts
  - apps/bot/src/whatsapp/getWhatsappToken.test.ts
  - apps/dashboard/app/actions/admin-tenants.ts
  - apps/dashboard/app/actions/admin-tenants.test.ts
  - packages/availability-engine/src/__fixtures__/rows.ts
  - scripts/apply-seed.ts
  - scripts/verify-admin-tenant-lifecycle.ts
  - scripts/verify-migration-0003.ts
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "El bot resuelve el token vía .rpc('get_whatsapp_token') en vez de leer una columna en claro"
    - "El panel superadmin escribe/rota el token vía .rpc('set_whatsapp_token_secret'), nunca una columna plana"
    - "getWhatsappToken sigue respetando el short-circuit WHATSAPP_DEV_TOKEN antes de tocar el RPC"
  artifacts:
    - apps/bot/src/whatsapp/getWhatsappToken.ts
    - apps/bot/src/whatsapp/getWhatsappToken.test.ts
    - apps/dashboard/app/actions/admin-tenants.ts
    - apps/dashboard/app/actions/admin-tenants.test.ts
  key_links:
    - "negocioInsertPayload deja de referenciar la columna dropeada whatsapp_token"
    - "La nueva action setWhatsappTokenSecret sigue el shape AdminActionResult<T> del resto del archivo"
---

<objective>
Migrar los dos call-sites del token de WhatsApp al camino Vault vía RPC (SEC-01, D-02 b/c),
con cobertura unit mockeada.

- **Lectura (bot):** `getWhatsappToken.ts` deja de leer `negocio.whatsapp_token` y pasa a
  `supabaseAdmin.rpc("get_whatsapp_token", { p_negocio_id })`. Firma, short-circuit de
  `WHATSAPP_DEV_TOKEN` y mensaje de error sin cambios.
- **Escritura (dashboard):** `admin-tenants.ts` deja de referenciar la columna dropeada y
  gana una action `setWhatsappTokenSecret(negocioId, token)` que llama
  `.rpc("set_whatsapp_token_secret", …)` y devuelve el `secret_id`.

Purpose: dejar el flujo Vault production-ready (D-02) y cerrar el wiring que las verificaciones
de 07-03 ejercitan en vivo.
Output: dos call-sites en RPC + dos tests unit mockeados verdes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-RESEARCH.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md
@apps/bot/src/whatsapp/getWhatsappToken.ts
@apps/bot/src/whatsapp/graphClient.test.ts
@apps/dashboard/app/actions/admin-tenants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar getWhatsappToken.ts a .rpc('get_whatsapp_token') + test unit mockeado</name>
  <files>apps/bot/src/whatsapp/getWhatsappToken.ts, apps/bot/src/whatsapp/getWhatsappToken.test.ts</files>
  <read_first>
    - apps/bot/src/whatsapp/getWhatsappToken.ts (cuerpo actual: short-circuit WHATSAPP_DEV_TOKEN, luego .from('negocio').select('whatsapp_token'); getPhoneNumberId queda intacto)
    - apps/bot/src/whatsapp/graphClient.test.ts (convención de mocking: vi.mock('../db/client.js') con supabaseAdmin mockeado; restaurar process.env en afterEach; assert "nunca llama" para el branch de short-circuit)
    - apps/bot/src/config/env.js (forma de loadEnv y del campo WHATSAPP_DEV_TOKEN)
    - .planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md (getWhatsappToken.ts nuevo cuerpo + getWhatsappToken.test.ts, redactados verbatim)
  </read_first>
  <behavior>
    - Test 1 (happy path): con WHATSAPP_DEV_TOKEN unset y supabaseAdmin.rpc resolviendo { data: "token-real", error: null }, getWhatsappToken(negocioId) devuelve "token-real" y llamó a .rpc con ("get_whatsapp_token", { p_negocio_id: negocioId }).
    - Test 2 (error path): con rpc resolviendo { data: null, error: {...} }, getWhatsappToken lanza Error con el mensaje `No whatsapp_token found for negocioId=<id>`.
    - Test 3 (short-circuit): con WHATSAPP_DEV_TOKEN seteado, getWhatsappToken devuelve ese valor y .rpc NUNCA se llama.
  </behavior>
  <action>
    En getWhatsappToken.ts, reemplazar SOLO el bloque de lectura: en vez de
    `.from("negocio").select("whatsapp_token").eq("id", negocioId).single()`, usar
    `await supabaseAdmin.rpc("get_whatsapp_token", { p_negocio_id: negocioId })`. Mantener el
    guard: `if (error || !data) throw new Error(\`No whatsapp_token found for negocioId=${negocioId}\`)`
    y `return data`. Preservar sin tocar: la firma `getWhatsappToken(negocioId: string): Promise<string>`,
    el short-circuit `if (env.WHATSAPP_DEV_TOKEN) return env.WHATSAPP_DEV_TOKEN`, y toda la
    función `getPhoneNumberId`. Actualizar el header-comment JSDoc para decir que el camino Vault
    RPC ya está vivo (en vez del TODO(SEC-01) de interim risk).

    Crear getWhatsappToken.test.ts siguiendo graphClient.test.ts: mockear el boundary
    `../db/client.js` con `supabaseAdmin: { rpc: vi.fn(), from: vi.fn() }` e importar
    getWhatsappToken dinámicamente después del mock. Controlar WHATSAPP_DEV_TOKEN mockeando
    `../config/env.js` (loadEnv devolviendo el objeto con/sin WHATSAPP_DEV_TOKEN) para no acoplar
    a env reales, y restaurar en afterEach. Cubrir los 3 tests del bloque <behavior>.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot test -- getWhatsappToken</automated>
  </verify>
  <acceptance_criteria>
    - `apps/bot/src/whatsapp/getWhatsappToken.ts` invoca `supabaseAdmin.rpc("get_whatsapp_token", { p_negocio_id: negocioId })` y ya no hace `.from("negocio").select("whatsapp_token")`.
    - El short-circuit `WHATSAPP_DEV_TOKEN` y la firma pública quedan intactos; `getPhoneNumberId` sin cambios.
    - `getWhatsappToken.test.ts` existe y sus 3 tests (happy, error, short-circuit) pasan.
    - El test del short-circuit asserta que `.rpc` no fue llamado cuando WHATSAPP_DEV_TOKEN está seteado.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin-tenants.ts — quitar la columna dropeada + action setWhatsappTokenSecret vía RPC + test mockeado</name>
  <files>apps/dashboard/app/actions/admin-tenants.ts, apps/dashboard/app/actions/admin-tenants.test.ts</files>
  <read_first>
    - apps/dashboard/app/actions/admin-tenants.ts (negocioInsertPayload líneas ~50-65 con el `whatsapp_token: null` a eliminar; el shape AdminActionResult<T> líneas 46-48; createNegocio/updateNegocio líneas 226-284 como patrón safeParse→rpc/from→error→revalidatePath→data; el import de createAdminClient)
    - .planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md (sección admin-tenants.ts: llamada .rpc('set_whatsapp_token_secret', { p_negocio_id, p_token, p_name }) y la nota "No Analog Found" sobre el test)
    - apps/bot/src/whatsapp/getWhatsappToken.test.ts (convención vi.mock de módulo-boundary a reusar para mockear createAdminClient)
  </read_first>
  <behavior>
    - Test 1 (happy path): con createAdminClient().rpc resolviendo { data: "<secret-uuid>", error: null }, setWhatsappTokenSecret(negocioId, "tok") devuelve { data: { secretId: "<secret-uuid>" } } y llamó .rpc con ("set_whatsapp_token_secret", { p_negocio_id: negocioId, p_token: "tok", p_name: `whatsapp-token-${negocioId}` }).
    - Test 2 (validación): token vacío o negocioId inválido devuelve { error } sin llamar .rpc.
    - Test 3 (error DB): con rpc devolviendo { error }, setWhatsappTokenSecret devuelve { error: GENERIC_ERROR } y no lanza.
  </behavior>
  <action>
    En admin-tenants.ts: (1) eliminar la propiedad de la columna dropeada de `negocioInsertPayload`
    (ya no existe en la tabla) — el resto del payload (whatsapp_phone_number_id, waba_id,
    display_phone_number) queda igual. (2) Agregar una action exportada
    `setWhatsappTokenSecret(negocioId: string, token: string): Promise<AdminActionResult<{ secretId: string }>>`
    que: valida negocioId (uuid) y token no-vacío devolviendo `{ error }` temprano si falla;
    obtiene el admin client con `createAdminClient()`; llama
    `admin.rpc("set_whatsapp_token_secret", { p_negocio_id: negocioId, p_token: token, p_name: \`whatsapp-token-${negocioId}\` })`;
    ante `error` devuelve `{ error: GENERIC_ERROR }`; en éxito hace `revalidatePath` del path de
    admin del negocio y devuelve `{ data: { secretId: data } }`. Seguir el mismo orden
    safeParse→rpc→error→revalidate→data del resto del archivo.

    Crear admin-tenants.test.ts (no existe análogo directo — ver "No Analog Found"): mockear el
    boundary de `createAdminClient` (`@/lib/supabase/admin` o el path real que importe el archivo,
    confirmar al leer) con `{ rpc: vi.fn() }`, importar la action dinámicamente, y cubrir los 3
    tests del bloque <behavior>. Mockear `next/cache`'s revalidatePath para que no rompa fuera del
    runtime de Next.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard test -- admin-tenants</automated>
  </verify>
  <acceptance_criteria>
    - `negocioInsertPayload` ya no referencia la columna plana dropeada; `pnpm --filter @turnosbot/dashboard build` no rompe por esa columna.
    - Existe `setWhatsappTokenSecret` exportada que llama `.rpc("set_whatsapp_token_secret", { p_negocio_id, p_token, p_name })` y devuelve el shape `AdminActionResult<{ secretId: string }>`.
    - `admin-tenants.test.ts` existe y sus 3 tests (happy, validación, error DB) pasan.
    - La action nunca escribe el token en una columna plana (solo pasa por el RPC).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Foldeo 07-01 — actualizar fixture + scripts que aún referencian la columna dropeada whatsapp_token</name>
  <files>packages/availability-engine/src/__fixtures__/rows.ts, scripts/apply-seed.ts, scripts/verify-admin-tenant-lifecycle.ts, scripts/verify-migration-0003.ts</files>
  <read_first>
    - packages/db-types/src/database.types.ts (negocio Row/Insert ya con whatsapp_token_secret_id, sin whatsapp_token — regenerado en 07-01)
    - .planning/phases/07-hardening-y-listo-para-produccion/07-01-SUMMARY.md (sección "Handoff a 07-02 — GAP de scope": estos 4 sitios rompen por 0005, foldeados acá por decisión del usuario)
  </read_first>
  <behavior>
    - `pnpm --filter @turnosbot/availability-engine build` (tsc -b) compila (el fixture ya no referencia la columna dropeada).
    - Los 3 scripts ya no referencian negocio.whatsapp_token; siguen expresando su intención original contra el schema post-0005.
  </behavior>
  <action>
    Contexto: la migración 0005 (07-01) dropeó `negocio.whatsapp_token` y la reemplazó por
    `negocio.whatsapp_token_secret_id uuid`. Estos 4 sitios quedaron referenciando la columna
    dropeada (rompen el build en el caso del fixture, o al correrse en el caso de los scripts).

    1. **packages/availability-engine/src/__fixtures__/rows.ts (~línea 54):** el objeto es un
       `negocio` Row COMPLETO, así que debe tener el campo nuevo. Reemplazar la línea
       `whatsapp_token: null,` por `whatsapp_token_secret_id: null,`.

    2. **scripts/apply-seed.ts (~línea 177):** es un INSERT de negocio. Eliminar la propiedad
       `whatsapp_token: null,` (en Insert el campo es opcional y default NULL; NO agregar
       whatsapp_token_secret_id — el seed no crea secretos de Vault). Actualizar los comentarios
       de header que mencionan `whatsapp_token` si quedan desactualizados.

    3. **scripts/verify-admin-tenant-lifecycle.ts:** (a) en el INSERT (~línea 111) eliminar la
       propiedad `whatsapp_token: null,`. (b) El chequeo anti-fuga (~líneas 130-135) que hoy
       asserta `negocio.whatsapp_token !== null` debe pasar a asegurar que el negocio recién
       creado NO tiene un secreto de token asociado: `if (negocio.whatsapp_token_secret_id !== null)`
       con mensaje/log actualizado (la intención D-04/T-02-24 sigue siendo "un negocio recién
       creado no arranca con token"; post-Vault eso se expresa como whatsapp_token_secret_id NULL).
       Asegurar que el `.select()` traiga `whatsapp_token_secret_id` (si usa `.select()` sin args,
       ya trae todas las columnas).

    4. **scripts/verify-migration-0003.ts:** este script verifica el estado post-0003 introspectando
       columnas live. Post-0005, `negocio` YA NO tiene `whatsapp_token`. (a) En `verifyNegocio`
       (~línea 122), en la lista de columnas que negocio DEBE tener, reemplazar `"whatsapp_token"`
       por `"whatsapp_token_secret_id"` (refleja el schema live actual). (b) En `verifyTenant`
       (~línea 113), la lista de columnas que tenant NO debe tener puede dejar `"whatsapp_token"`
       tal cual (tenant sigue sin tenerla — el assert de ausencia sigue siendo verdadero). Agregar
       un comentario breve notando que whatsapp_token fue dropeada de negocio por 0005 y ahora es
       whatsapp_token_secret_id.
  </action>
  <verify>
    <automated>corepack pnpm --filter @turnosbot/availability-engine build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/availability-engine/src/__fixtures__/rows.ts` usa `whatsapp_token_secret_id: null` y ya no `whatsapp_token`; el build de availability-engine (tsc -b) compila.
    - Ningún script en `files_modified` referencia `negocio.whatsapp_token` en un INSERT/SELECT/assert (grep de `whatsapp_token\b` sin `_secret_id` sobre esos 3 scripts no da hits en código ejecutable — solo comentarios históricos permitidos).
    - `verify-admin-tenant-lifecycle.ts` conserva su intención anti-fuga expresada como `whatsapp_token_secret_id === null` en la creación.
    - `verify-migration-0003.ts` espera `whatsapp_token_secret_id` (no `whatsapp_token`) en negocio.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superadmin (dashboard, service_role) → RPC set_whatsapp_token_secret | Único camino de escritura del token; nunca una columna plana |
| bot (service_role) → RPC get_whatsapp_token | Único camino de lectura del token en runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | call-sites del token (getWhatsappToken.ts / admin-tenants.ts) | high | mitigate | Ambos pasan por los wrappers RPC Vault; ninguno lee/escribe una columna plana; cobertura unit mockeada del wiring |
| T-07-01b | Information Disclosure | short-circuit WHATSAPP_DEV_TOKEN | low | accept | Override de dev documentado e intencional; test asserta que no toca el RPC — la verificación live de Vault (07-03) corre con la var unset |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Sin paquetes nuevos en esta fase |
</threat_model>

<verification>
- `pnpm --filter @turnosbot/bot test -- getWhatsappToken` verde (3 tests).
- `pnpm --filter @turnosbot/dashboard test -- admin-tenants` verde (3 tests).
- `pnpm -r build` compila (los call-sites ya no referencian la columna dropeada, contra el db-types regenerado en 07-01).
</verification>

<success_criteria>
Los dos call-sites del token de WhatsApp pasan exclusivamente por los wrappers RPC de Vault,
con tests unit mockeados que prueban el wiring (call-site → .rpc con los args correctos). El
comportamiento live queda para el script gated de 07-03.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-02)
- **Modified:** `apps/bot/src/whatsapp/getWhatsappToken.ts` — cuerpo → `.rpc('get_whatsapp_token', { p_negocio_id })`
- **New:** `apps/bot/src/whatsapp/getWhatsappToken.test.ts` (unit mockeado, 3 tests)
- **Modified:** `apps/dashboard/app/actions/admin-tenants.ts` — nueva action `setWhatsappTokenSecret(negocioId, token)`; `negocioInsertPayload` sin la columna plana
- **New:** `apps/dashboard/app/actions/admin-tenants.test.ts` (unit mockeado, 3 tests)
- **Modified (foldeo 07-01, Task 3):** `packages/availability-engine/src/__fixtures__/rows.ts`, `scripts/apply-seed.ts`, `scripts/verify-admin-tenant-lifecycle.ts`, `scripts/verify-migration-0003.ts` — migrados de `whatsapp_token` (dropeada por 0005) a `whatsapp_token_secret_id` / eliminación de la propiedad
</artifacts_produced>

<output>
Create `.planning/phases/07-hardening-y-listo-para-produccion/07-02-SUMMARY.md` when done
</output>
