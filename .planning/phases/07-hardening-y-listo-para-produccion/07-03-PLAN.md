---
phase: 07-hardening-y-listo-para-produccion
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - scripts/verify-vault-no-plaintext.ts
autonomous: false
requirements: [SEC-01]
user_setup:
  - service: supabase
    why: "El script gated corre contra la DB live bdgufnitakelyialjoqg y escribe/lee un secreto real en Vault"
    env_vars:
      - name: SUPABASE_URL
        source: ".env (debe contener bdgufnitakelyialjoqg)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env"

must_haves:
  truths:
    - "Un SELECT directo a negocio no devuelve ningún token en claro — solo whatsapp_token_secret_id"
    - "Tras setear un secreto vía set_whatsapp_token_secret, el bot resuelve el valor real vía Vault (RPC get_whatsapp_token) con WHATSAPP_DEV_TOKEN unset"
  artifacts:
    - scripts/verify-vault-no-plaintext.ts
  key_links:
    - "El script hace delete process.env.WHATSAPP_DEV_TOKEN antes de llamar getWhatsappToken (Pitfall 5) para no enmascarar un camino Vault roto"
    - "Guard de aislamiento inline: aborta si SUPABASE_URL no contiene bdgufnitakelyialjoqg"
---

<objective>
Probar en vivo SEC-01 Success Criterion #1: que el token de WhatsApp está encriptado en reposo
(D-02 d). Un script gated `scripts/verify-vault-no-plaintext.ts` que, contra bdgufnitakelyialjoqg:
(1) confirma que un SELECT directo a `negocio` no expone ningún token en claro; (2) setea un
secreto vía el RPC `set_whatsapp_token_secret` para un negocio seed; (3) con `WHATSAPP_DEV_TOKEN`
unset, confirma que `getWhatsappToken` resuelve el valor real vía el camino Vault; (4) limpia.

Purpose: los tests unit de 07-02 solo prueban el wiring mockeado; solo un script live prueba
que la encriptación en reposo realmente funciona (D-05: los 3 Success Criteria son asserts live).
Output: script gated que sale 0 cuando el token está encriptado y resoluble vía Vault.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-RESEARCH.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md
@scripts/verify-reschedule.ts
@apps/bot/src/whatsapp/getWhatsappToken.ts
@scripts/seed-fixtures.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir scripts/verify-vault-no-plaintext.ts (gated, live)</name>
  <files>scripts/verify-vault-no-plaintext.ts</files>
  <read_first>
    - scripts/verify-reschedule.ts (esqueleto env + guard de aislamiento inline líneas ~39-63; helper assert; shape main()/cleanup()/exit; createClient service_role con autoRefreshToken:false/persistSession:false)
    - scripts/seed-fixtures.ts (TENANT_A.negocioId = "21111111-1111-1111-1111-111111111111" como negocio bajo prueba)
    - apps/bot/src/whatsapp/getWhatsappToken.ts (la firma que el script invoca para probar el camino Vault; el short-circuit WHATSAPP_DEV_TOKEN a neutralizar)
    - .planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md (sección verify-vault-no-plaintext.ts con el esqueleto y los 3 pasos de main() redactados)
  </read_first>
  <action>
    Crear scripts/verify-vault-no-plaintext.ts copiando verbatim el esqueleto de env + guard de
    verify-reschedule.ts: leer SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY, abortar (exit 1) si faltan,
    y abortar si `!SUPABASE_URL.includes("bdgufnitakelyialjoqg")` (guard inline repetido por script,
    convención del repo — no importar un helper compartido). Crear un client service_role con
    createClient<Database>. Helper `assert(cond, msg)` que hace console.error + process.exit(1).

    main() en pasos:
    1. `delete process.env.WHATSAPP_DEV_TOKEN` ANTES de importar/llamar getWhatsappToken (Pitfall 5,
       07-RESEARCH.md) — si no, el short-circuit devuelve el token de dev y el chequeo no prueba nada.
    2. No-plaintext: hacer `select("*")` sobre `negocio` para TENANT_A.negocioId y asertar que
       ninguna key de la fila expone un token en claro (la columna plana ya no existe post-0005;
       la fila solo debe traer `whatsapp_token_secret_id`). Asertar que existe la key
       whatsapp_token_secret_id y que NO aparece ninguna key cuyo nombre sea el de la columna plana
       dropeada.
    3. Seed del secreto: llamar `supabaseAdmin.rpc("set_whatsapp_token_secret", { p_negocio_id:
       TENANT_A.negocioId, p_token: "<token-de-prueba-unico>", p_name: \`whatsapp-token-verify-${Date.now()}\` })`
       y asertar que devuelve un uuid sin error.
    4. Resolución vía Vault: llamar `getWhatsappToken(TENANT_A.negocioId)` (import del módulo del bot)
       y asertar que devuelve exactamente el token de prueba seteado en el paso 3 — probando que el
       RPC get_whatsapp_token descifra vía vault.decrypted_secrets.
    5. cleanup: dejar `negocio.whatsapp_token_secret_id` en NULL de nuevo para TENANT_A (UPDATE) para
       no dejar estado — mismo espíritu cleanup() que verify-reschedule.ts. Log de PASSED y exit 0.

    Envolver en `main().catch(err => { console.error(...); process.exit(1) })`.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('scripts/verify-vault-no-plaintext.ts','utf8'); const ok=t.includes('bdgufnitakelyialjoqg') && t.includes('set_whatsapp_token_secret') && t.includes('getWhatsappToken') && t.includes('WHATSAPP_DEV_TOKEN'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - El script contiene el guard de aislamiento inline (aborta si SUPABASE_URL no contiene `bdgufnitakelyialjoqg`).
    - Hace `delete process.env.WHATSAPP_DEV_TOKEN` antes de llamar getWhatsappToken.
    - Asserta: (a) el SELECT a negocio no expone token en claro; (b) set_whatsapp_token_secret devuelve un uuid; (c) getWhatsappToken devuelve el token seteado.
    - Limpia el secret_id del negocio al final (deja NULL).
    - Sale 1 con mensaje FAIL claro ante cualquier assert roto; sale 0 con PASSED al éxito.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Correr verify-vault-no-plaintext.ts en vivo contra bdgufnitakelyialjoqg</name>
  <files>scripts/verify-vault-no-plaintext.ts</files>
  <action>Correr `pnpm exec tsx scripts/verify-vault-no-plaintext.ts` con el .env real (tras confirmar el ref bdgufnitakelyialjoqg) y confirmar exit 0 (PASSED). Ver pasos y diagnóstico en how-to-verify.</action>
  <what-built>
    El script gated de la Task 1. Ahora hay que correrlo con el .env real (mismo patrón que
    todos los verify-*.ts previos) y confirmar exit 0.
  </what-built>
  <read_first>
    - CLAUDE.md (regla de aislamiento; confirmar el ref bdgufnitakelyialjoqg antes de correr)
    - scripts/verify-vault-no-plaintext.ts
  </read_first>
  <how-to-verify>
    1. Confirmar que el .env tiene SUPABASE_URL apuntando a `bdgufnitakelyialjoqg` (nunca el
       restaurante) y SUPABASE_SERVICE_ROLE_KEY.
    2. Correr `pnpm exec tsx scripts/verify-vault-no-plaintext.ts`.
    3. Esperado: el script imprime que negocio no expone token en claro, que set_whatsapp_token_secret
       devolvió un uuid, que getWhatsappToken resolvió el token real vía Vault, limpia, y termina con
       "verify-vault-no-plaintext.ts: PASSED" y exit 0.
    4. Si sale 1: leer el mensaje FAIL. Un fallo en el paso de resolución con WHATSAPP_DEV_TOKEN aún
       seteado sería Pitfall 5 (el script debe hacer delete; verificar). Un error de "schema cache /
       not found" al llamar el RPC indicaría que 0005 no se aplicó (volver a 07-01).
  </how-to-verify>
  <verify>
    <automated>pnpm exec tsx scripts/verify-vault-no-plaintext.ts</automated>
  </verify>
  <resume-signal>Escribí "verde" cuando el script salga 0 (PASSED), o pegá el mensaje FAIL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dump/SELECT directo a `negocio` → atacante | El script prueba que este camino ya no expone token en claro |
| bot (service_role) → Vault vía RPC | El script prueba que la resolución del token realmente descifra vía vault.decrypted_secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Information Disclosure | token en reposo en `negocio` | high | mitigate | Verificación live: SELECT directo no expone token en claro; solo whatsapp_token_secret_id — prueba SEC-01 Success Criterion #1 |
| T-07-01 | Information Disclosure | camino de lectura Vault (get_whatsapp_token) | high | mitigate | El script corre con WHATSAPP_DEV_TOKEN unset (Pitfall 5) para probar de verdad el RPC, no el override de dev |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Sin paquetes nuevos |
</threat_model>

<verification>
- `pnpm exec tsx scripts/verify-vault-no-plaintext.ts` sale 0 contra bdgufnitakelyialjoqg.
- El guard de aislamiento aborta si el ref no es el correcto (probable auto-verificar cambiando SUPABASE_URL a un valor dummy debería salir 1 — no ejecutar contra el restaurante).
</verification>

<success_criteria>
SEC-01 Success Criterion #1 probado en vivo: el token está encriptado en reposo (SELECT directo
no lo expone) y el bot lo resuelve vía Vault. Script gated verde, deja la DB limpia.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-03)
- **New:** `scripts/verify-vault-no-plaintext.ts` (script gated live, SEC-01 Success Criterion #1)
</artifacts_produced>

<output>
Create `.planning/phases/07-hardening-y-listo-para-produccion/07-03-SUMMARY.md` when done
</output>
