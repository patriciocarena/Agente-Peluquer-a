---
phase: 07-hardening-y-listo-para-produccion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-concurrent-booking.ts
autonomous: false
requirements: [SEC-02]
user_setup:
  - service: supabase
    why: "El script gated dispara N reservas concurrentes reales contra bdgufnitakelyialjoqg"
    env_vars:
      - name: SUPABASE_URL
        source: ".env (debe contener bdgufnitakelyialjoqg)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env"

must_haves:
  truths:
    - "N reservas concurrentes al MISMO slot vía bookAppointment: exactamente 1 éxito, N-1 slot_taken (nunca 'al menos 1')"
    - "El rechazo de las N-1 lo decide la GiST EXCLUDE (23P01 → slot_taken), no el chequeo in-memory de computeSlots"
  artifacts:
    - scripts/verify-concurrent-booking.ts
  key_links:
    - "freshData se fetchea UNA sola vez y se comparte por referencia entre las N llamadas (Pitfall 4) — si cada llamada re-fetchea, la carrera nunca llega a Postgres y el test no prueba nada"
    - "Guard de aislamiento inline: aborta si SUPABASE_URL no contiene bdgufnitakelyialjoqg"
---

<objective>
Probar en vivo SEC-02 Success Criterion #2 (D-03): que la constraint anti-doble-reserva (GiST
EXCLUDE `turno_no_overlap`, 23P01) se sostiene bajo reservas concurrentes al mismo slot, ejercitada
a través de la función de dominio real `bookAppointment` (no inserts crudos).

Un script gated `scripts/verify-concurrent-booking.ts` que: fetchea `freshData` UNA vez, dispara N
llamadas concurrentes a `bookAppointment` con ese MISMO objeto compartido vía `Promise.allSettled`,
y asserta exactamente 1 `{ ok: true }` y N-1 `{ ok: false, reason: "slot_taken" }`.

Purpose: `verify-double-booking.ts` ya probó la constraint con inserts crudos; SEC-02 prueba que
la garantía se sostiene por el camino de aplicación completo (bookAppointment con su chequeo de
frescura + mapeo de error). Solo un script live prueba la carrera real de Postgres.
Output: script gated que sale 0 con exactamente 1 ganador.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-RESEARCH.md
@.planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md
@scripts/verify-reschedule.ts
@packages/availability-engine/src/booking.ts
@scripts/seed-fixtures.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir scripts/verify-concurrent-booking.ts (gated, live, shared freshData)</name>
  <files>scripts/verify-concurrent-booking.ts</files>
  <read_first>
    - scripts/verify-reschedule.ts (esqueleto env+guard inline líneas ~39-63; técnica de freshData stale compartido líneas ~191-219; helpers de fecha arWallClockToUtcIso/findTargetMonday/dateStrFromUtcNoon líneas ~76-107; idiom cleanup()/seed de servicio-de-prueba líneas ~113-183; assert contra result.reason === "slot_taken" línea ~243)
    - packages/availability-engine/src/booking.ts (firma bookAppointment(input, { supabase, freshData }); shape AvailabilityData; isSlotTakenConcurrently chequea error.code === "23P01"; el branch que devuelve { ok:false, reason:"slot_taken" })
    - scripts/seed-fixtures.ts (TENANT_A: negocioId/profesionalId/clienteId para el slot bajo prueba)
    - .planning/phases/07-hardening-y-listo-para-produccion/07-PATTERNS.md y 07-RESEARCH.md (Pattern 2 / Code Examples traen el bloque Promise.allSettled + fetch único de freshData redactado verbatim)
  </read_first>
  <action>
    Crear scripts/verify-concurrent-booking.ts copiando verbatim el esqueleto de env + guard inline
    de verify-reschedule.ts (abortar si faltan SUPABASE_URL/SERVICE_ROLE_KEY o si SUPABASE_URL no
    contiene "bdgufnitakelyialjoqg"). Reusar sus helpers de fecha y el idiom de seed/cleanup de un
    servicio + horario de prueba para TENANT_A.

    Flujo de main():
    1. Elegir un slot objetivo libre (usar los helpers de fecha de verify-reschedule.ts para
       apuntar a un lunes futuro dentro del horario del profesional seed; inicio/fin en UTC ISO).
    2. Fetch de `freshData: AvailabilityData` UNA SOLA VEZ (Promise.all de horario_trabajo/bloqueo/
       turno/servicio filtrados por negocio_id + la fila negocio), tal cual el bloque de
       verify-reschedule.ts. Guardar el objeto en una const.
    3. Definir N (p.ej. const N = 10; parametrizable). Disparar `Promise.allSettled` sobre N
       invocaciones de `bookAppointment({ negocioId, profesionalId, clienteId, serviceIds:[servicioId],
       inicio, fin }, { supabase: supabaseAdmin, freshData })` pasando el MISMO objeto freshData por
       referencia a las N (NUNCA re-fetchear por llamada — Pitfall 4).
    4. Contar oks = resultados fulfilled con value.ok === true; slotTaken = fulfilled con
       value.ok === false && value.reason === "slot_taken". Asertar EXACTAMENTE
       `oks.length === 1 && slotTaken.length === N - 1` (no "al menos"); si no, exit 1 con un
       mensaje que reporte cuántos de cada tipo y cualquier reason inesperado (insert_error, etc.).
    5. cleanup: borrar el turno ganador y el servicio/horario de prueba seeded (mismo cleanup() de
       verify-reschedule.ts). Log PASSED y exit 0.

    Envolver en main().catch(err => { console.error(...); process.exit(1) }).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('scripts/verify-concurrent-booking.ts','utf8'); const ok=t.includes('bdgufnitakelyialjoqg') && t.includes('allSettled') && t.includes('bookAppointment') && t.includes('slot_taken'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - El script contiene el guard de aislamiento inline (aborta si SUPABASE_URL no contiene `bdgufnitakelyialjoqg`).
    - Fetchea `freshData` UNA vez y pasa el MISMO objeto a las N llamadas de `bookAppointment` (no hay re-fetch dentro del bucle/Promise).
    - Asserta EXACTAMENTE `oks.length === 1` y `slotTaken.length === N - 1` (no "al menos 1").
    - Distingue en el reporte de fallo entre slot_taken y otros reasons (insert_error) para diagnóstico.
    - Limpia el turno ganador y el estado seeded al final.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Correr verify-concurrent-booking.ts en vivo contra bdgufnitakelyialjoqg</name>
  <files>scripts/verify-concurrent-booking.ts</files>
  <action>Correr `pnpm exec tsx scripts/verify-concurrent-booking.ts` con el .env real (tras confirmar el ref bdgufnitakelyialjoqg) y confirmar exit 0 con exactamente 1 ganador; correr 2 veces para descartar flakiness. Ver diagnóstico en how-to-verify.</action>
  <what-built>
    El script gated de la Task 1. Correrlo con el .env real y confirmar que exactamente 1 de N
    reservas concurrentes gana.
  </what-built>
  <read_first>
    - CLAUDE.md (confirmar ref bdgufnitakelyialjoqg antes de correr)
    - scripts/verify-concurrent-booking.ts
  </read_first>
  <how-to-verify>
    1. Confirmar SUPABASE_URL → bdgufnitakelyialjoqg + SUPABASE_SERVICE_ROLE_KEY en .env.
    2. Correr `pnpm exec tsx scripts/verify-concurrent-booking.ts`.
    3. Esperado: reporta N intentos, exactamente 1 éxito y N-1 slot_taken, limpia, termina con
       PASSED y exit 0.
    4. Si reporta "N éxitos" o "0 éxitos": muy probablemente cada llamada re-fetcheó freshData
       (Pitfall 4) — la carrera no llegó a la GiST. Revisar que freshData se comparta por referencia.
       Correr una segunda vez para descartar flakiness (debe dar 1 ganador de forma determinista).
  </how-to-verify>
  <verify>
    <automated>pnpm exec tsx scripts/verify-concurrent-booking.ts</automated>
  </verify>
  <resume-signal>Escribí "verde" cuando salga 0 (exactamente 1 ganador), o pegá el conteo obtenido.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reservas concurrentes (WhatsApp + dashboard) → mismo slot de un profesional | La constraint de Postgres debe garantizar un único ganador |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering (integridad de datos) | doble-reserva bajo concurrencia | high | mitigate | GiST EXCLUDE `turno_no_overlap` (ya CORE-05) verificada en vivo a través de bookAppointment con freshData compartido; exactamente 1 éxito |
| T-07-03b | Tampering | falso verde por re-fetch de freshData | medium | mitigate | El script comparte un único freshData (Pitfall 4); el checkpoint corre 2 veces para descartar flakiness |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Sin paquetes nuevos (solo Promise.allSettled nativo + @supabase/supabase-js ya instalado) |
</threat_model>

<verification>
- `pnpm exec tsx scripts/verify-concurrent-booking.ts` sale 0 con exactamente 1 ganador, de forma determinista en 2 corridas.
- Independiente de la migración 0005: bookAppointment no toca las columnas de token; este plan puede correrse antes o después de 07-01.
</verification>

<success_criteria>
SEC-02 Success Criterion #2 probado en vivo: N reservas concurrentes al mismo slot vía
bookAppointment → exactamente 1 éxito y N-1 slot_taken, decidido por la GiST de Postgres.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-04)
- **New:** `scripts/verify-concurrent-booking.ts` (script gated live, SEC-02 Success Criterion #2)
</artifacts_produced>

<output>
Create `.planning/phases/07-hardening-y-listo-para-produccion/07-04-SUMMARY.md` when done
</output>
