---
phase: 01-fundaci-n-multitenant
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/package.json
  - apps/bot/tsconfig.json
  - apps/bot/src/server.ts
  - apps/bot/src/config/env.ts
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
autonomous: true
requirements: [CORE-04]

must_haves:
  truths:
    - "The project builds and starts inside a linux/arm64 container before any business dependencies accumulate (Success Criteria #5)"
    - "The bot container runs with an unambiguous UTC clock so no runtime relies on a process-local timezone (Pitfall 4 / CORE-04 foundation)"
    - "A health check on the running arm64 container returns HTTP 200"
    - "The arm64 skeleton stays minimal — only the health server, no app features or business dependencies — so ARM is validated early before deps accumulate (D-15)"
  artifacts:
    - path: "apps/bot/src/server.ts"
      provides: "Minimal Fastify server exposing GET /health"
      min_lines: 15
    - path: "Dockerfile"
      provides: "Node 24 arm64 build of the bot service with TZ set to UTC"
      contains: "node:24"
    - path: "docker-compose.yml"
      provides: "Bot service definition with a container healthcheck"
      contains: "healthcheck"
  key_links:
    - from: "docker-compose.yml"
      to: "apps/bot/src/server.ts"
      via: "container CMD starts the Fastify server, healthcheck curls /health"
      pattern: "health"
    - from: "Dockerfile"
      to: "runtime clock"
      via: "ENV TZ=UTC (or explicit UTC-only discipline)"
      pattern: "TZ=UTC"
---

<objective>
Own `apps/bot` and PROVE the project builds and starts on `linux/arm64` with a working health check — BEFORE any schema or business dependencies accumulate (Success Criteria #5, D-15). This plan is self-contained: `apps/bot` builds as a standalone Docker image (its own package.json + fastify), so it has zero file overlap with the scaffolding plan (01) and runs in the same wave. Also pins the container clock to UTC — the concrete CORE-04 / Pitfall-4 discipline at the container level.

Purpose: De-risk the Oracle Cloud ARM VPS target at the cheapest moment (Pitfall 12) against an empty dependency tree. `apps/bot` is picked up by the workspace `apps/*` glob once scaffolding exists; the arm64 proof itself does not depend on the workspace root.
Output: `apps/bot` Fastify `/health` service + Dockerfile + docker-compose that build and run on `linux/arm64`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.env.example

<interfaces>
<!-- apps/bot is a plain Node/Fastify long-running process (NOT edge). For the arm64 proof it builds STANDALONE: -->
<!--   apps/bot/package.json declares fastify ^5.9.0, pino, tsx (dev), typescript directly (no workspace deps yet). -->
<!--   The Dockerfile COPYs apps/bot and installs within it — no dependency on the workspace root/lockfile. -->
<!-- Later phases wire apps/bot into the pnpm workspace (packages/* deps); this skeleton keeps it minimal (D-15). -->
<!-- POINT-IN-TIME GATE: this arm64 build is a one-time Success-Criteria-#5 proof against a minimal apps/bot. -->
<!--   Plan 05 later augments apps/bot/package.json with @supabase/supabase-js + @turnosbot/db-types (both pure-JS, -->
<!--   arm64-safe) for the tenantScoped DB layer — that is NOT a regression of this gate (the gate already passed), -->
<!--   and a future phase makes the Dockerfile workspace-aware for production. Do not re-block on it here. -->
<!-- Pinned versions from STACK.md (npm-verified): Node 24.x LTS (linux-arm64 images), fastify ^5.9.0, pino (bundled). -->
<!-- ENV var names fixed by .env.example: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, etc. (read but not required at boot). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: apps/bot health server + arm64 Dockerfile + docker-compose with healthcheck</name>
  <files>apps/bot/package.json, apps/bot/tsconfig.json, apps/bot/src/server.ts, apps/bot/src/config/env.ts, Dockerfile, docker-compose.yml, .dockerignore</files>
  <read_first>
    - .planning/research/STACK.md ("Development Tools" — Docker Compose notes; Node 24 arm64 images; fastify/pino)
    - .planning/research/PITFALLS.md (Pitfall 4 — container TZ discipline; Pitfall 12 — arm64 native-dep failures)
    - .planning/research/ARCHITECTURE.md (apps/bot responsibility — webhook/health only shell in this phase)
    - .env.example (env var names the config loader reads)
  </read_first>
  <action>
    `apps/bot/package.json`: a self-contained package (`@turnosbot/bot`) declaring `fastify ^5.9.0`, `pino`, `tsx` (dev), `typescript`, with a `start` script. No pg-boss/AI-SDK yet (those land in their phases — keep the arm64 surface minimal per D-15). `apps/bot/tsconfig.json` extends the workspace base if present but must also compile standalone.
    `apps/bot/src/server.ts`: a minimal Fastify app listening on `0.0.0.0` at `process.env.PORT ?? 3001`, exposing `GET /health` returning HTTP 200 with a small JSON body (e.g. `{ status: 'ok' }`), using Fastify's built-in pino logger. No webhook, no DB, no AI (D-15).
    `apps/bot/src/config/env.ts`: a tiny loader reading the fixed env var names from `.env.example` but NOT requiring them at boot for the health endpoint (wired in later phases). Single env-access point.
    `Dockerfile`: multi-stage build FROM `node:24` (arm64 resolves on an arm64 host; platform pinned at build time in Task 2). Build ONLY `apps/bot` standalone (COPY apps/bot, install inside it), run as a non-root user. Set `ENV TZ=UTC` and rely on UTC internally only — the concrete CORE-04 / Pitfall-4 container discipline (NEVER hardcode `-3`; AR presentation conversion is deferred to consuming phases).
    `docker-compose.yml`: one `bot` service built from the Dockerfile, mapping the bot port, with a `healthcheck` curling `http://localhost:3001/health`. `.dockerignore` excludes `node_modules`, `.next`, `dist`, `.env`, `.git`.
  </action>
  <acceptance_criteria>
    - `apps/bot/src/server.ts` registers `GET /health` and starts a Fastify listener bound to `0.0.0.0`.
    - `Dockerfile` contains `node:24` and `ENV TZ=UTC` (or an equivalent explicit UTC-only statement) and a non-root `USER` directive.
    - `docker-compose.yml` defines a `bot` service with a `healthcheck` block hitting `/health`.
    - `.dockerignore` excludes `node_modules` and `.env`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "/health" apps/bot/src/server.ts && grep -q "node:24" Dockerfile && grep -Eq "TZ=UTC" Dockerfile && grep -q "healthcheck" docker-compose.yml && grep -q "node_modules" .dockerignore</automated>
  </verify>
  <done>apps/bot serves GET /health = 200; Dockerfile targets Node 24 with UTC clock and non-root user; compose defines a healthchecked bot service; builds standalone (no workspace-root dependency).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Verify build + start on linux/arm64 with health check</name>
  <files>docker-compose.yml</files>
  <read_first>
    - Dockerfile (from Task 1)
    - docker-compose.yml (from Task 1)
    - .planning/research/PITFALLS.md (Pitfall 12 — arm64 build verification via `--platform linux/arm64`, "works on my machine" trap)
  </read_first>
  <action>
    Build the bot image explicitly for the ARM target: `docker buildx build --platform linux/arm64 -t turnosbot-bot:arm64 .` (buildx/Docker required — the local host is arm64 so this is native). Start the container (`docker run` or `docker compose up -d bot`), wait for the healthcheck to report healthy, and confirm `GET /health` returns 200. Tear the container down after.
    If Docker/buildx is NOT installed in the execution environment, STOP and surface a blocker (this is the Success-Criteria-#5 gate and cannot be faked by a local `pnpm build` — a green non-container build is a false positive per Pitfall 12). Do not mark the plan complete without a real arm64 container run.
  </action>
  <acceptance_criteria>
    - `docker buildx build --platform linux/arm64 -t turnosbot-bot:arm64 .` exits 0.
    - The container starts and its healthcheck reaches `healthy` (or a manual `curl http://localhost:3001/health` against the running container returns HTTP 200).
    - The running image's architecture is arm64 (`docker image inspect turnosbot-bot:arm64 --format '{{.Architecture}}'` prints `arm64`).
  </acceptance_criteria>
  <verify>
    <automated>docker buildx build --platform linux/arm64 -t turnosbot-bot:arm64 . && docker run -d --name tb-health -p 3001:3001 turnosbot-bot:arm64 && sleep 5 && curl -fsS http://localhost:3001/health && docker image inspect turnosbot-bot:arm64 --format '{{.Architecture}}' | grep -q arm64; R=$?; docker rm -f tb-health >/dev/null 2>&1; exit $R</automated>
  </verify>
  <done>A linux/arm64 container built from apps/bot starts and answers GET /health with 200 — Success Criteria #5 proven against an empty dependency tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → bot container | The bot exposes an HTTP port; only `/health` exists in this phase (no untrusted business input yet) |
| host clock → app runtime | Container timezone must be unambiguous or timestamp meaning drifts across runtimes (CORE-04) |
| npm registry → build | fastify/pino/tsx pulled during the image build (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `/health` endpoint | mitigate | Health returns a minimal static body only — no env vars, versions, or DB state leaked |
| T-02-02 | Elevation of Privilege | container runtime | mitigate | Dockerfile runs as a non-root `USER`; `.dockerignore` keeps `.env` and `.git` out of the image |
| T-02-03 | Tampering | timestamp semantics | mitigate | `ENV TZ=UTC`, UTC-only internal discipline (Pitfall 4) — presentation-layer AR conversion deferred to consuming phases; `-3` offset never hardcoded |
| T-02-SC | Tampering (supply chain) | image build (fastify, pino, tsx, typescript) | mitigate | Canonical first-party libs, npm-verified in STACK.md; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages; slopcheck any unrecognized transitive |
</threat_model>

<verification>
- `docker buildx build --platform linux/arm64` exits 0 and the resulting image reports `arm64`.
- Running container's `/health` returns 200 and the healthcheck reports healthy.
- Dockerfile pins Node 24 + UTC clock + non-root user.
</verification>

<success_criteria>
apps/bot builds and starts in a `linux/arm64` container and answers a health check with HTTP 200, with the container clock pinned to UTC and no business dependencies yet accumulated (ROADMAP Phase 1 Success Criteria #5).
</success_criteria>

<output>
Create `.planning/phases/01-fundaci-n-multitenant/01-02-SUMMARY.md` when done.
</output>
