---
phase: 01-fundaci-n-multitenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .npmrc
  - apps/dashboard/package.json
  - apps/dashboard/tsconfig.json
  - packages/availability-engine/package.json
  - packages/availability-engine/src/index.ts
  - packages/db-types/package.json
  - packages/db-types/src/index.ts
  - packages/shared/package.json
  - packages/shared/src/index.ts
autonomous: true
requirements: [CORE-03]

must_haves:
  truths:
    - "The pnpm workspace installs and typechecks with every shared package present as a valid stub (foundation for Success Criteria #5 and for the typed tenant-scoped query layer, CORE-03)"
    - "packages/db-types is the single shared type source both apps consume so the RLS path and the bot's tenantScoped path never drift"
    - "The Phase-1 skeleton is the minimal-real monorepo structure (apps/*, packages/*, supabase/migrations) with no business logic — only the scaffolding every later plan builds on (D-14)"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace declaration (apps/*, packages/*)"
      contains: "packages:"
    - path: "packages/db-types/src/index.ts"
      provides: "Shared DB-types entry point (real types generated in plan 04)"
      min_lines: 1
    - path: "tsconfig.base.json"
      provides: "Shared strict TS config extended by every package/app"
      contains: "strict"
  key_links:
    - from: "packages/*/package.json"
      to: "root pnpm workspace"
      via: "apps/* + packages/* globs resolve the members"
      pattern: "packages:"
---

<objective>
Stand up the pnpm monorepo workspace scaffolding (D-14): root workspace config, a shared strict TS base config, and the empty `packages/*` and `apps/dashboard` stubs. No business logic. This is the structural substrate every later plan builds on, and specifically the home of `packages/db-types` — the single shared type source the dashboard RLS path and the bot's `tenantScoped` path both consume (CORE-03 foundation; the layer itself is implemented in plan 05).

Purpose: Keep the file-count budget small and the wave parallel — this plan owns the workspace root + shared packages + dashboard stub, while plan 02 independently owns `apps/bot` and the arm64 Docker proof (zero file overlap).
Output: pnpm workspace that installs and typechecks with `packages/availability-engine`, `packages/db-types`, `packages/shared`, and `apps/dashboard` stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.env.example

<interfaces>
<!-- Target monorepo layout (from ARCHITECTURE.md "Recommended Project Structure"). -->
<!-- Package manager: pnpm workspaces (Claude's Discretion per CONTEXT — pnpm chosen: lighter than Turborepo). -->
<!-- Pinned versions from STACK.md (all verified on npm registry via `npm view`): -->
<!--   Node 24.x LTS (linux-arm64), typescript ^5.7, tsx (dev). Next 16.2.x + @supabase/ssr declared in dashboard stub. -->
<!-- This plan does NOT create apps/bot — plan 02 owns it (self-contained arm64 Docker proof). The `apps/*` -->
<!--   workspace glob picks apps/bot up automatically once plan 02 creates it; no explicit listing needed. -->
<!-- ENV var names fixed by .env.example: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, -->
<!--   NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_DB_URL, GOOGLE_GENERATIVE_AI_API_KEY. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pnpm workspace root + shared package/dashboard stubs</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, .npmrc, apps/dashboard/package.json, apps/dashboard/tsconfig.json, packages/availability-engine/package.json, packages/availability-engine/src/index.ts, packages/db-types/package.json, packages/db-types/src/index.ts, packages/shared/package.json, packages/shared/src/index.ts</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md ("Recommended Project Structure" — the exact directory tree)
    - .planning/research/STACK.md (pinned versions: Node 24, typescript ^5.7, tsx, next 16.2.x, @supabase/ssr; "Version Compatibility" table)
    - ./CLAUDE.md (Technology Stack section — do NOT reuse anything from other projects; greenfield)
    - .env.example (fixed env var names)
  </read_first>
  <action>
    Create a pnpm workspace (`pnpm-workspace.yaml` listing `apps/*` and `packages/*` globs). Root `package.json`: private, `packageManager` pinned to a pnpm version, workspace scripts (`build`, `typecheck`), `engines.node` >= 24. Add `.npmrc` (`engine-strict=true`). Create `tsconfig.base.json` (target ES2022, module NodeNext, `strict: true`) that each package/app tsconfig `extends`.
    Create STUB packages only — no business logic: `packages/availability-engine` (export a placeholder `computeSlots` type signature stub, no implementation), `packages/db-types` (`index.ts` placeholder re-export — real generated types land in plan 04), `packages/shared` (export an `es-AR` constants placeholder). Scope names under `@turnosbot/*`.
    `apps/dashboard`: package.json declaring Next.js 16.2.x + `@supabase/ssr` + `@supabase/supabase-js` (DECLARED only, no app code — Phase 2 builds the UI) + a tsconfig stub extending the base.
    Do NOT create `apps/bot` (plan 02 owns it). Do NOT reuse any schema/code/patterns from the restaurant project (CLAUDE.md hard isolation). Everything greenfield.
  </action>
  <acceptance_criteria>
    - `pnpm install` at the repo root exits 0 and resolves the workspace members that exist (packages/* + apps/dashboard).
    - `pnpm -w exec tsc -b` (or `--dry`) exits 0 across the workspace.
    - `pnpm-workspace.yaml` contains both `apps/*` and `packages/*` globs.
    - `packages/availability-engine`, `packages/db-types`, `packages/shared` each exist with a `package.json` and a `src/index.ts` (stubs, zero business logic); `apps/dashboard` exists as a stub.
    - `grep -rEl "hzgunbftloevclkohcdf|menu_items|restaurants|call_logs" apps packages` returns no files (no restaurant-project contamination).
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && test -f pnpm-workspace.yaml && test -f packages/availability-engine/src/index.ts && test -f packages/db-types/src/index.ts && test -f packages/shared/src/index.ts && test -f apps/dashboard/package.json && test -z "$(grep -rEl 'hzgunbftloevclkohcdf|menu_items|restaurants|call_logs' apps packages 2>/dev/null)"</automated>
  </verify>
  <done>Workspace installs and typechecks; all three shared packages and the dashboard exist as valid stubs with no business logic and no cross-project contamination; apps/bot intentionally deferred to plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party packages are pulled during `pnpm install` (supply chain) |
| other projects → this repo | Hard isolation rule: no code/schema/patterns from the restaurant project may enter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | cross-project contamination | mitigate | grep gate rejects any restaurant-project identifier in apps/packages; greenfield-only per CLAUDE.md |
| T-01-SC | Tampering (supply chain) | `pnpm install` (typescript, tsx, next, @supabase/ssr, @supabase/supabase-js) | mitigate | All packages are canonical first-party libs version-verified against the npm registry in STACK.md (`npm view`, HIGH confidence). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages; run a slopcheck if an unrecognized transitive package appears |
</threat_model>

<verification>
- `pnpm install` + `pnpm -w exec tsc -b` pass.
- Workspace globs resolve; three shared packages + dashboard stub present.
- No restaurant-project identifiers anywhere in apps/packages.
</verification>

<success_criteria>
The pnpm monorepo scaffolding installs and typechecks with all shared packages and the dashboard present as valid stubs — the structural substrate (incl. packages/db-types for CORE-03) is in place, with apps/bot + arm64 proof handled independently in plan 02.
</success_criteria>

<output>
Create `.planning/phases/01-fundaci-n-multitenant/01-01-SUMMARY.md` when done.
</output>
