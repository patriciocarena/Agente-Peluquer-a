---
phase: 01-fundaci-n-multitenant
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - .env.example
  - supabase/config.toml
  - packages/db-types/src/database.types.ts
  - packages/db-types/src/index.ts
autonomous: false
requirements: [CORE-01, CORE-05]

must_haves:
  truths:
    - "The full reference schema, RLS policies, and the GiST constraint are live in the bdgufnitakelyialjoqg Supabase project (not just in files)"
    - "Generated TypeScript types mirror the live schema so both apps share one source of truth"
  artifacts:
    - path: "packages/db-types/src/database.types.ts"
      provides: "Supabase-generated TypeScript types for the live schema"
      min_lines: 30
  key_links:
    - from: "supabase/migrations/*.sql"
      to: "live Postgres (bdgufnitakelyialjoqg)"
      via: "supabase db push / Management API apply against the correct project ref"
      pattern: "bdgufnitakelyialjoqg"
---

<objective>
Apply the schema + RLS migrations to the LIVE Supabase project `bdgufnitakelyialjoqg` and generate shared TypeScript types from the applied schema. Without this, later verification passes against nothing and build/type checks would be a false positive (the schema-push gate). This runs only AFTER the arm64 target is proven (plan 02) and the scaffolding + migration files exist (plans 01, 03).

Purpose: Make the isolation, timezone, and anti-double-booking guarantees real in the actual database that Phase 2+ will use, and lock the shared type surface.
Output: migrations applied to `bdgufnitakelyialjoqg`; `packages/db-types/src/database.types.ts` generated.

CRITICAL ISOLATION: target ONLY `bdgufnitakelyialjoqg`. NEVER the restaurant project `hzgunbftloevclkohcdf`. Do NOT use the globally-configured `mcp__supabase__*` MCP (it points at the restaurant project). Use the Supabase CLI linked to `bdgufnitakelyialjoqg`, or the Management API with the PAT from `.env`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@./CLAUDE.md
@.planning/phases/01-fundaci-n-multitenant/01-CONTEXT.md
@.env.example
@supabase/migrations/0001_schema_core.sql
@supabase/migrations/0002_rls_policies.sql

<interfaces>
<!-- Target project ref: bdgufnitakelyialjoqg  (URL https://bdgufnitakelyialjoqg.supabase.co) -->
<!-- FORBIDDEN ref: hzgunbftloevclkohcdf (restaurant). FORBIDDEN tool: mcp__supabase__* (points at restaurant). -->
<!-- .env.example env var names: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_DB_URL, -->
<!--   NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY. -->
<!-- Push needs either: (a) SUPABASE_DB_URL (direct 5432 session-mode) for `supabase db push`, or -->
<!--   (b) a Supabase PAT (SUPABASE_ACCESS_TOKEN) for the CLI/Management API. Both are secrets not yet present. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human provides live credentials for bdgufnitakelyialjoqg</name>
  <what-built>Migration files are ready; applying them requires live secrets Claude cannot obtain (Supabase project DB password / access token). `.env` does not yet exist (only `.env.example`, and `.env` is gitignored).</what-built>
  <read_first>
    - .env.example (the exact var names to fill)
    - ./CLAUDE.md (isolation rule — which project ref is correct)
  </read_first>
  <how-to-verify>
    1. Copy `.env.example` to `.env` (gitignored).
    2. Fill values for the `bdgufnitakelyialjoqg` project ONLY (from the Supabase dashboard → Project Settings → API / Database):
       - `SUPABASE_URL=https://bdgufnitakelyialjoqg.supabase.co`
       - `SUPABASE_SERVICE_ROLE_KEY=` (service_role key)
       - `NEXT_PUBLIC_SUPABASE_URL=https://bdgufnitakelyialjoqg.supabase.co`
       - `NEXT_PUBLIC_SUPABASE_ANON_KEY=` (anon/publishable key)
       - `SUPABASE_DB_URL=postgresql://postgres:<DB_PASSWORD>@db.bdgufnitakelyialjoqg.supabase.co:5432/postgres`
       - Add `SUPABASE_ACCESS_TOKEN=` (a Personal Access Token from Supabase → Account → Access Tokens) — needed for CLI login / Management API. Also add this var name to `.env.example` (no value).
    3. Confirm the ref in every value is `bdgufnitakelyialjoqg`, NOT `hzgunbftloevclkohcdf`.
  </how-to-verify>
  <acceptance_criteria>
    - `.env` exists and contains a non-empty `SUPABASE_DB_URL` (or `SUPABASE_ACCESS_TOKEN`) whose host/ref is `bdgufnitakelyialjoqg`.
    - `grep -c hzgunbftloevclkohcdf .env` returns 0 (restaurant ref absent).
  </acceptance_criteria>
  <resume-signal>Type "listo" once `.env` is populated for bdgufnitakelyialjoqg, or describe any blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migrations to the live bdgufnitakelyialjoqg project</name>
  <files>supabase/config.toml</files>
  <read_first>
    - .env (populated in Task 1 — SUPABASE_DB_URL / SUPABASE_ACCESS_TOKEN, correct ref)
    - supabase/migrations/0001_schema_core.sql, supabase/migrations/0002_rls_policies.sql
    - ./CLAUDE.md (isolation rule; do NOT use mcp__supabase__*)
  </read_first>
  <action>
    Install/use the Supabase CLI (or Management API). Link the project to ref `bdgufnitakelyialjoqg` explicitly (`supabase link --project-ref bdgufnitakelyialjoqg` using `SUPABASE_ACCESS_TOKEN`), then `supabase db push` to apply `0001` and `0002`. Alternatively apply each migration file via the Management API SQL endpoint with the PAT, targeting `bdgufnitakelyialjoqg`. Set `SUPABASE_ACCESS_TOKEN` in the non-TTY environment so no interactive prompt is needed. If the CLI still requires an interactive DB-password prompt that cannot be suppressed, pass it via `SUPABASE_DB_PASSWORD` / the `SUPABASE_DB_URL` connection string.
    VERIFY on the live DB (via `psql "$SUPABASE_DB_URL"` or Management API SQL): the `btree_gist` extension is installed; the `turno` EXCLUDE constraint exists; `rowsecurity = true` for every tenant-scoped table in `pg_tables`; the `auth_tenant_id()` function exists; exactly 14 base tables exist in the `public` schema. ABORT and surface a blocker if the connected ref is not `bdgufnitakelyialjoqg`.
    Never touch `hzgunbftloevclkohcdf`. Never use `mcp__supabase__*`.
  </action>
  <acceptance_criteria>
    - A live query `SELECT extname FROM pg_extension WHERE extname='btree_gist'` returns one row.
    - A live query confirms the `turno` overlap EXCLUDE constraint exists (`SELECT conname FROM pg_constraint WHERE contype='x' AND conrelid='turno'::regclass` returns a row).
    - `SELECT count(*) FROM pg_tables WHERE schemaname='public' AND rowsecurity=false AND tablename IN (<tenant tables>)` returns 0 (RLS on for all tenant tables).
    - The connected project ref is `bdgufnitakelyialjoqg` (confirmed via `supabase projects list` / the DB host), and NOT `hzgunbftloevclkohcdf`.
  </acceptance_criteria>
  <verify>
    <automated>psql "$SUPABASE_DB_URL" -tAc "SELECT (SELECT count(*) FROM pg_extension WHERE extname='btree_gist') AS ext, (SELECT count(*) FROM pg_constraint WHERE contype='x' AND conrelid='turno'::regclass) AS excl, (SELECT count(*) FROM pg_proc WHERE proname='auth_tenant_id') AS fn;" | grep -Eq "1\|1\|1|1\|[1-9]"</automated>
  </verify>
  <done>The full schema, RLS, and GiST constraint are live in bdgufnitakelyialjoqg, confirmed by direct queries against the live database.</done>
</task>

<task type="auto">
  <name>Task 3: Generate shared TypeScript types from the live schema</name>
  <files>packages/db-types/src/database.types.ts, packages/db-types/src/index.ts</files>
  <read_first>
    - packages/db-types/src/index.ts (the stub from plan 01 — re-export the generated types)
    - .env (project ref / connection for type generation)
  </read_first>
  <action>
    Generate types from the live schema: `supabase gen types typescript --project-id bdgufnitakelyialjoqg > packages/db-types/src/database.types.ts` (or `--db-url "$SUPABASE_DB_URL"`). Update `packages/db-types/src/index.ts` to re-export `Database` (and convenient row/insert helper types) so both `apps/bot` and `apps/dashboard` consume one type source (ARCHITECTURE.md: prevents drift). Run the workspace typecheck to confirm the generated file compiles.
  </action>
  <acceptance_criteria>
    - `packages/db-types/src/database.types.ts` exists and exports a `Database` type that includes the Spanish domain tables (e.g. `turno`, `servicio`, `perfil`).
    - `packages/db-types/src/index.ts` re-exports the generated types.
    - `pnpm -w exec tsc -b` (or the db-types package typecheck) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/db-types/src/database.types.ts && grep -q "turno" packages/db-types/src/database.types.ts && grep -q "Database" packages/db-types/src/index.ts</automated>
  </verify>
  <done>Generated types mirror the live schema and are re-exported for both apps; the workspace typechecks against them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → live Supabase project | Migrations mutate the real database; targeting the wrong project ref would corrupt an unrelated project |
| secrets (.env) → tooling | DB password / PAT grant full DB control; must stay gitignored and off the restaurant project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | wrong-project migration apply | mitigate | Explicit `--project-ref bdgufnitakelyialjoqg`; pre-apply guard aborts if the connected ref is `hzgunbftloevclkohcdf`; the global `mcp__supabase__*` MCP (restaurant) is forbidden |
| T-04-02 | Information Disclosure | `.env` secrets | mitigate | `.env` is gitignored (confirmed in `.gitignore`); `.env.example` holds no real values; PAT/DB password never committed or logged |
| T-04-03 | Repudiation | untracked live schema drift | mitigate | Schema applied exclusively via versioned migration files (`0001`/`0002`), not ad-hoc SQL, so the live state is reproducible from the repo |
</threat_model>

<verification>
- Live DB confirms `btree_gist`, the `turno` EXCLUDE constraint, `auth_tenant_id()`, RLS enabled on all tenant tables, and exactly 14 base tables.
- Connected ref is `bdgufnitakelyialjoqg`; restaurant ref absent everywhere.
- Generated `database.types.ts` compiles and is re-exported.
</verification>

<success_criteria>
The reference schema, its RLS policies, and the anti-double-booking constraint are live in the correct Supabase project and reflected in generated shared types — the phase's later verification (plan 05) can now run against a real database.
</success_criteria>

<output>
Create `.planning/phases/01-fundaci-n-multitenant/01-04-SUMMARY.md` when done.
</output>
