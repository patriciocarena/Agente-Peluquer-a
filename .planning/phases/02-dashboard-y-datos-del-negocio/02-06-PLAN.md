---
phase: 02-dashboard-y-datos-del-negocio
plan: 06
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - apps/dashboard/app/(owner)/profesionales/page.tsx
  - apps/dashboard/app/(owner)/profesionales/nuevo/page.tsx
  - apps/dashboard/components/profesionales-table.tsx
  - apps/dashboard/components/profesional-form.tsx
  - apps/dashboard/app/actions/profesionales.ts
  - apps/dashboard/lib/schemas/profesional.ts
  - apps/dashboard/lib/schemas/profesional.test.ts
autonomous: true
requirements: [PRO-01]
must_haves:
  truths:
    - "El owner crea, edita, desactiva/reactiva y lista profesionales del negocio activo"
    - "Los profesionales inactivos permanecen en la tabla, muteados, con badge gris 'Inactivo', filtrables por Tabs Todos/Activos/Inactivos"
    - "El alta de un profesional es una página full-page (no modal) con espacio para datos generales (y, en el plan 07, horario y servicios)"
  artifacts:
    - "apps/dashboard/app/(owner)/profesionales/{page,nuevo/page}.tsx"
    - "apps/dashboard/components/{profesionales-table,profesional-form}.tsx"
    - "apps/dashboard/app/actions/profesionales.ts + lib/schemas/profesional.ts"
  key_links:
    - "negocio-context (negocio activo) -> profesionales.action derivan negocio_id server-side"
    - "profesional-form (datos generales) -> extendido en 02-07 con horario + matriz de servicios"
---

<objective>
CRUD base de profesionales (PRO-01): lista con Tabs/Switch/soft-delete y alta/edición full-page de los datos generales. Establece el `profesional-form` y el Server Action que el plan 02-07 extenderá con el editor de horario semanal y la matriz de servicios (PRO-02/03/04).

Purpose: El profesional es la unidad a la que se le asignan horarios, servicios y turnos; su CRUD base es prerequisito del editor de horario y la matriz.
Output: página lista, página nuevo, tabla, form base, action base, schema con test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md
@apps/dashboard/lib/negocio-context.ts
@apps/dashboard/components/servicios-table.tsx
@packages/db-types/src/database.types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema profesional + test unitario</name>
  <files>apps/dashboard/lib/schemas/profesional.ts, apps/dashboard/lib/schemas/profesional.test.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md Code Examples (ejemplo profesionalSchema: nombre requerido, activo boolean)
  </read_first>
  <behavior>
    - profesionalSchema rechaza nombre vacío; acepta activo boolean con default true. Test: caso válido + nombre vacío rechazado.
  </behavior>
  <action>
    Crear `lib/schemas/profesional.ts` (zod per el ejemplo de RESEARCH.md: nombre requerido con mensaje "El nombre es obligatorio", activo boolean default true; dejar el schema extensible para que 02-07 agregue horario/servicios si lo necesita, o mantener schemas separados para esas sub-entidades). Crear `lib/schemas/profesional.test.ts` (vitest): acepta un profesional válido, rechaza nombre vacío. Escribir el test primero (RED) y luego el schema (GREEN).
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec vitest run lib/schemas/profesional.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    profesional.test.ts pasa; el schema rechaza nombre vacío y acepta activo boolean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Lista de profesionales (Tabs/Switch/soft-delete)</name>
  <files>apps/dashboard/app/(owner)/profesionales/page.tsx, apps/dashboard/components/profesionales-table.tsx</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Profesional = full-page create/edit; Soft-Delete Tabs/Switch/AlertDialog con copy "¿Desactivar a {nombre}? ..."; badge gris "Inactivo"; empty state "Todavía no cargaste profesionales" + CTA "+ Nuevo profesional"; focal point = CTA acento)
    - apps/dashboard/components/servicios-table.tsx (mismo patrón Tabs/Switch/soft-delete ya construido en 02-05)
  </read_first>
  <action>
    Crear `app/(owner)/profesionales/page.tsx`: lista los profesionales del negocio activo (server read RLS-scoped), header con CTA "+ Nuevo profesional" (que navega a /profesionales/nuevo, único acento), empty state con el copy exacto. Crear `components/profesionales-table.tsx` (client) reusando el patrón de servicios-table: Table con Tabs "Todos/Activos/Inactivos", Switch por fila (apagar abre AlertDialog destructivo con copy exacto "¿Desactivar a {nombre}? Ya no va a aparecer disponible para nuevos turnos, pero se conserva su historial."; encender reactiva sin confirmación), filas inactivas muteadas con badge gris "Inactivo" (no rojo), y link "Editar" a /profesionales/[id]/editar por fila. NO reordenamiento (los profesionales no se ordenan por drag).
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const p=require('fs').readFileSync('apps/dashboard/app/(owner)/profesionales/page.tsx','utf8'); if(!p.includes('+ Nuevo profesional')){console.error('falta CTA');process.exit(1)} const t=require('fs').readFileSync('apps/dashboard/components/profesionales-table.tsx','utf8'); if(!t.includes('Tabs')||!t.includes('editar')){console.error('tabla incompleta');process.exit(1)} console.log('lista OK')"</automated>
  </verify>
  <acceptance_criteria>
    La lista muestra profesionales del negocio activo con CTA "+ Nuevo profesional" y empty state exacto; la tabla tiene Tabs, Switch con AlertDialog destructivo (copy exacto), badge gris "Inactivo", y link a editar; tsc pasa.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Alta full-page + form base + Server Action base</name>
  <files>apps/dashboard/app/(owner)/profesionales/nuevo/page.tsx, apps/dashboard/components/profesional-form.tsx, apps/dashboard/app/actions/profesionales.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (full-page create; secciones "Datos generales" / "Horario" / "Servicios" separadas por 32px — en este plan solo "Datos generales")
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md (anti-pattern: derivar negocio_id server-side)
    - apps/dashboard/lib/negocio-context.ts, apps/dashboard/lib/schemas/profesional.ts
  </read_first>
  <action>
    Crear `components/profesional-form.tsx` (client, react-hook-form + zodResolver con profesionalSchema): la sección "Datos generales" (nombre, activo) como primer bloque; estructurar el componente para que 02-07 pueda insertar debajo las secciones "Horario semanal" y "Servicios que realiza" sin reescribirlo (dejar slots/secciones claras separadas por 32px). Crear `app/(owner)/profesionales/nuevo/page.tsx`: full-page que renderiza el form con CTA "Guardar". Crear `app/actions/profesionales.ts` con `createProfesional` y `updateProfesional` (datos generales) y `toggleProfesionalActivo`: re-validan con profesionalSchema server-side, derivan negocio_id del contexto server-side (nunca del form), insert/update RLS-scoped, revalidan path. Dejar el archivo de actions preparado para que 02-07 agregue las actions de horario y matriz de servicios.
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const a=require('fs').readFileSync('apps/dashboard/app/actions/profesionales.ts','utf8'); for(const f of ['createProfesional','updateProfesional','toggleProfesionalActivo']){if(!a.includes(f)){console.error('falta',f);process.exit(1)}} if(!a.includes('profesionalSchema')){console.error('no re-valida');process.exit(1)} console.log('actions base OK')"</automated>
  </verify>
  <acceptance_criteria>
    /profesionales/nuevo renderiza el form full-page con "Datos generales"; createProfesional/updateProfesional/toggleProfesionalActivo re-validan con zod, derivan negocio_id server-side y persisten RLS-scoped; el form deja secciones para horario/servicios; tsc pasa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (form) -> Server Action | Input de profesional; negocio_id derivado server-side |
| owner JWT -> profesional (RLS por negocio) | Solo profesionales de negocios del propio tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Tampering | Owner envía negocio_id ajeno al crear profesional | high | mitigate | negocio_id derivado del contexto server-side + RLS por auth_negocio_ids() |
| T-02-17 | Tampering | nombre vacío u otros campos inválidos | low | mitigate | profesionalSchema re-validado server-side |
</threat_model>

<verification>
- vitest: profesional.test.ts pasa.
- tsc pasa; lista con Tabs/Switch/soft-delete; alta full-page; actions re-validan y derivan negocio_id server-side.
</verification>

<success_criteria>
El owner gestiona el CRUD base de profesionales del negocio activo con soft-delete, sobre una página de alta full-page lista para recibir horario y matriz de servicios en 02-07.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-y-datos-del-negocio/02-06-SUMMARY.md` when done
</output>
