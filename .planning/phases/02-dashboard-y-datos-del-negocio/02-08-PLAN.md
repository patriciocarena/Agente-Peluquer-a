---
phase: 02-dashboard-y-datos-del-negocio
plan: 08
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - apps/dashboard/app/(admin)/admin/layout.tsx
  - apps/dashboard/app/(admin)/admin/page.tsx
  - apps/dashboard/app/(admin)/admin/[tenantId]/page.tsx
  - apps/dashboard/components/admin/tenant-dialog.tsx
  - apps/dashboard/components/admin/negocio-dialog.tsx
  - apps/dashboard/app/actions/admin-tenants.ts
  - apps/dashboard/lib/schemas/admin.ts
  - apps/dashboard/lib/schemas/admin.test.ts
  - scripts/bootstrap-superadmin.ts
  - scripts/verify-admin-tenant-lifecycle.ts
autonomous: false
requirements: [SADMIN-01, SADMIN-02, SADMIN-03]
user_setup:
  - service: supabase
    why: "El primer superadmin no tiene alta self-service; hay que crearlo con service_role y credenciales reales que debe proveer el humano"
    env_vars:
      - name: SUPABASE_URL
        source: "bdgufnitakelyialjoqg (guard de aislamiento)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard bdgufnitakelyialjoqg -> API -> service_role"
    dashboard_config:
      - task: "Proveer/confirmar el email y contraseña del primer superadmin (no hardcodear en un archivo commiteado)"
        location: "Coordinar con el dueño del producto"
must_haves:
  truths:
    - "El superadmin crea, edita y desactiva Tenants (grupos, solo nombre) desde /admin"
    - "Dentro de un tenant, el superadmin crea, edita y desactiva Negocios con datos generales + config WhatsApp no-secreta (sin token)"
    - "El alta Tenant+owner+primer Negocio es atómica-por-compensación: si falla un insert en Postgres se borra el auth.user creado"
    - "Un owner nunca alcanza /admin; el panel usa service_role SOLO server-side"
  artifacts:
    - "apps/dashboard/app/(admin)/admin/{layout,page,[tenantId]/page}.tsx"
    - "apps/dashboard/components/admin/{tenant-dialog,negocio-dialog}.tsx"
    - "apps/dashboard/app/actions/admin-tenants.ts (service_role, compensating transaction)"
    - "scripts/bootstrap-superadmin.ts + scripts/verify-admin-tenant-lifecycle.ts"
  key_links:
    - "admin-tenants.ts -> lib/supabase/admin.ts (service_role, server-only) — único importador"
    - "createTenantWithNegocio -> auth.admin.createUser -> tenant -> negocio -> perfil(owner) -> rollback en fallo"
    - "bootstrap-superadmin -> perfil(rol=superadmin, tenant_id=NULL) -> habilita el gate /admin"
---

<objective>
Construir el panel superadmin `/admin` sobre la jerarquía Tenant(grupo) -> Negocio(s): CRUD de Tenants (solo nombre, SADMIN-01), CRUD de Negocios con config WhatsApp no-secreta dentro de cada tenant (SADMIN-02), listado aislado de RLS vía service_role server-side (SADMIN-03), el flujo de alta con transacción compensatoria (Pattern 3), y el bootstrap del primer superadmin (Pitfall 3) gateado por credenciales humanas.

Purpose: Sin superadmin nadie puede dar de alta tenants/negocios ni vincular WhatsApp; y sin bootstrap, /admin es inalcanzable (no existe ningún rol superadmin en la DB seed).
Output: layout+páginas /admin, dialogs Tenant/Negocio, admin-tenants actions, bootstrap script, verify-admin-tenant-lifecycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-CONTEXT.md
@apps/dashboard/lib/supabase/admin.ts
@scripts/apply-seed.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions superadmin (Tenant/Negocio) con transacción compensatoria + schemas</name>
  <files>apps/dashboard/app/actions/admin-tenants.ts, apps/dashboard/lib/schemas/admin.ts, apps/dashboard/lib/schemas/admin.test.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md Pattern 3 (createTenantWithNegocio verbatim, orden auth.user->tenant->negocio->perfil, rollback) + Pitfall 4 (file-boundary: solo este archivo importa admin.ts)
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-CONTEXT.md D-03/D-04 (Tenant=solo nombre; Negocio=datos generales + WhatsApp no-secreta SIN token)
    - apps/dashboard/lib/supabase/admin.ts (cliente service_role server-only)
  </read_first>
  <behavior>
    - tenantSchema: nombre requerido. negocioSchema(admin): nombre/timezone requeridos; whatsapp_phone_number_id/waba_id/display_phone_number opcionales; NO campo token. Tests: válidos + nombre vacío rechazado + rechaza presencia de un campo token.
    - createTenantWithNegocio: si el insert de negocio o perfil falla, borra el auth.user (y el tenant) creados.
  </behavior>
  <action>
    Crear `lib/schemas/admin.ts` (zod): `tenantSchema` (solo nombre requerido); `negocioAdminSchema` (nombre requerido, direccion/telefono opcionales, timezone IANA requerido, granularidad_min 15|30, y config WhatsApp no-secreta opcional: whatsapp_phone_number_id, waba_id, display_phone_number — SIN ningún campo de token, D-04). Crear `lib/schemas/admin.test.ts` (vitest): tenantSchema acepta nombre válido y rechaza vacío; negocioAdminSchema acepta config no-secreta y NO acepta/ignora cualquier campo token.
    Crear `app/actions/admin-tenants.ts` ('use server') — el ÚNICO archivo que importa lib/supabase/admin.ts (convención de file-boundary). Implementar per Pattern 3: `createTenantWithNegocio` (order: auth.admin.createUser email_confirm -> insert tenant{nombre} -> insert negocio{tenant_id, datos generales, WhatsApp no-secreta} -> insert perfil{id, tenant_id, rol:'owner'}; en cualquier fallo Postgres hacer compensación: deleteUser + delete tenant); `updateTenant` / `deactivateTenant` (update simple de tenant.nombre / tenant.activo); `createNegocio` / `updateNegocio` / `deactivateNegocio` (dentro de un tenant, con WhatsApp no-secreta, sin token). Todas re-validan con los schemas. NUNCA escribir un token real en negocio.whatsapp_token.
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec vitest run lib/schemas/admin.test.ts 2>&1 | tail -8; pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -3; node -e "const a=require('fs').readFileSync('apps/dashboard/app/actions/admin-tenants.ts','utf8'); if(!a.includes('createTenantWithNegocio')||!a.includes('deleteUser')){console.error('falta compensating tx');process.exit(1)} if(/whatsapp_token\s*[:=]\s*[^n]/.test(a)){console.error('escribe token real');process.exit(1)} console.log('admin actions OK')"</automated>
  </verify>
  <acceptance_criteria>
    admin.test.ts pasa; admin-tenants.ts es el único importador de admin.ts, implementa createTenantWithNegocio con rollback (deleteUser + delete tenant) y las actions de update/deactivate de Tenant y Negocio; ningún token real se escribe; tsc pasa.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Panel /admin — shell jerárquico + CRUD Tenant + CRUD Negocio</name>
  <files>apps/dashboard/app/(admin)/admin/layout.tsx, apps/dashboard/app/(admin)/admin/page.tsx, apps/dashboard/app/(admin)/admin/[tenantId]/page.tsx, apps/dashboard/components/admin/tenant-dialog.tsx, apps/dashboard/components/admin/negocio-dialog.tsx</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Superadmin shell jerárquico Grupos->Negocios; CRUD pattern Tenant=Dialog solo nombre, Negocio=Dialog datos generales+WhatsApp sin token; copy "+ Nuevo grupo"/"+ Nueva peluquería"; empty states y destructivos exactos; soft-delete Tabs/Switch)
    - apps/dashboard/app/actions/admin-tenants.ts
  </read_first>
  <action>
    Crear `app/(admin)/admin/layout.tsx`: shell superadmin separado (mismos tokens de diseño, distinguido solo por ruta/contenido) con navegación jerárquica Grupos -> Negocios. Crear `app/(admin)/admin/page.tsx`: lista de Tenants (Grupos) — server component que lee vía las actions service_role (SADMIN-03, aislado de RLS), CTA "+ Nuevo grupo" (único acento), empty state "Todavía no hay grupos registrados" + body exacto, Tabs/Switch soft-delete con AlertDialog "¿Desactivar el grupo {tenant}? ..." exacto; cada fila enlaza a /admin/[tenantId]. Crear `app/(admin)/admin/[tenantId]/page.tsx`: dentro de un grupo, breadcrumb/back a Grupos, lista de Negocios del tenant, CTA "+ Nueva peluquería", empty state "Este grupo todavía no tiene peluquerías" + body exacto, soft-delete con AlertDialog "¿Desactivar {negocio}? ..." exacto. Crear `components/admin/tenant-dialog.tsx` (Dialog con un único campo nombre, crear/editar) y `components/admin/negocio-dialog.tsx` (Dialog con secciones "Datos generales" nombre/dirección/teléfono/timezone/granularidad y "WhatsApp" phone_number_id/waba_id/número visible — SIN campo token, D-04). Todos los dialogs usan react-hook-form + zodResolver con los schemas admin.
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const p=require('fs').readFileSync('apps/dashboard/app/(admin)/admin/page.tsx','utf8'); if(!p.includes('+ Nuevo grupo')){console.error('falta CTA grupo');process.exit(1)} const n=require('fs').readFileSync('apps/dashboard/app/(admin)/admin/[tenantId]/page.tsx','utf8'); if(!n.includes('+ Nueva peluquería')){console.error('falta CTA negocio');process.exit(1)} const nd=require('fs').readFileSync('apps/dashboard/components/admin/negocio-dialog.tsx','utf8'); if(/token/i.test(nd)){console.error('el dialog de negocio tiene campo token');process.exit(1)} console.log('admin UI OK')"</automated>
  </verify>
  <acceptance_criteria>
    /admin lista Tenants con "+ Nuevo grupo" y navega a /admin/[tenantId] que lista Negocios con "+ Nueva peluquería"; los dialogs de Tenant (solo nombre) y Negocio (datos generales + WhatsApp sin token) validan con zod; copy de empty states y destructivos exactos; el dialog de negocio NO tiene campo token; tsc pasa.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [checkpoint:human-action] Ejecutar bootstrap-superadmin + verify-admin-tenant-lifecycle</name>
  <action>Gate humano: escribir scripts/bootstrap-superadmin.ts y scripts/verify-admin-tenant-lifecycle.ts, luego ejecutarlos con credenciales reales (email/password del primer superadmin provistos por env/prompt, nunca hardcodeados). Ver how-to-verify.</action>
  <what-built>El script `scripts/bootstrap-superadmin.ts` (mirroreando scripts/apply-seed.ts: guard de aislamiento a bdgufnitakelyialjoqg, service_role, auth.admin.createUser + perfil rol=superadmin tenant_id=NULL) y `scripts/verify-admin-tenant-lifecycle.ts` (prueba SADMIN-01/02/03 incluyendo el rollback de Pattern 3, y que un owner nunca alcanza /admin) están escritos. NO se ejecutaron: crear el primer superadmin requiere credenciales reales que solo el humano provee.</what-built>
  <how-to-verify>
    Con `.env` local poblado (SUPABASE_URL de bdgufnitakelyialjoqg, SUPABASE_SERVICE_ROLE_KEY):
    1. Proveer/confirmar el email y contraseña del primer superadmin (NO hardcodearlos en un archivo commiteado; pasarlos por env/prompt al script).
    2. Ejecutar `pnpm exec tsx scripts/bootstrap-superadmin.ts` y confirmar que crea el auth.user + un `perfil` con rol='superadmin' y tenant_id=NULL.
    3. Ejecutar `pnpm exec tsx scripts/verify-admin-tenant-lifecycle.ts` y confirmar: (a) createTenantWithNegocio crea Tenant+owner+Negocio; (b) un fallo simulado en el insert de negocio/perfil deja CERO auth.users huérfanos (rollback); (c) el owner recién creado NO puede alcanzar /admin (gate de rol).
    4. Loguear en el dashboard como el superadmin y confirmar que ve /admin y un owner no.
    Pega la salida de ambos scripts como evidencia antes de aprobar.
  </how-to-verify>
  <resume-signal>Escribí "bootstrap ok" cuando el primer superadmin exista en bdgufnitakelyialjoqg y verify-admin-tenant-lifecycle pase; o describí el error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador owner -> /admin | Owner NUNCA debe entrar; gate de rol en middleware |
| /admin Server Action -> service_role | Bypassa RLS por diseño; solo server-side, un único importador |
| auth.users (GoTrue) + Postgres | Dos sistemas sin transacción compartida en el alta de tenant |
| humano -> DB live (bootstrap) | Creación del primer superadmin con credenciales reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21 | Elevation of Privilege | Owner alcanza /admin | high | mitigate | Gate de rol en middleware (getUser + perfil.rol live); service_role nunca en cliente |
| T-02-22 | Data integrity / DoS | auth.users huérfano tras alta de tenant fallida | high | mitigate | Transacción compensatoria de Pattern 3: deleteUser + delete tenant si falla cualquier insert Postgres; verify-admin-tenant-lifecycle prueba el rollback |
| T-02-23 | Information Disclosure | service_role filtrado / usado en ruta owner | high | mitigate | import 'server-only' en admin.ts (heredado de 02-03); admin-tenants.ts único importador (Pitfall 4) |
| T-02-24 | Tampering | Se escribe un token WhatsApp real en plano | high | mitigate | Dialog y schema de Negocio sin campo token; check automatizado que falla si el código escribe whatsapp_token con valor no-null (D-04/SEC-01 -> Fase 7) |
| T-02-25 | Elevation of Privilege | Bootstrap con credenciales hardcodeadas commiteadas | medium | mitigate | checkpoint:human-action provee las credenciales por env/prompt; nunca en un archivo del repo |
</threat_model>

<verification>
- vitest: admin.test.ts pasa; el dialog/schema de Negocio no tiene campo token.
- tsc pasa; admin-tenants.ts único importador de admin.ts, con rollback.
- bootstrap + verify-admin-tenant-lifecycle ejecutados por el humano en el checkpoint (evidencia: salida de scripts).
</verification>

<success_criteria>
El superadmin gestiona Grupos (Tenants) y sus Peluquerías (Negocios) con WhatsApp no-secreta desde /admin aislado; el alta es atómica-por-compensación; un owner nunca alcanza /admin; existe el primer superadmin en bdgufnitakelyialjoqg.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-y-datos-del-negocio/02-08-SUMMARY.md` when done
</output>
