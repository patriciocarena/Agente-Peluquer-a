---
phase: 02-dashboard-y-datos-del-negocio
plan: 07
type: execute
wave: 5
depends_on: ["02-05", "02-06"]
files_modified:
  - apps/dashboard/app/(owner)/profesionales/[id]/editar/page.tsx
  - apps/dashboard/components/horario-editor.tsx
  - apps/dashboard/components/servicios-matrix.tsx
  - apps/dashboard/app/actions/profesionales.ts
  - apps/dashboard/lib/schemas/horario.ts
  - apps/dashboard/lib/schemas/horario.test.ts
autonomous: true
requirements: [PRO-02, PRO-03, PRO-04]
must_haves:
  truths:
    - "El owner define el horario semanal recurrente de un profesional con múltiples bloques por día y un atajo 'copiar a todos los días'"
    - "El owner asigna a cada profesional qué servicios realiza (checkbox) y opcionalmente un precio custom que pisa el precio base"
    - "Un día sin bloques se muestra como 'Cerrado'; horas se editan en formato 24h HH:mm"
  artifacts:
    - "apps/dashboard/app/(owner)/profesionales/[id]/editar/page.tsx"
    - "apps/dashboard/components/{horario-editor,servicios-matrix}.tsx"
    - "apps/dashboard/lib/schemas/horario.ts (+ test)"
  key_links:
    - "horario-editor -> horario_trabajo (una fila por bloque, dia_semana+hora_inicio+hora_fin) del profesional"
    - "servicios-matrix -> profesional_servicio (checkbox 'realiza' + precio_custom que pisa servicio.precio)"
---

<objective>
Completar el editor del profesional con el horario semanal recurrente multi-bloque (PRO-02) y la matriz de servicios con precio custom (PRO-03/04), integrados en la página de edición full-page que 02-06 dejó preparada. Incluye validación de solapamiento/orden de horas.

Purpose: Sin horario y sin servicios asignados, el motor de disponibilidad (Fase 3) no tiene sobre qué razonar; esta es la carga de datos operativa más rica de la fase.
Output: página de edición, horario-editor, servicios-matrix, actions de horario/matriz, schema de horario con test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md
@apps/dashboard/components/profesional-form.tsx
@apps/dashboard/app/actions/profesionales.ts
@apps/dashboard/lib/negocio-context.ts
@packages/db-types/src/database.types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema de horario + test (validación de bloques)</name>
  <files>apps/dashboard/lib/schemas/horario.ts, apps/dashboard/lib/schemas/horario.test.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Weekly Schedule Editor: 7 días, múltiples bloques HH:mm, día vacío = "Cerrado")
    - packages/db-types/src/database.types.ts (shape de horario_trabajo: dia_semana, hora_inicio, hora_fin)
  </read_first>
  <behavior>
    - horarioSchema rechaza: hora_fin <= hora_inicio en un bloque; bloques solapados dentro del mismo día. Acepta: día con 0 bloques (Cerrado), múltiples bloques disjuntos ordenados.
  </behavior>
  <action>
    Crear `lib/schemas/horario.ts` (zod): una estructura de 7 días, cada uno con una lista de bloques {hora_inicio, hora_fin} en formato HH:mm; validar que en cada bloque hora_fin > hora_inicio y que los bloques de un mismo día no se solapan. Exponer un helper puro que verifique solapamiento para reusar en el editor. Crear `lib/schemas/horario.test.ts` (vitest): rechaza hora_fin<=hora_inicio, rechaza bloques solapados (ej: 09:00-13:00 y 12:00-14:00), acepta 09:00-13:00 y 16:00-20:00, acepta día vacío. Test primero (RED), luego implementación (GREEN).
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec vitest run lib/schemas/horario.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    horario.test.ts pasa; el schema rechaza hora_fin<=hora_inicio y bloques solapados, y acepta múltiples bloques disjuntos y días vacíos.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Editor de horario semanal (PRO-02) + matriz de servicios (PRO-03/04) + página de edición</name>
  <files>apps/dashboard/app/(owner)/profesionales/[id]/editar/page.tsx, apps/dashboard/components/horario-editor.tsx, apps/dashboard/components/servicios-matrix.tsx</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Weekly Schedule Editor y Services + Custom Price Matrix — specs completas; aria-label="Quitar bloque horario"; "Copiar a todos los días" con undo toast Sonner 5s; step según granularidad BIZ-03; Formatting es-AR de precio)
    - apps/dashboard/components/profesional-form.tsx (secciones preparadas en 02-06)
    - apps/dashboard/lib/schemas/horario.ts
    - packages/db-types (horario_trabajo, profesional_servicio, servicio)
  </read_first>
  <action>
    Crear `components/horario-editor.tsx` (client) per la UI-SPEC: 7 filas (Lunes–Domingo, siempre visibles, nombres completos en español), cada fila con una lista horizontal de bloques [input time]–[input time][× quitar] (aria-label="Quitar bloque horario"), botón ghost "+ Agregar bloque", botón secundario "Copiar a todos los días" (copia los bloques del primer día configurado a los otros 6, con undo toast Sonner de 5s), inputs `<input type="time">` estilados vía Input con step según la granularidad del negocio activo (BIZ-03), y un día sin bloques renderiza "Cerrado" (12px, muted, itálica). Validar solapamiento con el helper de horario.ts antes de permitir guardar.
    Crear `components/servicios-matrix.tsx` (client) per la UI-SPEC: Table con una fila por servicio ACTIVO del negocio (leídos de la tabla servicio), columnas [checkbox "Realiza"] | Servicio | Precio base (solo lectura, muted, formato es-AR) | Precio personalizado (Input, deshabilitado salvo que "Realiza" esté tildado, placeholder = precio base). Destildar "Realiza" limpia el precio custom de ese servicio al guardar (sin confirmación).
    Crear `app/(owner)/profesionales/[id]/editar/page.tsx`: full-page que carga el profesional (RLS-scoped al negocio activo), renderiza el profesional-form (datos generales de 02-06) + el horario-editor + la servicios-matrix como secciones separadas (32px), con "Guardar cambios".
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const h=require('fs').readFileSync('apps/dashboard/components/horario-editor.tsx','utf8'); for(const s of ['Quitar bloque horario','Copiar a todos los días','Agregar bloque','Cerrado']){if(!h.includes(s)){console.error('falta',s);process.exit(1)}} const m=require('fs').readFileSync('apps/dashboard/components/servicios-matrix.tsx','utf8'); if(!m.includes('Realiza')){console.error('matriz sin Realiza');process.exit(1)} console.log('editor+matriz OK')"</automated>
  </verify>
  <acceptance_criteria>
    El horario-editor tiene 7 días, múltiples bloques por día, "+ Agregar bloque", "Copiar a todos los días" con undo toast, inputs time con step por granularidad, y "Cerrado" para día vacío; la matriz lista servicios activos con checkbox "Realiza", precio base solo-lectura y precio custom editable solo si realiza; la página de edición integra las tres secciones; tsc pasa.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Server Actions de horario y matriz de servicios</name>
  <files>apps/dashboard/app/actions/profesionales.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md (anti-pattern: derivar negocio_id server-side; nunca confiar campos ocultos)
    - apps/dashboard/lib/schemas/horario.ts, apps/dashboard/lib/negocio-context.ts
    - packages/db-types (horario_trabajo, profesional_servicio)
  </read_first>
  <action>
    Extender `app/actions/profesionales.ts` con: `updateHorario(profesionalId, dias)` — re-valida con horarioSchema server-side, verifica que el profesional pertenece al negocio activo (RLS + chequeo explícito), y reemplaza atómicamente las filas de horario_trabajo del profesional (delete+insert de las filas por bloque, una fila por {dia_semana, hora_inicio, hora_fin}); y `updateServiciosMatrix(profesionalId, asignaciones)` — persiste profesional_servicio (upsert de las filas donde "realiza" está tildado con su precio_custom nullable; borra las filas de servicios destildados), todo RLS-scoped al negocio activo. Ambas derivan/validan el negocio_id server-side, nunca desde el cliente, y revalidan el path. No permitir asignar un servicio de otro negocio (chequear que servicio.negocio_id == negocio activo).
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const a=require('fs').readFileSync('apps/dashboard/app/actions/profesionales.ts','utf8'); for(const f of ['updateHorario','updateServiciosMatrix','horarioSchema']){if(!a.includes(f)){console.error('falta',f);process.exit(1)}} console.log('actions horario/matriz OK')"</automated>
  </verify>
  <acceptance_criteria>
    updateHorario re-valida con horarioSchema y reemplaza las filas de horario_trabajo del profesional (una por bloque); updateServiciosMatrix upserta/borra profesional_servicio con precio_custom que pisa el base; ambas RLS-scoped al negocio activo, rechazan servicios/profesionales de otro negocio; tsc pasa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (form) -> Server Action | Input de horario/matriz; negocio_id y pertenencia validados server-side |
| owner JWT -> horario_trabajo/profesional_servicio (RLS por negocio) | Solo filas de negocios del propio tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Tampering | Owner asigna a un profesional un servicio de otro negocio, o edita el horario de un profesional ajeno | high | mitigate | Chequeo explícito de que profesional y servicio pertenecen al negocio activo + RLS por auth_negocio_ids(); ids ajenos no matchean |
| T-02-19 | Tampering | Bloques de horario solapados o hora_fin<=hora_inicio | low | mitigate | horarioSchema re-validado server-side (solapamiento + orden de horas) |
| T-02-20 | Tampering | precio_custom negativo | low | mitigate | Validación server-side del precio_custom (>=0) en updateServiciosMatrix |
</threat_model>

<verification>
- vitest: horario.test.ts pasa.
- tsc pasa; editor de horario multi-bloque + matriz con precio custom; actions re-validan y RLS-scoped.
</verification>

<success_criteria>
El owner define horarios semanales multi-bloque y asigna servicios (con precio custom opcional) a cada profesional del negocio activo.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-y-datos-del-negocio/02-07-SUMMARY.md` when done
</output>
