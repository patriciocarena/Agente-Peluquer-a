---
phase: 02-dashboard-y-datos-del-negocio
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - apps/dashboard/app/(owner)/layout.tsx
  - apps/dashboard/components/owner-sidebar.tsx
  - apps/dashboard/components/negocio-selector.tsx
  - apps/dashboard/components/user-menu.tsx
  - apps/dashboard/lib/negocio-context.ts
  - apps/dashboard/app/(owner)/negocio/page.tsx
  - apps/dashboard/app/actions/negocio.ts
  - apps/dashboard/lib/schemas/negocio.ts
  - apps/dashboard/lib/schemas/negocio.test.ts
autonomous: true
requirements: [BIZ-01, BIZ-02, BIZ-03]
must_haves:
  truths:
    - "El owner ve un shell con sidebar (Profesionales/Servicios/Negocio) y un selector de negocio en el topbar que fija el negocio activo de la sesión"
    - "Si el tenant tiene un solo negocio, el selector colapsa a una etiqueta fija sin dejar de persistir el negocio_id activo"
    - "El owner edita nombre, dirección, teléfono, horario general, timezone y granularidad (15/30) del negocio seleccionado, y ve su WhatsApp vinculado"
    - "El owner puede cerrar sesión desde el user-menu en cualquier página"
  artifacts:
    - "apps/dashboard/app/(owner)/layout.tsx + components/{owner-sidebar,negocio-selector,user-menu}.tsx"
    - "apps/dashboard/lib/negocio-context.ts"
    - "apps/dashboard/app/(owner)/negocio/page.tsx + app/actions/negocio.ts + lib/schemas/negocio.ts"
  key_links:
    - "negocio-selector -> negocio-context (cookie/URL) -> negocio_id activo consumido por todo el CRUD operativo"
    - "perfil.tenant_id -> negocios del tenant (RLS por auth_tenant_id()) -> opciones del selector"
---

<objective>
Construir el shell del owner (sidebar + topbar con user-menu y logout) y el selector de negocio (D-13) que fija el `negocio_id` activo persistido, y la página de Perfil del negocio (BIZ-01/02/03) que opera sobre ese negocio. El contexto de negocio activo es la base que todo el CRUD operativo (Servicios, Profesionales) consumirá.

Purpose: Dar al owner un contenedor de navegación y el mecanismo de "negocio activo" sobre el que se agrupa todo su trabajo, más el CRUD de perfil del negocio.
Output: layout owner, sidebar, selector de negocio + contexto, user-menu con logout, página y action de perfil del negocio.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-CONTEXT.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md
@apps/dashboard/lib/supabase/server.ts
@apps/dashboard/lib/auth/require-role.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owner shell (layout + sidebar + user-menu con logout) + selector de negocio + contexto</name>
  <files>apps/dashboard/app/(owner)/layout.tsx, apps/dashboard/components/owner-sidebar.tsx, apps/dashboard/components/negocio-selector.tsx, apps/dashboard/components/user-menu.tsx, apps/dashboard/lib/negocio-context.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Layout & Navigation: sidebar 240px, secciones, selector de negocio en topbar, responsive; user-menu: theme toggle + "Cerrar sesión")
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-CONTEXT.md D-13 (selector persistido en cookie/URL; colapsa a etiqueta si 1 negocio)
    - apps/dashboard/app/actions/auth.ts (signOut para el user-menu)
  </read_first>
  <action>
    Crear `app/(owner)/layout.tsx`: el shell con sidebar fijo (usar el bloque shadcn `sidebar`, 240px, tokens --sidebar) con las tres secciones Profesionales / Servicios / Negocio (rutas separadas App Router), item activo en acento, densidad cómoda (16px). Topbar con `negocio-selector` a la izquierda y `user-menu` a la derecha. Responsive: rail icon-only <768px, Sheet off-canvas <640px.
    Crear `lib/negocio-context.ts`: helper server-side que resuelve el `negocio_id` activo desde cookie (o URL param) y lo persiste; provee la lista de negocios del tenant del owner (query RLS-scoped por auth_tenant_id()) y el negocio activo; si el owner tiene un solo negocio, fija ese id automáticamente. Este helper es la ÚNICA fuente del negocio activo que consumirán Servicios/Profesionales/Perfil (no confiar en un campo de form del cliente para el negocio_id).
    Crear `components/negocio-selector.tsx`: dropdown (Select/DropdownMenu) que lista los negocios del tenant y setea la cookie/URL del negocio activo al cambiar; si hay un solo negocio, renderiza una etiqueta fija con negocio.nombre (sin dropdown interactivo) pero el concepto/persistencia siguen existiendo.
    Crear `components/user-menu.tsx`: avatar + dropdown con el toggle de tema (Sun/Moon, aria-label="Cambiar tema") y "Cerrar sesión" (invoca la Server Action signOut, sin confirmación).
    Todo icon-only control lleva aria-label + focus ring de acento (MANDATORY de la UI-SPEC).
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const u=require('fs').readFileSync('apps/dashboard/components/user-menu.tsx','utf8'); if(!u.includes('Cerrar sesión')||!u.includes('aria-label')){console.error('user-menu incompleto');process.exit(1)} const s=require('fs').readFileSync('apps/dashboard/components/negocio-selector.tsx','utf8'); require('fs').accessSync('apps/dashboard/lib/negocio-context.ts'); console.log('shell OK')"</automated>
  </verify>
  <acceptance_criteria>
    El shell owner tiene sidebar con las tres secciones, selector de negocio en el topbar (colapsa a etiqueta con 1 negocio, persiste en cookie/URL), y user-menu con theme toggle + "Cerrar sesión"; negocio-context provee el negocio activo server-side desde perfil.tenant_id; todos los controles icon-only tienen aria-label; tsc pasa.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Perfil del negocio — página + Server Action + schema (BIZ-01/02/03)</name>
  <files>apps/dashboard/app/(owner)/negocio/page.tsx, apps/dashboard/app/actions/negocio.ts, apps/dashboard/lib/schemas/negocio.ts, apps/dashboard/lib/schemas/negocio.test.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (CRUD pattern: Perfil = settings page inline + "Guardar cambios"; Formatting es-AR; granularidad 15/30 BIZ-03; WhatsApp visible BIZ-02)
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md (zod schema compartido; anti-pattern: nunca confiar negocio_id de un campo oculto)
    - apps/dashboard/lib/negocio-context.ts (negocio activo)
    - packages/db-types/src/database.types.ts (shape de negocio post-0003: display_phone_number, granularidad_min, timezone, etc.)
  </read_first>
  <behavior>
    - negocioSchema rechaza: nombre vacío, granularidad_min fuera de {15,30}, timezone vacío. Test: casos válidos e inválidos.
    - updateNegocio opera SIEMPRE sobre el negocio activo del contexto server-side, nunca sobre un id enviado por el cliente.
  </behavior>
  <action>
    Crear `lib/schemas/negocio.ts` (zod: nombre requerido, direccion/telefono opcionales, timezone IANA no vacío, granularidad_min enum 15|30, horario_general). Crear `lib/schemas/negocio.test.ts` (vitest, unit): afirma que el schema acepta un perfil válido y rechaza granularidad fuera de {15,30}, nombre vacío y timezone vacío. Crear `app/(owner)/negocio/page.tsx`: settings page inline (react-hook-form + zodResolver) que carga el negocio ACTIVO (desde negocio-context) y permite editar nombre, dirección, teléfono, horario general, timezone y granularidad (Select 15/30), con CTA "Guardar cambios" (acento). Mostrar el WhatsApp vinculado (BIZ-02) leyendo `negocio.display_phone_number` del negocio activo, en modo solo-lectura (la config de WhatsApp la gestiona el superadmin, no el owner). Crear `app/actions/negocio.ts` con `updateNegocio`: re-valida con negocioSchema server-side, deriva el negocio_id del contexto server-side (NO de un campo del form), hace update RLS-scoped, y revalida el path. Aplicar formato es-AR donde corresponda.
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec vitest run lib/schemas/negocio.test.ts 2>&1 | tail -8; pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    negocio.test.ts pasa (acepta válido, rechaza granularidad!=15/30, nombre vacío, timezone vacío); la página edita los campos de perfil del negocio activo con "Guardar cambios" y muestra el display_phone_number en solo-lectura; updateNegocio deriva el negocio_id del contexto server-side y re-valida con zod; tsc pasa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (form) -> Server Action | Input de perfil; negocio_id NO debe venir del cliente |
| owner JWT -> negocio (RLS) | Solo negocios del propio tenant son visibles/editables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | Owner manipula el negocio_id activo (cookie/URL) para editar un negocio de otro tenant | high | mitigate | negocio-context valida que el negocio_id activo pertenece a un negocio del tenant del owner (RLS por auth_tenant_id()); el update va RLS-scoped, un id ajeno no matchea ninguna fila |
| T-02-11 | Tampering | Campo oculto negocio_id en el form de perfil | medium | mitigate | El Server Action deriva negocio_id del contexto server-side, nunca del form (anti-pattern de RESEARCH.md); re-validación zod server-side |
| T-02-12 | Information Disclosure | Owner intenta editar la config WhatsApp del negocio | low | mitigate | display_phone_number es solo-lectura para el owner; la escritura de config WhatsApp es exclusiva del panel superadmin |
</threat_model>

<verification>
- negocio.test.ts (vitest) pasa.
- tsc pasa; user-menu con logout y aria-labels; selector persiste el negocio activo.
</verification>

<success_criteria>
El owner navega el shell, elige negocio activo, edita el perfil de ese negocio (incluida granularidad 15/30), ve su WhatsApp vinculado, y cierra sesión desde cualquier página.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-y-datos-del-negocio/02-04-SUMMARY.md` when done
</output>
