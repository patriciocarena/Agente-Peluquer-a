---
phase: 02-dashboard-y-datos-del-negocio
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - apps/dashboard/app/(owner)/servicios/page.tsx
  - apps/dashboard/components/servicio-dialog.tsx
  - apps/dashboard/components/servicios-table.tsx
  - apps/dashboard/app/actions/servicios.ts
  - apps/dashboard/lib/schemas/servicio.ts
  - apps/dashboard/lib/schemas/servicio.test.ts
  - apps/dashboard/lib/reorder.ts
  - apps/dashboard/lib/reorder.test.ts
autonomous: true
requirements: [SVC-01, SVC-02]
must_haves:
  truths:
    - "El owner crea, edita, desactiva/reactiva y lista servicios (nombre, descripción, precio, duración) del negocio activo"
    - "El owner reordena servicios con drag-and-drop y el nuevo orden persiste en servicio.orden"
    - "Los servicios inactivos permanecen en la tabla, muteados, con badge gris 'Inactivo', filtrables por Tabs Todos/Activos/Inactivos"
  artifacts:
    - "apps/dashboard/app/(owner)/servicios/page.tsx + components/{servicio-dialog,servicios-table}.tsx"
    - "apps/dashboard/app/actions/servicios.ts + lib/schemas/servicio.ts + lib/reorder.ts"
  key_links:
    - "negocio-context (negocio activo) -> servicios.action derivan negocio_id server-side"
    - "@dnd-kit onDragEnd -> reorder() -> reorderServiciosAction -> servicio.orden"
---

<objective>
CRUD completo de servicios (SVC-01) con soft-delete y ordenamiento drag-and-drop (SVC-02), operando sobre el negocio activo. Incluye el `reorder()` puro con test unitario (garantiza orden sin huecos/duplicados) y el schema zod de servicio con test.

Purpose: El owner carga el catálogo de servicios (nombre/precio/duración) que luego consumen los profesionales y el motor de disponibilidad; el orden define la presentación al cliente del bot.
Output: página de servicios, dialog crear/editar, tabla con Tabs+Switch+dnd, Server Actions, schema y reorder con tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md
@apps/dashboard/lib/negocio-context.ts
@apps/dashboard/lib/schemas/negocio.ts
@packages/db-types/src/database.types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema servicio + reorder puro (con tests unitarios)</name>
  <files>apps/dashboard/lib/schemas/servicio.ts, apps/dashboard/lib/schemas/servicio.test.ts, apps/dashboard/lib/reorder.ts, apps/dashboard/lib/reorder.test.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md (zod compartido; ejemplo @dnd-kit arrayMove; Validation Architecture: test de reorder "sin huecos/dupes")
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Servicio: 4 campos nombre/descripción/precio/duración)
  </read_first>
  <behavior>
    - servicioSchema rechaza: nombre vacío, precio negativo, duracion_min <= 0 o no entera. Acepta descripción opcional.
    - reorder(items, fromId, toId): devuelve el array reordenado con `orden` reasignado 0..n-1 sin huecos ni duplicados. Test: mover primero->último, último->primero, no-op cuando from==to.
  </behavior>
  <action>
    Crear `lib/schemas/servicio.ts` (zod: nombre requerido, descripcion opcional, precio numérico >= 0, duracion_min entero > 0). Crear `lib/reorder.ts`: función pura `reorder` que dado el array actual y un evento de drag (fromId/toId) usa arrayMove y reasigna `orden` como índice contiguo 0..n-1 (representación de almacenamiento). Crear los tests vitest `servicio.test.ts` (válido/ inválidos: precio negativo, duración 0, nombre vacío) y `reorder.test.ts` (afirma no hay huecos ni duplicados en `orden` tras varios movimientos, y no-op si from==to). Estos tests deben escribirse ANTES de la implementación y fallar primero (RED), luego pasar (GREEN).
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec vitest run lib/schemas/servicio.test.ts lib/reorder.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    servicio.test.ts y reorder.test.ts pasan; reorder reasigna orden contiguo sin huecos/duplicados; el schema rechaza precio negativo y duración no positiva.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Página de servicios + tabla (Tabs/Switch/soft-delete) + dialog crear/editar</name>
  <files>apps/dashboard/app/(owner)/servicios/page.tsx, apps/dashboard/components/servicios-table.tsx, apps/dashboard/components/servicio-dialog.tsx</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Servicio = Dialog; Soft-Delete: Tabs Todos/Activos/Inactivos, Switch en fila, AlertDialog destructivo con copy exacto; SVC-02 drag-handle GripVertical aria-label="Reordenar servicio"; empty states y CTA "+ Nuevo servicio"; Formatting es-AR de precio/duración)
    - apps/dashboard/lib/reorder.ts, apps/dashboard/lib/schemas/servicio.ts
  </read_first>
  <action>
    Crear `app/(owner)/servicios/page.tsx`: lista los servicios del negocio activo (server component read RLS-scoped), header con CTA "+ Nuevo servicio" (único acento de la página), empty state con el copy exacto de la UI-SPEC. Crear `components/servicios-table.tsx` (client): Table con Tabs "Todos/Activos/Inactivos", filas con Switch de activo/inactivo (apagar abre el AlertDialog destructivo con el copy exacto "¿Desactivar {servicio}? ..."; encender reactiva sin confirmación), filas inactivas muteadas (~60% opacidad) con badge gris "Inactivo" (NO rojo), precio formateado es-AR y duración en minutos, y drag-and-drop con @dnd-kit (handle GripVertical, aria-label="Reordenar servicio", keyboard sensor) que en onDragEnd llama reorder() y persiste vía la action (UI optimista + toast de rollback en fallo). Crear `components/servicio-dialog.tsx`: Dialog modal crear/editar (react-hook-form + zodResolver con servicioSchema) con los 4 campos.
    NO usar fences de código en descripciones. NOTA: no incluir literalmente la cadena "Inactivo" como texto de comentario en el código de forma que un grep negativo pudiera confundirse; el badge la usa como contenido visible legítimamente.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const t=require('fs').readFileSync('apps/dashboard/components/servicios-table.tsx','utf8'); for(const s of ['Reordenar servicio','GripVertical','Tabs']){if(!t.includes(s)){console.error('falta',s);process.exit(1)}} const p=require('fs').readFileSync('apps/dashboard/app/(owner)/servicios/page.tsx','utf8'); if(!p.includes('+ Nuevo servicio')){console.error('falta CTA');process.exit(1)} console.log('servicios UI OK')"</automated>
  </verify>
  <acceptance_criteria>
    La página lista servicios del negocio activo con CTA "+ Nuevo servicio" y empty state exacto; la tabla tiene Tabs Todos/Activos/Inactivos, Switch con AlertDialog destructivo al desactivar, badge gris "Inactivo", y drag-handle accesible (aria-label="Reordenar servicio"); el dialog crea/edita con los 4 campos validados; tsc pasa.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Server Actions de servicios (crear/editar/toggle/reorder)</name>
  <files>apps/dashboard/app/actions/servicios.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md (anti-pattern: derivar negocio_id server-side, nunca de un campo del cliente; reorderServiciosAction rollback en fallo)
    - apps/dashboard/lib/negocio-context.ts, apps/dashboard/lib/schemas/servicio.ts
  </read_first>
  <action>
    Crear `app/actions/servicios.ts` con las Server Actions: `createServicio` / `updateServicio` (re-validan con servicioSchema server-side; derivan negocio_id del contexto server-side, nunca de un campo del form; insert/update RLS-scoped; al crear, asignar orden = siguiente índice), `toggleServicioActivo` (setea activo true/false), y `reorderServicios` (recibe la lista [{id, orden}] y persiste servicio.orden en batch, todo RLS-scoped al negocio activo). Todas revalidan el path tras mutar. Ninguna confía en un negocio_id/tenant_id enviado por el cliente.
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>&1 | tail -5; node -e "const a=require('fs').readFileSync('apps/dashboard/app/actions/servicios.ts','utf8'); for(const f of ['createServicio','updateServicio','toggleServicioActivo','reorderServicios']){if(!a.includes(f)){console.error('falta',f);process.exit(1)}} if(!a.includes('servicioSchema')){console.error('no re-valida con zod');process.exit(1)} console.log('actions OK')"</automated>
  </verify>
  <acceptance_criteria>
    Las cuatro actions existen, re-validan con servicioSchema, derivan negocio_id server-side, y persisten RLS-scoped; reorderServicios actualiza servicio.orden en batch; tsc pasa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (form/dnd) -> Server Action | Input de servicio y orden; negocio_id derivado server-side |
| owner JWT -> servicio (RLS por negocio) | Solo servicios de negocios del propio tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Tampering | Owner envía negocio_id ajeno al crear/reordenar servicio | high | mitigate | negocio_id derivado del contexto server-side + RLS por auth_negocio_ids(); un id ajeno no matchea ninguna fila del tenant |
| T-02-14 | Tampering | precio negativo / duración inválida | low | mitigate | servicioSchema re-validado server-side (precio>=0, duracion_min>0 entero) |
| T-02-15 | Denial of Service | reorder deja huecos/duplicados corrompiendo la presentación | low | mitigate | reorder() puro con test unitario que garantiza orden contiguo; persistencia en batch RLS-scoped |
</threat_model>

<verification>
- vitest: servicio.test.ts + reorder.test.ts pasan.
- tsc pasa; UI con Tabs/Switch/dnd accesible; actions re-validan y derivan negocio_id server-side.
</verification>

<success_criteria>
El owner gestiona el catálogo de servicios del negocio activo con soft-delete y reordenamiento drag-and-drop persistente.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-y-datos-del-negocio/02-05-SUMMARY.md` when done
</output>
