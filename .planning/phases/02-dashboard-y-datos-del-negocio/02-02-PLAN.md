---
phase: 02-dashboard-y-datos-del-negocio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/package.json
  - apps/dashboard/components.json
  - apps/dashboard/postcss.config.mjs
  - apps/dashboard/app/globals.css
  - apps/dashboard/app/layout.tsx
  - apps/dashboard/components/ui/
  - apps/dashboard/components/theme-provider.tsx
  - apps/dashboard/lib/utils.ts
  - apps/dashboard/vitest.config.ts
  - apps/dashboard/next.config.ts
autonomous: false
requirements: [AUTH-01]
must_haves:
  truths:
    - "El dashboard arranca (next build) con Tailwind v4 + shadcn inicializado y sirve un layout con modo claro/oscuro conmutable"
    - "La paleta es neutra (grises) con un único acento azul aplicado solo a --primary/--ring/--sidebar-primary"
    - "Existe un runner de tests (vitest) configurado y ejecutable para apps/dashboard"
  artifacts:
    - "apps/dashboard/components.json"
    - "apps/dashboard/app/globals.css (tokens neutral + overrides de acento)"
    - "apps/dashboard/app/layout.tsx (ThemeProvider + Inter)"
    - "apps/dashboard/vitest.config.ts"
  key_links:
    - "next-themes ThemeProvider (class strategy) -> Tailwind dark: variant"
    - "shadcn components.json -> components/ui/* -> lib/utils cn()"
---

<objective>
Convertir el stub `apps/dashboard` en una app Next.js 16 con Tailwind v4 + shadcn/ui inicializados, tema claro/oscuro (next-themes), fuente Inter self-hosted, los componentes shadcn del inventario de la UI-SPEC, y un runner de tests (vitest). Es la base visual y de tooling sobre la que se construyen auth y todo el CRUD.

Purpose: Materializar el contrato visual de 02-UI-SPEC.md (D-01/D-02) de forma reproducible y determinística, y crear el andamiaje de tests (Wave 0 de RESEARCH.md) que las fases de CRUD llenarán.
Output: dashboard con shadcn inicializado, globals.css con paleta neutral+acento, ThemeProvider, componentes UI, vitest configurado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-CONTEXT.md
@apps/dashboard/package.json
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: [checkpoint:human-verify] Legitimidad de paquetes npm nuevos</name>
  <action>Gate humano: verificar en npmjs.com cada paquete nuevo (org/repo esperado, sin postinstall sospechoso) antes de instalar, ya que slopcheck no pudo correr; ver how-to-verify.</action>
  <what-built>Se identificaron los paquetes npm nuevos a instalar en apps/dashboard. slopcheck NO pudo correr en el entorno de research (sin pip), así que todos quedaron marcados `[ASSUMED]` y requieren verificación humana antes de instalar (regla de legitimidad de paquetes).</what-built>
  <how-to-verify>
    Para CADA paquete nuevo, verificar en el registro oficial que existe, es el paquete correcto (org/repo esperado) y no fue reemplazado por un typosquat: revisar en npmjs.com/package cada uno: `next-themes` (pacocoursey), `sonner` (emilkowalski), `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` (clauderic/dnd-kit), `react-hook-form` (react-hook-form org), `@hookform/resolvers` (react-hook-form org), `lucide-react` (lucide-icons), `shadcn` (CLI, shadcn-ui/ui), `class-variance-authority` (joe-bell), `tailwind-merge` (dcastil), `clsx` (lukeed), `tailwindcss` + `@tailwindcss/postcss` (tailwindlabs).
    Confirmar además que ninguno declara un `postinstall` sospechoso. Correr `npm view <pkg> version` inmediatamente antes de instalar para fijar la versión actual (la lista de RESEARCH.md es punto de partida, no pin final).
  </how-to-verify>
  <resume-signal>Escribí "aprobado" cuando hayas verificado los paquetes en npmjs.com, o listá cuáles se ven sospechosos para removerlos del set.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Inicializar Tailwind v4 + shadcn + tema + componentes UI</name>
  <files>apps/dashboard/package.json, apps/dashboard/components.json, apps/dashboard/postcss.config.mjs, apps/dashboard/app/globals.css, apps/dashboard/app/layout.tsx, apps/dashboard/components/theme-provider.tsx, apps/dashboard/components/ui/, apps/dashboard/lib/utils.ts, apps/dashboard/next.config.ts</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-UI-SPEC.md (Design System, Color, Typography, Component Inventory — contrato verbatim)
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md (Installation command block; "Don't Hand-Roll" next-themes)
    - apps/dashboard/package.json (deps ya declaradas: next 16.2.10, react 19, @supabase/ssr)
  </read_first>
  <action>
    Inicializar shadcn y Tailwind v4 scoped al dashboard EXACTAMENTE como fija la UI-SPEC: correr `npx shadcn@latest init -t next -b radix --css-variables -c apps/dashboard -y` (template Next, base Radix, CSS-variables, aceptando base color neutral). Instalar los paquetes verificados en el checkpoint anterior (`pnpm add next-themes sonner react-hook-form @hookform/resolvers @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities` en apps/dashboard). Agregar los bloques shadcn del inventario: `npx shadcn@latest add button input label textarea select checkbox switch table tabs dialog alert-dialog card badge separator dropdown-menu sonner skeleton form avatar sidebar`.
    Editar `app/globals.css`: conservar TODOS los tokens neutral de shadcn tal cual (--background, --foreground, --card, --secondary, --muted, --border, --input, --destructive) y sobreescribir SOLO --primary/--primary-foreground/--ring/--sidebar-primary/--sidebar-primary-foreground/--sidebar-ring con el acento azul de la sección Color (light #2563EB, dark #3B82F6) en los bloques :root y .dark. NO agregar tailwind.config.js (Tailwind v4 CSS-first).
    Crear `components/theme-provider.tsx` envolviendo next-themes con estrategia `class` (attribute="class"). Editar `app/layout.tsx`: cargar Inter vía next/font/google (self-hosted, NO CDN), envolver el árbol en el ThemeProvider, y renderizar `<Toaster />` de sonner. Reemplazar/eliminar `app/placeholder.ts`. Confirmar `next.config.ts` mínimo si el init no lo creó.
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec next build 2>&1 | tail -5; test -f apps/dashboard/components.json && test -f apps/dashboard/app/globals.css && node -e "const c=require('fs').readFileSync('apps/dashboard/app/globals.css','utf8'); if(!/--primary/.test(c)||!/\.dark/.test(c)){console.error('faltan tokens de tema');process.exit(1)} console.log('globals OK')" && ls apps/dashboard/components/ui/button.tsx apps/dashboard/components/ui/sidebar.tsx apps/dashboard/components/ui/form.tsx</automated>
  </verify>
  <acceptance_criteria>
    `components.json` existe; `globals.css` tiene tokens neutral con overrides de acento en :root y .dark; existen los componentes del inventario en components/ui/; `app/layout.tsx` carga Inter self-hosted + ThemeProvider + Toaster; `next build` compila sin error; no hay tailwind.config.js ni referencias a cdn.tailwindcss.com.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Configurar vitest (andamiaje de tests Wave 0)</name>
  <files>apps/dashboard/vitest.config.ts, apps/dashboard/package.json</files>
  <read_first>
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md (Validation Architecture — Vitest, comandos de run, Wave 0 gaps)
  </read_first>
  <action>
    Agregar Vitest como devDependency del dashboard y crear `apps/dashboard/vitest.config.ts` (ESM, compatible con `"type": "module"`) apuntando a los tests de `lib/schemas` y `lib/reorder` que las fases de CRUD crearán. Agregar script `"test": "vitest run"` a `apps/dashboard/package.json`. Crear un test trivial de humo (p.ej. `lib/__smoke__.test.ts` que afirme `expect(true).toBe(true)`) SOLO para probar que el runner arranca, y removerlo o dejarlo marcado como placeholder; el objetivo es que `pnpm --filter @turnosbot/dashboard exec vitest run` devuelva verde con al menos un test corriendo. Confirmar que el comando de run documentado en RESEARCH.md funciona.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    `vitest.config.ts` existe; `pnpm --filter @turnosbot/dashboard exec vitest run` ejecuta y pasa; el script `test` está en package.json.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desarrollador -> registro npm | Instalación de dependencias nuevas; superficie de supply-chain |
| navegador -> bundle del dashboard | Código cliente; ninguna credencial debe entrar al bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (next-themes, sonner, @dnd-kit/*, react-hook-form, etc.) | high | mitigate | slopcheck no corrió (sin pip) -> checkpoint:human-verify bloqueante que valida cada paquete en npmjs.com + ausencia de postinstall antes de instalar; `npm view` inmediatamente antes de `pnpm add` |
| T-02-05 | Information Disclosure | Variables NEXT_PUBLIC_* en el bundle | low | accept | En esta fase solo se configuran URL/anon-key públicas (seguras por diseño); el service_role NUNCA se prefija NEXT_PUBLIC_ (enforced en el plan de clientes) |
</threat_model>

<verification>
- `next build` compila; componentes UI presentes; tokens de tema en globals.css.
- vitest corre y pasa.
- Checkpoint de legitimidad de paquetes aprobado antes de cualquier install.
</verification>

<success_criteria>
El dashboard tiene shadcn+Tailwind v4 inicializados, tema claro/oscuro, fuente Inter self-hosted, los componentes del inventario, y un runner de tests operativo — todo compilando.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-y-datos-del-negocio/02-02-SUMMARY.md` when done
</output>
