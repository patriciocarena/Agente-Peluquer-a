---
phase: 02-dashboard-y-datos-del-negocio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0003_tenant_negocio_split.sql
  - scripts/apply-seed.ts
  - scripts/seed-fixtures.ts
  - scripts/verify-migration-0003.ts
  - packages/db-types/src/database.types.ts
autonomous: false
requirements: [SADMIN-01, SADMIN-02, AUTH-03, BIZ-02]
user_setup:
  - service: supabase
    why: "Aplicar la migración 0003 en vivo y regenerar db-types requiere credenciales reales del proyecto bdgufnitakelyialjoqg (no están en el repo)"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard bdgufnitakelyialjoqg -> Project Settings -> API (debe contener 'bdgufnitakelyialjoqg')"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard bdgufnitakelyialjoqg -> Project Settings -> API -> service_role secret"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (PAT para Management API / supabase gen types)"
    dashboard_config:
      - task: "Confirmar que el proyecto objetivo es bdgufnitakelyialjoqg y NUNCA hzgunbftloevclkohcdf (regla dura de aislamiento CLAUDE.md)"
        location: "Supabase Dashboard -> project ref"

must_haves:
  truths:
    - "El schema live de bdgufnitakelyialjoqg refleja el modelo Tenant(nombre) -> Negocio(1..N con WhatsApp) -> tablas operativas por negocio_id"
    - "Un owner autenticado solo alcanza filas operativas cuyo negocio pertenece a su tenant (RLS por auth_negocio_ids())"
    - "Los tipos generados en packages/db-types reflejan el shape post-0003 (tenant sin columnas WhatsApp; negocio con ellas + activo; 11 tablas con negocio_id, sin tenant_id)"
  artifacts:
    - "supabase/migrations/0003_tenant_negocio_split.sql"
    - "scripts/apply-seed.ts (actualizado al nuevo shape)"
    - "scripts/verify-migration-0003.ts (asserta el shape post-0003 en vivo)"
    - "packages/db-types/src/database.types.ts (regenerado)"
  key_links:
    - "auth_negocio_ids() -> negocio.tenant_id = auth_tenant_id() -> RLS de las 11 tablas operativas"
    - "backfill WhatsApp: tenant.* -> negocio.* ANTES de DROP COLUMN en tenant"
---

<objective>
[BLOCKING] Reorganizar el schema de Postgres del modelo 1:1 Tenant=Negocio (Fase 1) al modelo Tenant(grupo) -> Negocio(s), aplicarlo en vivo contra bdgufnitakelyialjoqg, y regenerar los tipos. Es el prerequisito duro de TODO el CRUD de esta fase: build y typecheck pasan sin aplicar la migración (los tipos vienen de config, no de la DB live), creando un falso verde — por eso la aplicación en vivo se gatea con un checkpoint humano.

Purpose: Habilitar que WhatsApp, profesionales, servicios, clientes, turnos y horarios cuelguen del `negocio` (unidad operativa) y no del `tenant` (grupo/contenedor), con RLS que aísle por "el negocio pertenece a mi tenant" (D-09..D-12).
Output: `0003_tenant_negocio_split.sql`, seeds actualizadas, `scripts/verify-migration-0003.ts` (verificador vivo del shape post-0003), y `packages/db-types` regenerado desde el schema live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-CONTEXT.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-HANDOFF.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md
@.planning/phases/02-dashboard-y-datos-del-negocio/02-PATTERNS.md
@supabase/migrations/0001_schema_core.sql
@supabase/migrations/0002_rls_policies.sql
@scripts/apply-seed.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración 0003_tenant_negocio_split.sql</name>
  <files>supabase/migrations/0003_tenant_negocio_split.sql</files>
  <read_first>
    - supabase/migrations/0001_schema_core.sql (shapes exactos de las 14 tablas, FKs, uniques, índices compuestos)
    - supabase/migrations/0002_rls_policies.sql (helper auth_tenant_id(), naming *_aislamiento, header banner style)
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-HANDOFF.md §4 (spec canónica paso a paso)
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-RESEARCH.md "Schema Readiness Audit" (reflejo del spec)
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-PATTERNS.md (convención auth_negocio_ids() idéntica a auth_tenant_id())
  </read_first>
  <action>
    Crear la migración `0003` como una única transacción idempotente donde sea razonable, siguiendo la spec de 02-HANDOFF.md §4 al pie de la letra (implementa D-09..D-12, habilita SADMIN-01/02, BIZ-02, AUTH-03). Abrir con el banner `====` de la casa (ver 0002 líneas 1-22) citando D-09..D-12, SADMIN-01/02 y el threat model. Pasos:
    (1) `tenant`: ADD COLUMN nombre text; backfill `tenant.nombre` desde su `negocio.nombre` (hoy 1:1); SET NOT NULL; DROP COLUMN whatsapp_phone_number_id, waba_id, whatsapp_token, display_phone_number; DROP INDEX idx_tenant_whatsapp_phone_number_id (D-10: el WhatsApp deja de vivir en Tenant).
    (2) `negocio`: ADD COLUMN whatsapp_phone_number_id text, waba_id text, whatsapp_token text, display_phone_number text, activo boolean NOT NULL DEFAULT true; BACKFILL los cuatro valores WhatsApp desde el tenant padre ANTES de dropearlos de tenant (el orden importa: backfill negocio primero, luego drop en tenant); UNIQUE en whatsapp_phone_number_id; índice en whatsapp_phone_number_id (D-10: WhatsApp pasa a vivir en Negocio, cada uno con su propio número; D-11: se agrega `activo` para soft-delete de sucursal, junto a los campos de perfil — nombre/direccion/telefono/timezone/granularidad_min/horario_general — que ya vivían en `negocio` desde Fase 1 y no se tocan).
    (3) Las 11 tablas operativas (`profesional`, `horario_trabajo`, `servicio`, `profesional_servicio`, `cliente`, `turno`, `turno_servicio`, `bloqueo`, `conversacion`, `mensaje`, `recordatorio`): ADD COLUMN negocio_id uuid REFERENCES negocio(id) ON DELETE CASCADE; backfill 1:1 (para tablas hijas como horario_trabajo / profesional_servicio / turno_servicio, derivar el negocio_id desde el negocio_id del padre ya migrado, no desde tenant_id); SET NOT NULL; dropear FK+índice de tenant_id y DROP COLUMN tenant_id; agregar idx_<tabla>_negocio_id; actualizar índices compuestos (idx_turno_tenant_profesional_inicio -> por negocio_id; ídem bloqueo).
    (4) Uniques: `cliente` (tenant_id,telefono) -> (negocio_id,telefono); `conversacion` (tenant_id,cliente_id) -> (negocio_id,cliente_id).
    (5) EXCLUDE constraints (turno_no_overlap, bloqueo_no_overlap) keyean por profesional_id — NO tocar.
    (6) RLS: crear helper `auth_negocio_ids()` con hardening IDÉNTICO a auth_tenant_id() (LANGUAGE sql, STABLE, SECURITY DEFINER, SET search_path = '', REVOKE ALL FROM public, GRANT EXECUTE TO authenticated) que devuelva `SELECT id FROM public.negocio WHERE tenant_id = auth_tenant_id()`. DROP de las 11 policies `<tabla>_aislamiento` viejas (predicado tenant_id) y CREATE de las nuevas con mismo nombre/`FOR ALL TO authenticated` pero predicado `USING/WITH CHECK (negocio_id IN (SELECT auth_negocio_ids()))`. NO tocar las policies de perfil/tenant/negocio (siguen por auth_tenant_id(); negocio sigue `tenant_id = auth_tenant_id()`).
    NOTA de token: NO escribir ningún token real; `negocio.whatsapp_token` queda como columna nullable sin usar hasta Fase 7 (D-04/SEC-01).
    NO usar fences de código en descripciones; el archivo SQL es el entregable.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0003_tenant_negocio_split.sql','utf8').split('\n').filter(l=>!l.trim().startsWith('--')).join('\n'); const need=['auth_negocio_ids','ADD COLUMN nombre','activo boolean','ON DELETE CASCADE','auth_negocio_ids()','DROP COLUMN tenant_id']; const missing=need.filter(t=>!s.includes(t)); if(missing.length){console.error('FALTAN:',missing);process.exit(1)} const tables=['profesional','horario_trabajo','servicio','profesional_servicio','cliente','turno','turno_servicio','bloqueo','conversacion','mensaje','recordatorio']; const notMigrated=tables.filter(t=>!s.includes(t+'_aislamiento')); if(notMigrated.length){console.error('policies sin recrear:',notMigrated);process.exit(1)} console.log('0003 estructura OK')"</automated>
  </verify>
  <acceptance_criteria>
    El archivo existe; contiene el helper `auth_negocio_ids()` con `search_path = ''` + SECURITY DEFINER + GRANT solo a authenticated; agrega `tenant.nombre` y dropea las 4 columnas WhatsApp de tenant; mueve las 4 columnas WhatsApp + `activo` a negocio con backfill previo al drop; agrega `negocio_id` (FK ON DELETE CASCADE, NOT NULL, backfilled) y dropea `tenant_id` en las 11 tablas operativas; re-scopea los uniques de cliente/conversacion; recrea las 11 policies `<tabla>_aislamiento` con predicado por negocio_id; NO altera las EXCLUDE constraints ni las policies de perfil/tenant/negocio; no escribe ningún token real.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Actualizar seeds/fixtures + escribir verify-migration-0003.ts (verificador vivo del shape post-0003)</name>
  <files>scripts/apply-seed.ts, scripts/seed-fixtures.ts, scripts/verify-migration-0003.ts</files>
  <read_first>
    - scripts/apply-seed.ts (upsertTenant/ensureOwner actuales; UUIDs fijos; guard de aislamiento a bdgufnitakelyialjoqg)
    - scripts/seed-fixtures.ts (fixtures compartidas TENANT_A/TENANT_B)
    - scripts/verify-isolation.ts (patrón existente de script tsx contra la DB live: carga de env, cliente service_role, aserciones con exit code)
    - .planning/phases/02-dashboard-y-datos-del-negocio/02-HANDOFF.md §4 (spec canónica del shape post-0003 que este verificador debe assertar) y paso 8 (decisión de shape de seed)
  </read_first>
  <action>
    Parte A — seeds. Reescribir `apply-seed.ts` (y `seed-fixtures.ts` si comparte los objetos) al nuevo shape post-0003, manteniendo el guard de aislamiento que aborta si SUPABASE_URL no contiene 'bdgufnitakelyialjoqg'. Cambios: (a) `tenant` upsert pasa a llevar SOLO `nombre` + `activo` (quitar los campos WhatsApp de la fila tenant); (b) el WhatsApp (`whatsapp_phone_number_id`, `waba_id`, `display_phone_number`, `whatsapp_token: null`) + `activo` se cargan ahora en el upsert de `negocio`; (c) todas las filas operativas (profesional/servicio/cliente/turno) pasan a llevar `negocio_id` en vez de `tenant_id`. Para ejercitar el modelo 1:N, dar a TENANT_A un SEGUNDO negocio (misma tenant, distinto negocio_id/WhatsApp) además del negocio principal — así el selector de negocio del owner (D-13) tiene algo real que elegir; conservar TENANT_A y TENANT_B como DOS tenants distintos para que verify-isolation.ts siga probando aislamiento cross-tenant. Mantener los owners (`owner-norte@`, `owner-sur@`) ligados al `tenant_id` (no al negocio) vía `perfil` (rol owner). Reusar los UUID fijos existentes y agregar UUIDs nuevos determinísticos para el segundo negocio y sus filas operativas.

    Parte B — verificador vivo. Crear `scripts/verify-migration-0003.ts` (script tsx, mismo patrón que scripts/verify-isolation.ts: carga env, aborta si SUPABASE_URL no contiene 'bdgufnitakelyialjoqg', cliente service_role, exit 0 si todo OK / exit 1 con detalle si algo falla) que consulte el catálogo live (information_schema / pg_catalog vía RPC o queries a las tablas del sistema) y asserte el contrato de 02-VALIDATION.md Wave 0: (1) `tenant` tiene columnas nombre/activo y NO tiene whatsapp_phone_number_id/waba_id/whatsapp_token/display_phone_number; (2) `negocio` SÍ tiene esas cuatro columnas WhatsApp + activo; (3) cada una de las 11 tablas operativas (profesional, horario_trabajo, servicio, profesional_servicio, cliente, turno, turno_servicio, bloqueo, conversacion, mensaje, recordatorio) tiene `negocio_id` NOT NULL y NO tiene `tenant_id`; (4) existe la función `auth_negocio_ids()` y las 11 policies `<tabla>_aislamiento` referencian negocio_id (verificable vía pg_policies / pg_proc). Este script es el mecanismo de confirmación reutilizable que 02-VALIDATION.md exige sobre la tarea más riesgosa de la fase; reemplaza el "ojo humano al diff" por una aserción determinística. Requiere credenciales live: se ESCRIBE en esta tarea (estática) pero se EJECUTA en el checkpoint humano (Task 3) tras aplicar la migración.
    NO usar fences de código en descripciones.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/dashboard exec tsc --noEmit 2>/dev/null; node -e "const s=require('fs').readFileSync('scripts/apply-seed.ts','utf8'); if(!s.includes('bdgufnitakelyialjoqg')){console.error('perdió el guard de aislamiento');process.exit(1)} if(/from\(.tenant.\)[\s\S]{0,200}whatsapp_phone_number_id/.test(s)){console.error('tenant todavía carga WhatsApp');process.exit(1)} if(!/from\(.negocio.\)[\s\S]{0,300}whatsapp_phone_number_id/.test(s)){console.error('negocio no carga WhatsApp');process.exit(1)} const v=require('fs').readFileSync('scripts/verify-migration-0003.ts','utf8'); if(!v.includes('bdgufnitakelyialjoqg')){console.error('verify-migration-0003 sin guard de aislamiento');process.exit(1)} if(!v.includes('auth_negocio_ids')){console.error('verify-migration-0003 no chequea auth_negocio_ids');process.exit(1)} if(!v.includes('negocio_id')){console.error('verify-migration-0003 no chequea negocio_id');process.exit(1)} console.log('seed + verify-migration shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    `apply-seed.ts` conserva el guard de aislamiento a bdgufnitakelyialjoqg; `tenant` upsert solo lleva nombre+activo; el WhatsApp vive en el upsert de `negocio`; las filas operativas usan `negocio_id`; TENANT_A tiene 2 negocios (1:N) y siguen existiendo 2 tenants distintos; ningún token real se escribe. `scripts/verify-migration-0003.ts` existe, tipa, comparte el guard de aislamiento, y asserta live las cuatro condiciones del contrato Wave 0 (tenant sin WhatsApp / negocio con WhatsApp+activo / 11 tablas con negocio_id NOT NULL sin tenant_id / auth_negocio_ids() + policies por negocio_id).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [checkpoint:human-action] Aplicar 0003 en vivo + regenerar db-types + reseed + correr verify-migration-0003.ts</name>
  <action>Gate humano: aplicar la migración 0003 a bdgufnitakelyialjoqg, regenerar packages/db-types, re-aplicar el seed y ejecutar `scripts/verify-migration-0003.ts` (exit 0) como confirmación del shape live. Requiere credenciales reales que no están en el repo; ver how-to-verify.</action>
  <what-built>La migración `0003_tenant_negocio_split.sql` y los seeds actualizados están escritos y validados estructuralmente en el repo, pero NO se han aplicado a ninguna base de datos.</what-built>
  <how-to-verify>
    Esta acción requiere credenciales reales del proyecto bdgufnitakelyialjoqg que NO están en el repo. El humano debe, con su `.env` local poblado (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ACCESS_TOKEN):
    1. Confirmar explícitamente que el proyecto objetivo es `bdgufnitakelyialjoqg` (NUNCA el proyecto del restaurante `hzgunbftloevclkohcdf` — regla dura de CLAUDE.md).
    2. Aplicar `supabase/migrations/0003_tenant_negocio_split.sql` en vivo, vía `supabase db push` (con SUPABASE_ACCESS_TOKEN exportado para entorno no-TTY) o ejecutando el SQL con la Management API usando el PAT contra el ref bdgufnitakelyialjoqg específicamente.
    3. Regenerar los tipos: `supabase gen types typescript --project-id bdgufnitakelyialjoqg > packages/db-types/src/database.types.ts` y confirmar en el diff que `tenant` ya no tiene columnas WhatsApp, que `negocio` sí las tiene + `activo`, y que las 11 tablas operativas tienen `negocio_id` (no `tenant_id`).
    4. Re-ejecutar `pnpm exec tsx scripts/apply-seed.ts` para resembrar al nuevo shape.
    5. Verificación automatizada del shape live (mecanismo de confirmación reutilizable, no "ojo humano al diff"): ejecutar `pnpm exec tsx scripts/verify-migration-0003.ts` y confirmar exit code 0. Este script asserta contra la DB live que `tenant` no tiene columnas WhatsApp, que `negocio` las tiene + `activo`, que las 11 tablas operativas tienen `negocio_id` NOT NULL sin `tenant_id`, y que existe `auth_negocio_ids()` con las 11 policies `<tabla>_aislamiento` por negocio_id.
    Pega la salida de `verify-migration-0003.ts` (exit 0) + el diff resumido de `database.types.ts` (columnas movidas) como evidencia antes de aprobar.
  </how-to-verify>
  <resume-signal>Escribí "aplicada" cuando 0003 esté aplicada en vivo contra bdgufnitakelyialjoqg, db-types regenerado y seed re-aplicado; o describí el error si algo falló.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (authenticated JWT) -> Postgres | El owner consulta tablas operativas; RLS debe filtrar por negocio de su tenant |
| operador/humano -> DB live (service_role/PAT) | Aplicación de DDL con máximo privilegio; riesgo de tocar el proyecto equivocado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | auth_negocio_ids() SECURITY DEFINER helper | high | mitigate | Hardening idéntico a auth_tenant_id(): STABLE, SET search_path = '', REVOKE ALL FROM public, GRANT EXECUTE solo a authenticated (previene search_path hijacking) |
| T-02-02 | Information Disclosure | Ventana de migración: negocio_id NOT NULL con RLS ya activa | high | mitigate | Backfill de negocio_id ANTES de SET NOT NULL y antes de recrear policies; la migración corre como service_role (bypassa RLS) en una transacción, sin ventana donde una fila quede inalcanzable en tráfico normal |
| T-02-03 | Tampering | DDL aplicada al proyecto Supabase equivocado (restaurante) | high | mitigate | Guard en apply-seed.ts que aborta si SUPABASE_URL no contiene bdgufnitakelyialjoqg; checkpoint humano exige confirmación explícita del ref antes de aplicar |
| T-02-04 | Information Disclosure | whatsapp_token en columna plana | medium | accept | D-04/SEC-01: la columna existe pero NUNCA se escribe con un token real en Fase 2; encriptación (Vault/AES-GCM) diferida a Fase 7 |
</threat_model>

<verification>
- Estructura de 0003 validada por el check automatizado del Task 1.
- Seeds y `verify-migration-0003.ts` tipan (`tsc --noEmit`) y conservan el guard de aislamiento.
- Aplicación en vivo + regen de tipos + reseed confirmados por el humano en el checkpoint, con `scripts/verify-migration-0003.ts` en exit 0 como aserción determinística del shape live (evidencia: salida del script + diff de database.types.ts).
</verification>

<success_criteria>
El schema live de bdgufnitakelyialjoqg está en el modelo Tenant(nombre)->Negocio(s con WhatsApp)->operativas(negocio_id), con RLS por auth_negocio_ids(); packages/db-types refleja ese shape; los seeds re-aplican sin error. Ninguna otra base de datos fue tocada.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-y-datos-del-negocio/02-01-SUMMARY.md` when done
</output>
