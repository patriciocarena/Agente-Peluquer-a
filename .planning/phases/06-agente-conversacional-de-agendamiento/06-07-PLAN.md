---
phase: 06-agente-conversacional-de-agendamiento
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/src/conversation/responder.ts
  - apps/bot/src/conversation/responder.test.ts
  - apps/bot/src/conversation/systemPrompt.ts
  - apps/bot/evals/promptfooconfig.test.ts
autonomous: true
gap_closure: true
requirements: [BOT-01, BOT-04, BOT-05]
user_setup: []

must_haves:
  truths:
    - "Un mensaje que el cliente escribió en el turno N sigue presente en el history que recibe generateText en el turno N+1 (memoria multi-turno)."
    - "El bot avanza el agendamiento sin re-preguntar día/servicio ya contestados, y llega a buscarHorarios/confirmarTurno."
    - "Tras un tool-result de una tool de consulta, el bot NUNCA envía un mensaje vacío al cliente por WhatsApp."
    - "El system prompt (buildSystemPrompt) exige explícitamente narrar en texto el resultado de una tool de consulta."
  artifacts:
    - "apps/bot/src/conversation/responder.ts"
    - "apps/bot/src/conversation/responder.test.ts"
    - "apps/bot/src/conversation/systemPrompt.ts"
  key_links:
    - "responder() persiste { role: 'user', content: mensajeEntrante } en AMBOS caminos (feliz + error) antes de que parseConversationContext lo lea el próximo turno."
    - "El guard de empty-text corre antes de retornar finalText, de modo que ninguna cadena vacía llega a sendWhatsappMessage."
---

<objective>
Cerrar los 2 blockers encontrados en el UAT de cierre de Fase 6 (06-UAT.md, Tests 2 y 3), ambos diagnosticados con root cause confirmado por sesiones de debug independientes:

- **Gap 1 (memoria conversacional):** `responder()` nunca persiste el mensaje `{ role: "user", content: mensajeEntrante }` en `conversacion.context.messages` — solo persiste `result.response.messages` (que el AI SDK v7 documenta como SOLO lo generado por el modelo esa llamada). El modelo nunca ve lo que el cliente dijo en turnos anteriores, entra en loop pidiendo los mismos datos y nunca llega a `buscarHorarios`/`confirmarTurno`. Desbloquea además los Tests 4, 5 y 7 del UAT (reagendar/cancelar/guardrail), hoy `blocked_by: prior-phase`.
- **Gap 2 (texto vacío tras tool de consulta):** tras un tool-call exitoso de una tool de lectura (ej. `consultarNegocio` para precios), Gemini 2.5 Flash-Lite a veces devuelve `finishReason:"stop"` con `text:""` — el bot obtiene el dato pero no le dice nada al cliente. Es un quirk documentado y no-determinista del modelo (no eliminable de raíz); se mitiga con dos palancas bajo nuestro control: (a) una instrucción POSITIVA en el system prompt y (b) un guard de código que nunca envíe un mensaje en blanco.

Purpose: Restaurar el valor central del producto — agendar un turno real conversando por WhatsApp en varios mensajes — y que las consultas de precios/horarios comuniquen el dato real al cliente.
Output: `responder.ts` persiste el mensaje del cliente y nunca envía texto vacío; `systemPrompt.ts` exige narrar resultados de consulta; tests de regresión deterministas (fallan sin el fix, pasan con el fix) que mockean `generateText` — nunca llaman a Gemini real.

Scope acotado: SOLO estos 2 gaps. NO tocar tools de lectura/escritura, `closingLanguage.ts`, `conversationState.ts`, ni `evals/` salvo el freshness guard de `promptfooconfig.test.ts`. El resto de la Fase 6 está verificado y funcionando.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-agente-conversacional-de-agendamiento/06-UAT.md
@.planning/debug/responder-history-drops-user-messages.md
@.planning/debug/responder-empty-text-after-tool-call.md

@apps/bot/src/conversation/responder.ts
@apps/bot/src/conversation/responder.test.ts
@apps/bot/src/conversation/systemPrompt.ts
@apps/bot/src/conversation/conversationState.ts
@apps/bot/evals/promptfooconfig.test.ts

# Contexto de interfaz para el executor:
# - responder(conversacion, mensajeEntrante, deps=defaultDeps): Promise<string> — firma posicional, NO cambiar.
# - result.response.messages (AI SDK v7): SOLO mensajes generados por el modelo esa llamada (assistant/tool), nunca echo del input.
# - parseConversationContext(context) -> { messages, needsHuman }; serializeConversationContext({ messages, needsHuman }) -> objeto plano. Pass-through puro, NO filtra role:user.
# - ResponderDeps: { generateText, model, buildTools, negocioScoped, log }. Los tests inyectan generateText fake vía deps — NUNCA Gemini real.
# - SAFE_FALLBACK_MESSAGE ya existe y se reutiliza en el camino de error y el gate D-12.
# - CLOSING_LANGUAGE_LEXICON se interpola en el prompt (D-12) y lo verifica promptfooconfig.test.ts con toContain (positivo, no negative-grep).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistir el mensaje del cliente en el historial (Gap 1 — memoria multi-turno)</name>
  <files>apps/bot/src/conversation/responder.ts, apps/bot/src/conversation/responder.test.ts</files>
  <behavior>
    - Round-trip (test nuevo, el que cierra el gap): tras un turno 1 con `context: {}`, el objeto persistido vía `updateConversacion` debe contener `{ role: "user", content: <mensaje del turno 1> }`. Al alimentar ese context persistido como `conversacion.context` de un turno 2, `generateText` debe recibir en `messages` el mensaje `role:"user"` del turno 1 (además del history assistant y el mensaje actual). SIN el fix este test falla porque el history de turno 2 no tiene ningún `role:"user"` previo.
    - Camino feliz: el array persistido es `[...history, { role:"user", content: mensajeEntrante }, ...messagesToPersist]` — el mensaje del cliente queda ENTRE el history previo y los mensajes generados por el modelo, respetando el orden cronológico real del turno.
    - Camino de error (catch de generateText): el array persistido es `[...history, { role:"user", content: mensajeEntrante }]` — el mensaje que disparó el error también sobrevive (hoy la línea del catch persiste solo `history` y lo descarta). `needsHuman` sigue en `true` y el retorno sigue siendo `SAFE_FALLBACK_MESSAGE`.
    - Reescribir (no solo re-correr) el test existente "persiste result.response.messages + needsHuman ..." (aprox. línea 227-240): hoy afirma el contrato CON el bug (`messages: [...history, ...responseMessages]`) como correcto. Debe pasar a esperar `messages: [...history, { role:"user", content:"hola de nuevo" }, ...responseMessages]`.
    - Los tests CR-02 existentes (persistedMessages `toContainEqual` el mensaje seguro / `not.toContainEqual` el fantasma) deben seguir verdes: el array ahora también incluye el `role:"user"`, pero las aserciones `toContainEqual` no se rompen.
  </behavior>
  <action>
    Cerrar Gap 1 de 06-UAT.md (root cause en .planning/debug/responder-history-drops-user-messages.md, CONFIRMADO por inspección de código + docs AI SDK v7). Requisito BOT-01/BOT-04: el bot debe recordar lo que el cliente contestó en turnos previos.

    En `responder.ts`, introducir una única constante local del mensaje del cliente al inicio de `responder()`, p.ej. `userMessage = { role: "user" as const, content: mensajeEntrante }`, y reutilizarla en los TRES lugares: (1) el array `messages` que se pasa a `deps.generateText` (hoy construye el objeto inline en la línea ~195 — reemplazar por `userMessage` para tener una sola fuente), (2) el `serializeConversationContext` del camino de error (hoy `{ messages: history, ... }` en la línea ~220 → `{ messages: [...history, userMessage], needsHuman: true }`), y (3) el `serializeConversationContext` del camino feliz (hoy `{ messages: [...history, ...messagesToPersist], ... }` en la línea ~267-270 → `{ messages: [...history, userMessage, ...messagesToPersist], needsHuman }`).

    NO tocar `conversationState.ts` (ya es pass-through puro, confirmado en el debug — el bug es 100% en cómo responder.ts construye lo que persiste). NO cambiar la firma de `responder`. El gate D-12 y `replaceLastAssistantText` no cambian: siguen operando sobre `messagesToPersist` (= `result.response.messages`), que NO incluye el mensaje del cliente — el `userMessage` se antepone recién en el merge final, así que `replaceLastAssistantText` sigue actuando solo sobre el último assistant.

    En `responder.test.ts`: reescribir el test de persistencia existente (aprox. línea 227-240) al nuevo contrato, agregar el test de round-trip descrito en <behavior> (dos llamadas a `responder`, la 2da con el context producido por la 1ra vía el mock de `updateConversacion`), y agregar una aserción al test del camino de error (aprox. línea 242-259) de que el `messages` persistido contiene `{ role:"user", content:"hola" }`. Seguir el patrón determinista existente: `generateText` siempre fake vía `deps`, `vi.mock("../db/client.js", ...)` a nivel de módulo.
  </action>
  <verify>
    <automated>cd apps/bot && pnpm test src/conversation/responder.test.ts</automated>
  </verify>
  <done>El mensaje `{ role:"user", content: mensajeEntrante }` aparece en `conversacion.context.messages` tras cada turno (feliz y error); el test de round-trip demuestra que un mensaje user del turno N llega al `messages` de generateText en el turno N+1; toda la suite de responder.test.ts pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrucción positiva de narración en el system prompt (Gap 2a)</name>
  <files>apps/bot/src/conversation/systemPrompt.ts, apps/bot/evals/promptfooconfig.test.ts</files>
  <behavior>
    - Freshness guard nuevo en `promptfooconfig.test.ts` (mismo estilo `toContain`/`toMatch`, sin llamar a Gemini): `buildSystemPrompt()` contiene una frase que exige responder en texto tras usar una herramienta de consulta. SIN la nueva sección del prompt, este test falla.
    - Los guards existentes de `promptfooconfig.test.ts` (lexicón D-12 verbatim, framing D-13 "El negocio de esta conversación es fijo", D-08 "confirmación explícita") deben seguir verdes — la nueva sección se AGREGA, no reemplaza nada.
  </behavior>
  <action>
    Cerrar Gap 2a de 06-UAT.md (mitigación bajo control del proyecto; el quirk del modelo en sí NO es eliminable — ver .planning/debug/responder-empty-text-after-tool-call.md). Requisito BOT-05: el bot debe responder consultas de precios/horarios con datos reales.

    En `systemPrompt.ts`, agregar una nueva sección POSITIVA al string de `buildSystemPrompt()` (p.ej. bajo un encabezado tipo "# Siempre comunicá el resultado de una consulta"). La regla debe decir, en la voz informal argentina ya establecida, que cada vez que use una herramienta de consulta (precios, horarios, disponibilidad, estado de un turno) y esta devuelva datos, SIEMPRE debe escribir después un mensaje de texto en lenguaje natural comunicándole ese dato al cliente en el mismo turno; que usar la herramienta no alcanza y que nunca debe terminar un turno en silencio después de consultar un dato. Esta regla es COMPLEMENTARIA a la "Regla de oro" D-12 (que es un negativo: no inventar): D-12 dice de dónde tiene que salir el dato, esta dice que el dato real SIEMPRE hay que ponerlo en palabras. Mantené el mismo registro y brevedad que el resto del prompt.

    Actualizar el comentario de cabecera del archivo (la lista de guardrails codificados) para incluir esta nueva regla de narración, coherente con cómo hoy documenta D-01/05/06/08/12/13.

    En `promptfooconfig.test.ts`, agregar el test de freshness descrito en <behavior> que asegura que la nueva instrucción sigue presente en `buildSystemPrompt()` — usando una sub-cadena estable de la nueva sección (o un `toMatch` de un patrón robusto), consistente con los guards existentes del archivo.
  </action>
  <verify>
    <automated>cd apps/bot && pnpm test evals/promptfooconfig.test.ts</automated>
  </verify>
  <done>`buildSystemPrompt()` incluye una instrucción positiva explícita de narrar en texto el resultado de una tool de consulta; el nuevo freshness guard y los tres guards existentes de promptfooconfig.test.ts pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard de código contra texto vacío tras un tool-result (Gap 2b)</name>
  <files>apps/bot/src/conversation/responder.ts, apps/bot/src/conversation/responder.test.ts</files>
  <behavior>
    - Tool-result exitoso + `result.text` vacío → el bot NO devuelve `""`. Reintenta la generación UNA vez con un nudge de continuación; si el reintento produce texto no vacío, ese texto es el que se retorna y se persiste (junto con los mensajes generados del primer intento + el reintento), y el nudge sintético NO se persiste como si el cliente lo hubiera dicho.
    - Reintento también vacío (o el reintento lanza) → `finalText = SAFE_FALLBACK_MESSAGE`; nunca se retorna cadena vacía.
    - Texto vacío SIN ningún tool-result en `result.steps` → sin reintento, se degrada directo a `SAFE_FALLBACK_MESSAGE` (nunca se envía blanco a WhatsApp).
    - `result.text` no vacío → comportamiento intacto: ni reintento ni fallback (el guard no debe alterar el camino sano ni interferir con el gate D-12 existente).
    - El mensaje `{ role:"user" }` del cliente (Task 1) sigue persistiéndose correctamente en el camino del reintento y del fallback.
  </behavior>
  <action>
    Cerrar Gap 2b de 06-UAT.md (segundo control bajo nuestro alcance; ver debug session). El guard corre DESPUÉS del bloque del gate D-12 existente (el gate no dispara con texto vacío porque `hasClosingLanguage("")` es falso, así que son independientes) y ANTES del `serializeConversationContext` final.

    Agregar un helper puro en `responder.ts`, p.ej. `hadToolResult(steps)` que devuelve true si algún step tiene un `toolResults` no vacío (reutilizando el tipo `ResponderGenerateTextResult["steps"]` ya definido). Definir una constante local de nudge de continuación (texto en español que le pide al modelo comunicar en un mensaje de texto el dato que la tool ya devolvió) — priorizar no perder el dato real ya obtenido por la tool, por eso se reintenta en vez de degradar directo.

    Lógica del guard (en prosa, no inline en el <action>): si `finalText.trim()` es vacío y `hadToolResult(result.steps)` es true, hacer un único reintento de `deps.generateText` con los mismos `model`/`system`/`tools`/`stopWhen`/`temperature`/`maxOutputTokens`/`maxRetries`, y `messages` = el history + el userMessage + los mensajes generados del primer intento (`result.response.messages`) + un mensaje sintético de nudge; envolver el reintento en try/catch. Si el reintento devuelve texto no vacío: `finalText` pasa a ese texto y `messagesToPersist` pasa a `[...result.response.messages, ...retry.response.messages]` (SIN el nudge sintético, para no ensuciar el historial). Si el reintento vuelve vacío o lanza: `finalText = SAFE_FALLBACK_MESSAGE` (dejar `messagesToPersist` como estaba). Si `finalText.trim()` es vacío pero NO hubo tool-result: `finalText = SAFE_FALLBACK_MESSAGE` sin reintento. En todos los casos, el `serializeConversationContext` final sigue anteponiendo el `userMessage` (Task 1).

    NO cambiar la firma de `responder`, ni el gate D-12, ni `SAFE_FALLBACK_MESSAGE`. NO llamar a Gemini real en tests.

    En `responder.test.ts`, agregar tests deterministas usando `generateTextImpl` (o `mockResolvedValueOnce` encadenado) para simular: (1) primer intento con `text:""` + un step con toolResults + segundo intento con texto real → el reply es el texto real y no queda el nudge en lo persistido; (2) ambos intentos vacíos → reply `SAFE_FALLBACK_MESSAGE`; (3) `text:""` sin ningún tool-result → reply `SAFE_FALLBACK_MESSAGE` sin segundo intento (`generateText` llamado una sola vez); (4) `text` no vacío → `generateText` llamado una sola vez y reply intacto (no regresión del camino sano).
  </action>
  <verify>
    <automated>cd apps/bot && pnpm test src/conversation/responder.test.ts</automated>
  </verify>
  <done>Ningún camino de `responder()` retorna cadena vacía; ante tool-result + texto vacío reintenta una vez y prioriza narrar el dato real, degradando a SAFE_FALLBACK_MESSAGE solo si el reintento también falla; el camino sano (texto no vacío) no dispara reintentos; toda la suite de responder.test.ts pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente WhatsApp → responder() | El texto del cliente (`mensajeEntrante`) es input no confiable que ahora se PERSISTE en `conversacion.context.messages` y se relee cada turno. |
| responder() → Gemini (generateText) | El nudge de reintento y el history persistido se reenvían al modelo el próximo turno. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07-01 | Tampering/Injection | mensaje user persistido en context | medium | mitigate | El mensaje se persiste como `content` de un `role:"user"` (dato, no instrucción de sistema); el aislamiento por negocio ya lo garantizan las closures de las tools (D-13) y `negocioScoped`, no el texto del prompt. El framing anti-injection D-13 del system prompt sigue intacto (verificado por promptfooconfig.test.ts). No se agrega ninguna interpolación de ids al prompt. |
| T-06-07-02 | Information Disclosure | guard de empty-text / nudge de reintento | low | mitigate | El nudge de continuación es texto fijo local (no incorpora datos de otro cliente/negocio) y el reintento reutiliza exactamente el mismo `system`/`tools` scopeados; el fallback `SAFE_FALLBACK_MESSAGE` nunca revela datos. |
| T-06-07-03 | Denial of Service | reintento extra de generateText | low | accept | El guard hace como máximo UN reintento adicional por turno y solo cuando hubo tool-result con texto vacío (caso no-determinista poco frecuente); el `maxRetries` interno del SDK no cambia. Costo acotado y aceptable en el free/paid tier. |

Sin instalación de paquetes nuevos (no aplica gate de legitimidad npm). Sin nuevos secrets ni superficie de red.
</threat_model>

<verification>
- `cd apps/bot && pnpm test` — la suite completa del bot verde (los ~196 tests previos + los nuevos de regresión), sin llamadas live a Gemini.
- `cd apps/bot && pnpm exec tsc --noEmit` — typecheck verde (o el comando de typecheck del repo).
- Inspección: `result.response.messages` NO se muta para inyectar el user message (el merge se hace solo en el `serializeConversationContext` final); el gate D-12 y CR-02 (`replaceLastAssistantText`) siguen operando igual.
- Ningún camino de `responder()` puede retornar `""`.
</verification>

<success_criteria>
- Gap 1 cerrado: un mensaje `role:"user"` del turno N sobrevive en el history que recibe `generateText` en el turno N+1 (probado por test de round-trip determinista); persistido en camino feliz y de error.
- Gap 2 cerrado: (a) el system prompt exige narrar el resultado de una tool de consulta; (b) `responder()` nunca envía texto vacío — reintenta una vez priorizando el dato real y degrada a `SAFE_FALLBACK_MESSAGE` si hace falta.
- Cada fix viene con un test que falla SIN el fix y pasa CON el fix (regresión), siguiendo el patrón mockeado existente.
- Scope acotado: solo `responder.ts`, `responder.test.ts`, `systemPrompt.ts`, `promptfooconfig.test.ts` modificados.
</success_criteria>

<output>
Create `.planning/phases/06-agente-conversacional-de-agendamiento/06-07-SUMMARY.md` when done
</output>
