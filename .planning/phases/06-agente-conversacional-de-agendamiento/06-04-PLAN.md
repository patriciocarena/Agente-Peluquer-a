---
phase: 06-agente-conversacional-de-agendamiento
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/bot/src/conversation/tools/confirmarTurno.ts
  - apps/bot/src/conversation/tools/confirmarTurno.test.ts
  - apps/bot/src/conversation/tools/reagendarTurno.ts
  - apps/bot/src/conversation/tools/reagendarTurno.test.ts
  - apps/bot/src/conversation/tools/cancelarTurno.ts
  - apps/bot/src/conversation/tools/cancelarTurno.test.ts
autonomous: true
requirements: [BOT-04, BOT-09, BOT-10]
must_haves:
  truths:
    - "confirmarTurno devuelve el turno_id real de bookAppointment — nunca un id inventado (D-12/BOT-04)"
    - "confirmarTurno NO usa skipBookingWindow (el bot respeta la ventana 60min/30d, a diferencia del owner)"
    - "reagendarTurno delega en rescheduleAppointment con la misma forma de input que el dashboard (D-09/BOT-10)"
    - "cancelarTurno envuelve cancelAppointment compartido, nunca escribe estado='cancelado' inline (BOT-09/AVAIL-04)"
    - "Ninguna tool de escritura recibe negocioId/clienteId como campo de su inputSchema (D-13)"
  artifacts:
    - path: "apps/bot/src/conversation/tools/confirmarTurno.ts"
      provides: "tool confirmarTurno wrapping bookAppointment, surface real turnoId"
      contains: "bookAppointment"
    - path: "apps/bot/src/conversation/tools/reagendarTurno.ts"
      provides: "tool reagendarTurno wrapping rescheduleAppointment"
      contains: "rescheduleAppointment"
    - path: "apps/bot/src/conversation/tools/cancelarTurno.ts"
      provides: "tool cancelarTurno wrapping cancelAppointment (compartido)"
      contains: "cancelAppointment"
  key_links:
    - from: "apps/bot/src/conversation/tools/cancelarTurno.ts"
      to: "cancelAppointment"
      via: "import desde @turnosbot/availability-engine (plan 06-01)"
      pattern: "cancelAppointment"
    - from: "apps/bot/src/conversation/tools/confirmarTurno.ts"
      to: "bookAppointment"
      via: "execute closure sobre negocioId/clienteId + freshData"
      pattern: "bookAppointment"
---

<objective>
Las tres tools de ESCRITURA del agente, cada una envolviendo la función de dominio compartida correspondiente (nunca reimplementando la escritura): `confirmarTurno` (envuelve `bookAppointment`, devuelve el `turno_id` real — BOT-04/D-12), `reagendarTurno` (envuelve `rescheduleAppointment`, misma forma de input que el dashboard — BOT-10/D-09), y `cancelarTurno` (envuelve el `cancelAppointment` compartido del plan 06-01 — BOT-09/AVAIL-04). `negocioId`/`clienteId` cerrados en closure; `bookAppointment`/`rescheduleAppointment` heredan la re-validación anti-cache y la traducción del `23P01` (slot_taken).

Purpose: Estas tools son el único camino por el que el bot muta la agenda. Son la fuente del `turno_id` real que el gate D-12 (plan 06-05) exige antes de dejar salir una confirmación. Al envolver funciones compartidas, el bot y el dashboard nunca discrepan (AVAIL-04) y el bot hereda automáticamente las garantías de concurrencia (CORE-05).
Output: 3 tools de escritura + tests, listas para el tool-loop del responder (plan 06-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agente-conversacional-de-agendamiento/06-RESEARCH.md
@.planning/phases/06-agente-conversacional-de-agendamiento/06-AI-SPEC.md
@.planning/phases/06-agente-conversacional-de-agendamiento/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tool confirmarTurno (bookAppointment, surface turno_id real — BOT-04/D-12)</name>
  <read_first>
    - packages/availability-engine/src/booking.ts (bookAppointment líneas 233-... firma + BookAppointmentResult líneas 192-196: { ok:true; turnoId; precioTotal } | validation_error | slot_taken | insert_error; bookAppointmentInputSchema)
    - apps/dashboard/app/actions/turnos.ts (crearTurnoManual líneas 60-101 — patrón caller: buildAvailabilityData(freshData) inmediatamente antes de bookAppointment; mapBookResult líneas 39-52. OJO: el dashboard usa skipBookingWindow:true; el BOT NO — debe respetar la ventana 60min/30d)
    - apps/bot/src/conversation/buildBotAvailabilityData.ts (freshData scopeado por negocio, plan 06-02)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-PATTERNS.md (Pattern 7 anti-cache: fetch freshData DENTRO del execute; Pattern 1 negocioId/clienteId closure-captured; confirmarTurno debe devolver el turnoId real)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-AI-SPEC.md (Pitfall 3: D-12 se enforcea en código sobre result.steps — la tool DEBE devolver el turnoId en su valor estructurado para que el gate del plan 06-05 lo inspeccione)
  </read_first>
  <behavior>
    - Test: confirmarTurno con { profesionalId, servicioIds, slotInicio, slotFin } y un bookAppointment mockeado que devuelve { ok:true, turnoId:"...", precioTotal } → la tool devuelve una estructura que INCLUYE el turnoId real (para el gate D-12).
    - Test: confirmarTurno llama bookAppointment SIN skipBookingWindow (o con skipBookingWindow ausente/false) — assert sobre el input que recibió bookAppointment mockeado.
    - Test: negocioId y clienteId pasados a bookAppointment provienen de la closure, NO del inputSchema (assert: no están en el schema; el mock recibe los valores capturados).
    - Test: bookAppointment devuelve { ok:false, reason:"slot_taken" } → la tool devuelve una estructura de error mapeada (sin turnoId), con copy D-03-aligned ("ese horario se acaba de ocupar, ¿probamos otro?"), NUNCA un turnoId inventado.
    - Test: reason validation_error/insert_error → estructura de error genérica sin turnoId.
  </behavior>
  <action>
    Crear apps/bot/src/conversation/tools/confirmarTurno.ts factory `confirmarTurnoTool(negocioId: string, clienteId: string, deps?)` devolviendo `tool({...})`. `inputSchema`: `z.object({ profesionalId: uuidLike, servicioIds: z.array(uuidLike).min(1), slotInicio: z.iso.datetime(), slotFin: z.iso.datetime() })` — SIN negocioId/clienteId. `execute`: fetch `freshData = await buildBotAvailabilityData(negocioId)` DENTRO del execute (anti-cache, Pattern 7), llama `bookAppointment({ negocioId, profesionalId, clienteId, serviceIds: input.servicioIds, inicio: input.slotInicio, fin: input.slotFin }, { supabase, freshData })` — negocioId/clienteId de la closure, y SIN skipBookingWindow (el bot respeta la ventana 60min/30d). Mapear BookAppointmentResult a una estructura de retorno que en el caso ok incluya el turnoId real (crítico para el gate D-12 del plan 06-05) y en los casos de error incluya un mensaje user-facing sin turnoId (slot_taken → copy de re-oferta, otros → genérico). El cliente supabase se inyecta vía deps (mismo service_role usado por negocioScoped). Patrón de deps opcional inyectable (bookAppointment + buildBotAvailabilityData mockeables).
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec vitest run src/conversation/tools/confirmarTurno.test.ts</automated>
  </verify>
  <done>confirmarTurno surface el turnoId real en el caso ok, no usa skipBookingWindow, deriva negocioId/clienteId de la closure, mapea slot_taken sin inventar turnoId; tests verdes.</done>
  <acceptance_criteria>
    - `grep -c "bookAppointment" apps/bot/src/conversation/tools/confirmarTurno.ts` >= 1.
    - `grep -c "skipBookingWindow" apps/bot/src/conversation/tools/confirmarTurno.ts` es 0 (el bot no la usa).
    - El inputSchema no contiene negocioId ni clienteId (test estructural).
    - El test "surface turnoId real en caso ok" pasa y el test "slot_taken no devuelve turnoId" pasa.
    - vitest de confirmarTurno.test.ts verde.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tools reagendarTurno (rescheduleAppointment) + cancelarTurno (cancelAppointment compartido)</name>
  <read_first>
    - packages/availability-engine/src/booking.ts (rescheduleAppointment líneas ~344-410 — firma + result; cancelAppointment agregado en 06-01 — firma { negocioId, turnoId }, deps { supabase })
    - apps/dashboard/app/actions/turnos.ts (reagendarTurno líneas 134-163 — patrón caller EXACTO a espejar: fetchTurnoServicios → serviceIds → buildAvailabilityData → rescheduleAppointment con MISMA forma de input; NO skipBookingWindow para el bot)
    - apps/bot/src/db/negocioScoped.ts (turnoServicios() para traer los serviceIds del turno a reagendar)
    - packages/availability-engine/src/index.ts (confirmar que cancelAppointment/CancelAppointmentResult/uuidLike se exportan tras 06-01)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-CONTEXT.md (D-08: cancelar requiere confirmación explícita — el guardrail vive en el prompt/responder; la tool solo ejecuta; D-09: reagendar misma lógica de dominio que el dashboard)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-PATTERNS.md (Pattern 4 discriminated union; el anti-pattern explícito: NO escribir estado='cancelado' inline en la tool)
  </read_first>
  <behavior>
    - Test reagendarTurno: dado un turnoId, trae serviceIds vía turnoServicios (scopeado), arma freshData, y llama rescheduleAppointment con { negocioId, turnoId, profesionalId, serviceIds, inicio, fin } — MISMA forma que el dashboard (assert sobre el input del mock).
    - Test reagendarTurno: reason slot_taken → estructura de error mapeada (re-oferta), ok → estructura con turnoId.
    - Test cancelarTurno: llama cancelAppointment({ negocioId, turnoId }, { supabase }) — negocioId de la closure; devuelve estructura ok con turnoId cancelado.
    - Test cancelarTurno: reason not_found/already_cancelled/update_error → estructura de error mapeada, NUNCA un estado='cancelado' escrito inline (assert: el mock de cancelAppointment fue llamado; ningún .update inline en la tool).
    - Test ambas: negocioId no está en el inputSchema (closure-captured).
  </behavior>
  <action>
    Crear apps/bot/src/conversation/tools/reagendarTurno.ts factory `reagendarTurnoTool(negocioId, clienteId, deps?)` → `tool({...})`. `inputSchema`: `z.object({ turnoId: uuidLike, profesionalId: uuidLike, nuevoSlotInicio: z.iso.datetime(), nuevoSlotFin: z.iso.datetime() })` — SIN negocioId. `execute`: trae serviceIds del turno vía `negocioScoped(negocioId).turnoServicios()` filtrando por turnoId (los serviceIds no cambian al reagendar); fetch freshData vía buildBotAvailabilityData; llama `rescheduleAppointment({ negocioId, turnoId, profesionalId, serviceIds, inicio: input.nuevoSlotInicio, fin: input.nuevoSlotFin }, { supabase, freshData })` — MISMA forma que reagendarTurno del dashboard (D-09), sin skipBookingWindow. Mapear result (ok→turnoId, slot_taken→re-oferta, otros→genérico).

    Crear apps/bot/src/conversation/tools/cancelarTurno.ts factory `cancelarTurnoTool(negocioId, clienteId, deps?)` → `tool({...})`. `inputSchema`: `z.object({ turnoId: uuidLike })` — SIN negocioId. `execute`: llama `cancelAppointment({ negocioId, turnoId: input.turnoId }, { supabase })` importado de `@turnosbot/availability-engine` (plan 06-01). PROHIBIDO escribir `estado='cancelado'` inline — solo delegar en la función compartida (AVAIL-04, anti-pattern explícito de PATTERNS.md). Mapear CancelAppointmentResult (ok→confirmación de cancelación con turnoId; not_found/already_cancelled/update_error→mensajes user-facing). La confirmación explícita D-08 la garantiza el system prompt + gate del responder (plan 06-05), no esta tool.

    Crear los tests correspondientes con mocks de rescheduleAppointment/cancelAppointment y negocioScoped, sin Gemini.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec vitest run src/conversation/tools/reagendarTurno.test.ts src/conversation/tools/cancelarTurno.test.ts</automated>
  </verify>
  <done>reagendarTurno delega en rescheduleAppointment con la misma forma que el dashboard; cancelarTurno delega en cancelAppointment compartido sin UPDATE inline; ninguna acepta negocioId en el schema; tests verdes.</done>
  <acceptance_criteria>
    - `grep -c "rescheduleAppointment" apps/bot/src/conversation/tools/reagendarTurno.ts` >= 1 y `grep -c "skipBookingWindow" apps/bot/src/conversation/tools/reagendarTurno.ts` es 0.
    - `grep -c "cancelAppointment" apps/bot/src/conversation/tools/cancelarTurno.ts` >= 1.
    - `grep -c 'estado.*cancelado' apps/bot/src/conversation/tools/cancelarTurno.ts` es 0 (sin UPDATE de estado inline — todo vía cancelAppointment).
    - Los inputSchema de ambas tools no contienen negocioId (test estructural).
    - vitest de ambos tests verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp user → tool write inputs | El modelo llena turnoId/profesionalId/slots desde el mensaje del usuario (no confiable) |
| bot service (service_role) → turno writes | El bot escribe sin RLS; toda mutación pasa por funciones compartidas que filtran por negocio_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Repudiation / Tampering | confirmarTurno (confirmación fantasma) | mitigate | La tool devuelve el turnoId real de bookAppointment; en error NO devuelve turnoId — el gate D-12 (plan 06-05) bloquea el envío de "confirmado" sin turnoId real (BOT-04) |
| T-06-12 | Tampering | doble reserva / condición de carrera | mitigate | bookAppointment/rescheduleAppointment heredan la re-validación anti-cache + la GiST 23P01→slot_taken (CORE-05) — las tools no eluden esa capa |
| T-06-13 | Tampering | cancelación divergente bot/dashboard | mitigate | cancelarTurno delega en cancelAppointment compartido (grep de UPDATE inline == 0) — una sola fuente de verdad (AVAIL-04/D-09) |
| T-06-14 | Elevation of Privilege | negocioId cross-tenant en write tools | mitigate | negocioId closure-captured, nunca en inputSchema (test estructural) — imposible reagendar/cancelar un turno de otro negocio (D-13) |
| T-06-15 | Tampering | cancelación sin confirmación (D-08) | mitigate | La tool solo ejecuta; el gate anti-cancelación-implícita vive en el system prompt + responder (plan 06-05) — reforzado offline por E5 |
| T-06-SC | Tampering | npm installs | mitigate | Este plan no instala paquetes (installs en 06-02) |
</threat_model>

<verification>
- confirmarTurno/reagendarTurno/cancelarTurno testeadas con las funciones de dominio mockeadas, sin Gemini.
- confirmarTurno surface turnoId real; cancelarTurno sin UPDATE inline; reagendar misma forma que el dashboard.
- Ninguna write tool acepta negocioId en su inputSchema.
- typecheck de apps/bot verde.
</verification>

<success_criteria>
- El bot muta la agenda SOLO vía funciones de dominio compartidas (AVAIL-04): book/reschedule/cancel.
- confirmarTurno provee el turnoId real que el gate D-12 requiere (BOT-04).
- cancelar/reagendar por WhatsApp usan la misma lógica que el dashboard (BOT-09/BOT-10/D-09).
</success_criteria>

<output>
Create `.planning/phases/06-agente-conversacional-de-agendamiento/06-04-SUMMARY.md` when done
</output>
