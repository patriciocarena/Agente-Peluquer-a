---
phase: 06-agente-conversacional-de-agendamiento
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/bot/src/conversation/tools/buscarHorarios.ts
  - apps/bot/src/conversation/tools/buscarHorarios.test.ts
  - apps/bot/src/conversation/tools/asignarProfesional.ts
  - apps/bot/src/conversation/tools/asignarProfesional.test.ts
  - apps/bot/src/conversation/tools/consultarNegocio.ts
  - apps/bot/src/conversation/tools/consultarNegocio.test.ts
autonomous: true
requirements: [BOT-01, BOT-02, BOT-03, BOT-05, BOT-06, BOT-07, BOT-08]
must_haves:
  truths:
    - "buscarHorarios devuelve slots reales de computeSlots — el bot nunca inventa disponibilidad (D-12)"
    - "asignarProfesional resuelve 'sin preferencia' vía autoAssign, no una heurística propia (D-04)"
    - "consultarNegocio responde precios/horarios/estado de turno leyendo datos reales scopeados por negocio"
    - "Ninguna tool recibe negocioId como campo de su inputSchema — el scope viene de la closure (D-13)"
  artifacts:
    - path: "apps/bot/src/conversation/tools/buscarHorarios.ts"
      provides: "tool buscarHorarios wrapping computeSlots"
      contains: "computeSlots"
    - path: "apps/bot/src/conversation/tools/asignarProfesional.ts"
      provides: "tool asignarProfesional wrapping autoAssign"
      contains: "autoAssign"
    - path: "apps/bot/src/conversation/tools/consultarNegocio.ts"
      provides: "tool de consulta de precios/horarios/estado (BOT-05/06/08)"
      contains: "negocioScoped"
  key_links:
    - from: "apps/bot/src/conversation/tools/buscarHorarios.ts"
      to: "computeSlots + buildBotAvailabilityData"
      via: "execute closure sobre db=negocioScoped(negocioId)"
      pattern: "computeSlots"
---

<objective>
Las tres tools de LECTURA/CONSULTA del agente (no escriben en la DB): `buscarHorarios` (envuelve `computeSlots` — disponibilidad real, BOT-01/03/07), `asignarProfesional` (envuelve `autoAssign` para "sin preferencia", BOT-02/D-04), y `consultarNegocio` (precios de servicios BOT-05, horarios de profesionales BOT-06, estado de un turno existente BOT-08 — todo leído vía `negocioScoped`). Cada tool cierra sobre `db = negocioScoped(negocioId)` capturado ANTES del LLM; ninguna acepta `negocioId` como parámetro del modelo (D-13/BOT-11).

Purpose: Estas tools son el único camino por el que el modelo obtiene datos reales del negocio. Todo horario/precio/estado que el bot mencione al cliente proviene de estas llamadas, nunca del conocimiento del modelo (D-12). Se testean con `computeSlots`/`autoAssign` reales inyectados sobre fixtures deterministas, sin llamar a Gemini.
Output: 3 tools de lectura + sus tests, listas para registrarse en el tool-loop del responder (plan 06-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agente-conversacional-de-agendamiento/06-RESEARCH.md
@.planning/phases/06-agente-conversacional-de-agendamiento/06-AI-SPEC.md
@.planning/phases/06-agente-conversacional-de-agendamiento/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tool buscarHorarios (computeSlots) + asignarProfesional (autoAssign)</name>
  <read_first>
    - apps/bot/src/conversation/buildBotAvailabilityData.ts (creado en 06-02 — la fuente de AvailabilityData scopeada por negocio)
    - packages/availability-engine/src/computeSlots.ts (firma de computeSlots y ComputeSlotsInput; reglas de ventana 60min/30d en constants.ts)
    - packages/availability-engine/src/autoAssign.ts (líneas 35-53 — autoAssign(slotsByProfessional) puro, tie-break determinista; el wrapper NO reimplementa heurística)
    - packages/availability-engine/src/booking.ts (uuidLike líneas 66-77 — importarlo del barrel per 06-01; patrón de inputSchema)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-AI-SPEC.md (Section 3 entry point — shape de tool({ description, inputSchema, execute }); Pitfall 4 negocioId nunca en inputSchema; Pitfall 5 fechas: NO z.string() suelto, constreñir formato)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-PATTERNS.md (Shared Pattern 1 negocioId closure-captured; Pattern 2 uuidLike; Pattern 5 negocio() por tenant_id)
  </read_first>
  <behavior>
    - Test buscarHorarios: dado un negocioId + fixtures de disponibilidad, execute con { servicioIds, fechaDeseada } devuelve los mismos slots que computeSlots devuelve para ese input (assert de igualdad contra computeSlots real inyectado sobre fixtures de packages/availability-engine/src/__fixtures__/rows.ts).
    - Test buscarHorarios: con profesionalId opcional, filtra a ese profesional; sin profesionalId, devuelve slots de todos los elegibles.
    - Test buscarHorarios: la tool NUNCA recibe negocioId en su inputSchema (assert estructural: parsear el shape del inputSchema y verificar que no tiene la clave negocioId).
    - Test buscarHorarios: un input con servicioId no-UUID-like es rechazado por el inputSchema (uuidLike) antes de llamar computeSlots.
    - Test asignarProfesional: dado un mapa slots-por-profesional, devuelve el resultado de autoAssign (mismo professionalId+slot que la función pura); ante mapa vacío devuelve null/estructura equivalente sin lanzar.
  </behavior>
  <action>
    Crear apps/bot/src/conversation/tools/buscarHorarios.ts que exporte una factory `buscarHorariosTool(negocioId: string, deps?)` devolviendo un objeto `tool({...})` del AI SDK (`import { tool } from "ai"`). `inputSchema`: `z.object({ servicioIds: z.array(uuidLike).min(1), profesionalId: uuidLike.optional(), fechaDeseada: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, "YYYY-MM-DD"), franjaHoraria: z.enum(["manana","tarde","noche"]).optional() })` — importar `uuidLike` desde `@turnosbot/availability-engine` (exportado en 06-01). NUNCA incluir negocioId en el schema. `execute`: cierra sobre negocioId, llama `buildBotAvailabilityData(negocioId)` para freshData, arma `ComputeSlotsInput` con `date = input.fechaDeseada` + `professionalId = input.profesionalId` + `serviceIds = input.servicioIds`, llama `computeSlots(input, freshData)`, y devuelve slots reales (2-3 opciones concretas per D-03 — o todas y que el prompt filtre a 2-3; documentá la elección). Aplicar el patrón de deps opcional inyectable (computeSlots + buildBotAvailabilityData inyectables para test).

    Crear apps/bot/src/conversation/tools/asignarProfesional.ts factory `asignarProfesionalTool(negocioId, deps?)` que envuelve `autoAssign` (importado del barrel). `execute`: para el/los servicio(s) + fecha pedidos, computa slots por profesional (reusando buildBotAvailabilityData + computeSlots) y pasa el mapa a `autoAssign`, devolviendo `{ professionalId, slot }` o null. NO reimplementar la heurística de selección.

    Crear los tests correspondientes usando los fixtures de packages/availability-engine/src/__fixtures__/rows.ts, inyectando computeSlots/autoAssign reales y buildBotAvailabilityData mockeado que devuelve los fixtures — sin llamar a Gemini.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec vitest run src/conversation/tools/buscarHorarios.test.ts src/conversation/tools/asignarProfesional.test.ts</automated>
  </verify>
  <done>buscarHorarios devuelve slots reales de computeSlots sin negocioId en el schema; asignarProfesional delega en autoAssign; tests verdes.</done>
  <acceptance_criteria>
    - `grep -c "computeSlots" apps/bot/src/conversation/tools/buscarHorarios.ts` >= 1.
    - `grep -c "negocioId" apps/bot/src/conversation/tools/buscarHorarios.ts` >= 1 pero NINGUNA ocurrencia dentro del `inputSchema`/`z.object` (verificable por el test estructural del bloque behavior).
    - `grep -c "autoAssign" apps/bot/src/conversation/tools/asignarProfesional.ts` >= 1.
    - `grep -c "uuidLike" apps/bot/src/conversation/tools/buscarHorarios.ts` >= 1 y `grep -c "z.uuid\|z.string().uuid" apps/bot/src/conversation/tools/buscarHorarios.ts` es 0.
    - vitest de ambos tests verde.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tool consultarNegocio — precios / horarios profesionales / estado de turno (BOT-05/06/08)</name>
  <read_first>
    - apps/bot/src/db/negocioScoped.ts (accessors servicios/profesionales/horariosTrabajo/turnos/turnoServicios — la tool lee solo por acá)
    - packages/availability-engine/src/booking.ts (líneas 104-151 buildTurnoServicioSnapshots/sumPrecioTotal — al describir un turno EXISTENTE se leen los *_snapshot congelados de turno_servicio, NUNCA servicio.precio vivo; Pattern 6 de PATTERNS.md)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-PATTERNS.md (Shared Pattern 6: nunca re-derivar precio/duración de un join vivo — solo snapshots; Pattern 1 negocioId closure-captured)
    - .planning/phases/06-agente-conversacional-de-agendamiento/06-CONTEXT.md (D-07: el cliente se identifica por su número; el estado de turno consultado es del clienteId de la conversación — nunca de otro cliente)
  </read_first>
  <behavior>
    - Test: consultarNegocio con { tipo: "precios" } devuelve la lista de servicios activos del negocio con nombre + precio + duración (leídos de servicio vía negocioScoped).
    - Test: consultarNegocio con { tipo: "horarios_profesional", profesionalId } devuelve los bloques de horario_trabajo de ese profesional.
    - Test: consultarNegocio con { tipo: "estado_turno" } devuelve los turnos NO cancelados del clienteId de la conversación (cerrado en closure), con precio/servicios leídos de los *_snapshot de turno_servicio (no del servicio vivo).
    - Test: consultarNegocio con estado_turno NUNCA devuelve turnos de otro clienteId (assert: se filtra por el clienteId capturado, no por un parámetro del modelo).
    - Test: la tool no acepta negocioId ni clienteId como campos de su inputSchema.
  </behavior>
  <action>
    Crear apps/bot/src/conversation/tools/consultarNegocio.ts factory `consultarNegocioTool(negocioId: string, clienteId: string, deps?)` devolviendo `tool({...})`. `inputSchema`: `z.object({ tipo: z.enum(["precios","horarios_profesional","estado_turno"]), profesionalId: uuidLike.optional() })` — SIN negocioId/clienteId (ambos closure-captured, D-13/D-07). `execute` ramifica por `tipo`: "precios" → `negocioScoped(negocioId).servicios()` filtrando activos, devuelve nombre/precio/duracion_min; "horarios_profesional" → `negocioScoped(negocioId).horariosTrabajo()` para el profesionalId; "estado_turno" → `negocioScoped(negocioId).turnos()` filtrando por el `clienteId` capturado y `estado != 'cancelado'`, y para cada turno leer sus `turno_servicio` (accessor turnoServicios) usando los campos `*_snapshot` congelados (nombre_snapshot/precio_snapshot/duracion_snapshot) para el precio/servicios — NUNCA re-derivar de `servicio.precio` vivo (Pattern 6/AVAIL-03). Devolver estructura serializable que el modelo pueda narrar. Patrón de deps opcional inyectable para test.
  </action>
  <verify>
    <automated>pnpm --filter @turnosbot/bot exec vitest run src/conversation/tools/consultarNegocio.test.ts</automated>
  </verify>
  <done>consultarNegocio responde precios/horarios/estado leyendo solo vía negocioScoped, filtra estado_turno por el clienteId capturado, usa snapshots congelados; tests verdes.</done>
  <acceptance_criteria>
    - `grep -c "negocioScoped(" apps/bot/src/conversation/tools/consultarNegocio.ts` >= 1 y `grep -c "supabaseAdmin\.from\|createClient" apps/bot/src/conversation/tools/consultarNegocio.ts` es 0.
    - `grep -ci "snapshot" apps/bot/src/conversation/tools/consultarNegocio.ts` >= 1 (estado_turno usa los snapshots congelados).
    - El inputSchema no contiene negocioId ni clienteId (verificado por el test estructural).
    - vitest de consultarNegocio.test.ts verde con los 5 casos.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp user → tool inputs | El modelo llena los inputSchema con contenido derivado del mensaje del usuario (no confiable) |
| negocioId/clienteId (server-resolved) → tool scope | Ambos vienen de la closure resuelta en inboundWorker.ts, nunca del modelo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | consultarNegocio estado_turno | mitigate | El filtro por clienteId es closure-captured, no parámetro del modelo — un mensaje "mostrame los turnos de otro cliente" no puede cambiar el scope (D-07/D-13) |
| T-06-08 | Elevation of Privilege | todas las tools de lectura | mitigate | negocioId nunca en el inputSchema (test estructural) — prompt-injection no puede reasignar tenant (Pattern 1/BOT-11) |
| T-06-09 | Tampering | inputs de fecha/uuid del modelo | mitigate | uuidLike + regex YYYY-MM-DD en el inputSchema cortan input malformado/alucinado antes de tocar la DB (V5, Pitfall 5) |
| T-06-10 | Information Disclosure | precios de turnos existentes | mitigate | estado_turno lee *_snapshot congelados, nunca el join vivo — reporta el precio real del turno, no uno recalculado que podría filtrar cambios (AVAIL-03) |
| T-06-SC | Tampering | npm installs | mitigate | Este plan no instala paquetes (installs en 06-02) |
</threat_model>

<verification>
- buscarHorarios/asignarProfesional/consultarNegocio testeados con computeSlots/autoAssign/negocioScoped reales/mockeados sobre fixtures, sin Gemini.
- Ninguna tool acepta negocioId/clienteId en su inputSchema (tests estructurales verdes).
- typecheck de apps/bot verde.
</verification>

<success_criteria>
- El bot obtiene disponibilidad, auto-asignación e info del negocio SOLO de datos reales scopeados (D-12).
- Aislamiento estructural: scope closure-captured, nunca del modelo (BOT-11/D-13).
</success_criteria>

<output>
Create `.planning/phases/06-agente-conversacional-de-agendamiento/06-03-SUMMARY.md` when done
</output>
